# Task 5.1 exact involution note

## Purpose and canonical scope

This is the canonical post-theta Task 5.1 note named in `GAPS.md` and
`plans/2026-03-28-task5_1-status-alignment.md`. It records exactly what the verified
glued-model computation proves, and exactly what remains outside the current audit
boundary.

The exact source of truth is:

- `computations/task5_1_involution.sage`
- `computations/task5_1_primitive_results.txt`
- `computations/task5_1_theta_results.txt`

`notes/task5_1_route_reset.md` remains canonical for the failure of the old ad hoc
eigenbasis-splice route and for the corrected route order.

## Exact lattice setup

The current script constructs an explicit even unimodular ambient lattice of signature
`(3,19)` by gluing

- `S_Co = <2> + <-2>^10`
- `T_expected = <2>^2 + <-2>^9`

inside an exact model of `Λ_K3`.

Within that explicit ambient lattice, the script then:

- embeds `S_Co` primitively;
- computes the actual orthogonal complement `T := S_Co^⊥`;
- defines `θ` only after that primitive orthogonal decomposition is real, by `-I` on the
  embedded `S_Co` and `+I` on the computed complement.

## Verified primitive decomposition facts

The checked primitive-complement artifact establishes the following exact facts for the
explicit glued ambient lattice:

- ambient lattice rank `22`, signature `(3,19)`, determinant `-1`;
- embedded `S_Co` primitive `True`, with Gram diagonal
  `[2, -2, -2, -2, -2, -2, -2, -2, -2, -2, -2]`;
- computed orthogonal complement rank `11`, signature `(2,9)`, determinant `-2048`;
- cross pairing between the embedded `S_Co` basis and the computed complement basis is
  zero;
- actual complement discriminant group order `2048`, invariants
  `(2, 2, 2, 2, 2, 2, 2, 2, 2, 2, 2)`, and `Brown(q_T(actual)) = 1 = -Brown(q_S) mod 8`.

These are exact facts from `computations/task5_1_primitive_results.txt`, not
literature-level geometric interpretation.

## Verified involution facts

The checked theta artifact establishes the following exact facts for the same explicit
glued ambient lattice:

- `θ` is integral in the ambient basis;
- `θ^2 = I`;
- `θ^T G θ = G`;
- `θ` acts by `-I` on the embedded `S_Co`;
- `θ` acts by `+I` on the computed complement.

The artifact outcome line is:

> `PASS: exact theta verification succeeded on the actual glued ambient lattice.`

Therefore the repo now has an explicit exact lattice model in which the sign action
defines an involution `θ ∈ O(Λ_K3)` on that model.

## Claims

This note supports the following claims, and only these claims:

- the old Task 5.1 route has been replaced by the primitive-embedding / true
  orthogonal-complement / sign-action route recorded in `notes/task5_1_route_reset.md`;
- the raw lattice-construction blocker is resolved for the explicit glued ambient model
  implemented in `computations/task5_1_involution.sage`;
- on that explicit model, the sign action is integral and defines an isometry of the
  ambient K3 lattice;
- future repo prose should cite this note for the exact post-theta computational status
  instead of reconstructing the result from stdout transcripts.

## Non-claims

This note does **not** claim any of the following:

- that the computation alone proves the broader geometric interpretation of the
  involution for the full Coble-surface or K3-cover story;
- that the computation replaces the literature-backed period-domain, Torelli, or moduli
  layer;
- that the checked artifacts alone establish every literature-level identification of
  the computed complement with the intended geometric `T_Co` beyond this explicit glued
  model;
- that CARAT is part of the main proof route for the indefinite rank-22 problem.

## Literature boundary

When repo prose moves from this exact lattice statement to geometric or moduli
interpretation, it should cite the literature spine rather than extending the claim of
this note.

- For the Coble sextic, blowup model, K3 cover, and the lattice generated by the
  hyperplane and exceptional classes, use `notes/literature_claim_map.md` and
  `REFERENCES.md` under Coble, Nikulin, and related entries.
- For the signature `(2,9)` period-domain picture and the 9-dimensional Type IV layer,
  use `notes/literature_claim_map.md` together with Scattone, Sterk, and Dolgachev–Kondō
  in `REFERENCES.md`.
- For passage from lattice/Hodge data to moduli statements, use Friedman and the other
  canonical references in `REFERENCES.md`.

In the language of `PLAN.md` and `REFERENCES.md`, this computation is supporting exact
evidence. It is not a substitute for standard references.
