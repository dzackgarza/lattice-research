# Coble Task And Moduli Facts

Trigger: implementing or decomposing GOAL.md Coble/K3/Enriques tasks, writing category/spec cards around Coble lattices, or deciding what counts as verified.

Stored task facts:

- Task 1.1 rational sextic: degree-6 plane curve has arithmetic genus `10`; ten ordinary nodes each contribute delta `1`; ten nodes give geometric genus `0`. Birational parametrization check is generic-fiber degree `1`.
- Task 1.2 lattice pipeline: on the blowup of `P^2` at ten nodes, `Pic(S)` has generators `H, E_1,...,E_10`, signature `(1,10)`. The K3 double-cover pullback doubles intersections, giving expected Gram `diag(2,-2,...,-2) = <2> + <-2>^10`. Discriminant group/Nikulin invariants are outputs from the computed lattice, not inputs.
- Task 1.3 embedding chain: `T_Co -> T_En -> T_dP -> Lambda_K3`, with `Lambda_K3 = U^3 + E_8(-1)^2`; `T_dP` rank `9`, `T_En` rank `10`, `T_Co` rank `11`, signature `(2,9)`. Primitive embeddings and complements route through Nikulin/Oscar, not hand-coded embedding matrices.
- Task 2.1 discriminant orbit input: start only after Task 1.2 computes and verifies the relevant rank-11 lattice and discriminant form. If `A_T = (Z/2Z)^11`, then `|A_T| = 2048` and stored theory expects `528` isotropic vectors including zero.
- Task 2.2 orbit lifting: divisibility must be derived from the actual Gram model. In the diagonal even model, pairings are even, so primitive isotropic vectors have divisibility `2`, not `1`.
- Task 3.1 arithmetic group: `Gamma_Co = Stab_{O(T_En)}(h_Co) ∩ Z_{O(T_En)}(theta)`, with `h_Co^2 = 2`; explicit generators are not yet computed in theory.
- Task 3.2 isotropic planes: maximal isotropic dimension is `2` for signature `(2,9)`. For primitive isotropic plane `J`, `J^perp/J` is negative definite of rank `7`. The predicted unique orbit and `A_1^7` quotient are unverified computationally; a prior bounded search found examples but did not perform the GAP orbit computation.
- Task 4.1 Coxeter/parabolics: `W(S_Co)` is generated by reflections in roots `r in S_Co` with `r^2=-2`. The claim of a unique maximal parabolic `~B_7(2)` is unverified; an old script hand-coded adjacency instead of deriving the Coxeter diagram. Recompute through Vinberg or equivalent.
- Task 5.1 involution: need an explicit `22x22` matrix `theta in O(Lambda_K3)` with `theta^2 = I`, `theta^T G theta = G`, `Lambda_K3^{+theta} = T_Co` signature `(2,9)`, and `Lambda_K3^{-theta} = S_Co` signature `(1,10)`. Old glued-lattice script was not independently verified.
- Task 6.1 surgery vector/slc: stored mathematical fact is `l_i = h_Co . alpha_i = 0` by orthogonality in the stated setup. The slc stability verification remains genuine geometry work; old script booleans do not prove it.

Stored moduli facts:

- The Coble period-domain dimension claim is: if `T_Co` has signature `(2,9)`, the associated Type IV period domain has complex dimension `9`.
- Coble/K3/period-domain facts should be described as standard literature unless the repo is proving a narrower computed example.
- Canonical source chain for the 9-dimensional moduli picture: Scattone for Type IV/Baily-Borel compactification, Sterk for Enriques cusp/boundary machinery, Dolgachev-Kondo for Coble/nodal Enriques moduli framing, Friedman for the Torelli passage.
- Coble rational sextic/blowup model source chain: Coble 1917/1929, Thas 1994 for a constructed rational 10-nodal sextic from Desargues configuration, Coolidge for classical point constraints, Pieroni 2026 for modern Coble-surface treatment and `E_10 = Num(X)` context.
- K3 lattice/embedding source chain: Nikulin 1979 for 2-elementary lattices, primitive embeddings, discriminant forms, uniqueness/surjectivity; Huybrechts for K3 lattice/Hodge/moduli background.
- Modern compactification source chain: AEGS 2023 for degree-2 Enriques compactification and IAS/nodal K3 cover language; Pieroni 2026 for Coble surfaces/projective models/automorphisms.

Stored verification policy:

- Never promote "expected notation" to data. Compute discriminant groups, Nikulin invariants, complements, divisibility, orbit lifts, stabilizers, centralizers, and cusp counts from the actual lattice/geometric pipeline.
- Old deleted/invalid scripts cannot be used as proof. A future agent may preserve their mathematical theorem statements but must discard their self-validating computational status.
- For every task card derived from these facts, classify the claim as source-backed, computed-and-verified, expected-target, or unverified. Do not blur these states.

Source anchors: `theory/foundations/coble-task-background.md`, `theory/moduli/moduli-dimension-claim.md`, `theory/references/index.md`, `theory/references/claim-map.md`.

Verification: a future card using this memory should name the task number, the stored fact being used, and whether the fact is already source-backed or still requires computation.
