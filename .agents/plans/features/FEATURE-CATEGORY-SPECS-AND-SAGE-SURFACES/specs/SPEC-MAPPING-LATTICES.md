---
id: SPEC-MAPPING-LATTICES
trackerStatus:
  type: spec
parents:
- '[[FEATURE-CATEGORY-SPECS-AND-SAGE-SURFACES]]'
dependsOn:
- '[[PHASE-MAPPING-DOC-SPEC-CONVERSION-AND-MATHEMATICAL-AUDIT]]'
title: Track lattices mapping spec
status: in-progress
priority: critical
requirement: Convert category_specs/lattices/docs/MAPPING.md into a tracked spec surface
  and audit it for Sage-source completeness, mathematical correctness, and well-typed
  lattice, form, dual, discriminant, isometry, and algorithm signatures.
acceptanceCriteria:
- Source paths category_specs/lattices/docs/MAPPING.md and category_specs/lattices/docs/SAGE_INVENTORY.md
  are reviewed.
- Every admitted row states caller category, complete input data, hypotheses, return
  object, and source evidence.
- Methods are placed at the highest category where they are mathematically well-defined.
- Nonmathematical targets and raw Sage implementation containers are rejected or marked
  interop-only.
- Missing Sage surfaces or mathematical ambiguities become tracked cards or decisions.
complexity: 90
tags:
- FEATURE-CATEGORY-SPECS-AND-SAGE-SURFACES
constructorNameInventories:
- owner: category_specs.lattices.LatticesCategory._Constructors
  sageConstructorNames:
  - IntegralLattice
  - IntegralLatticeDirectSum
  - IntegralLatticeGluing
  projectOwnedConstructionNames: []
---
# MAPPING.md — Lattices Subtree Spec

This tracked spec is the canonical mapping surface converted from `category_specs/lattices/docs/MAPPING.md`.

Source inventory: `category_specs/lattices/docs/SAGE_INVENTORY.md`.

Current status: the source-visibility and source-definition name audits are closed for
the checked Sage 10.7 corpus, but final symbol-by-symbol semantic audit remains open.
Do not use the historical review log below as a parent-feature or GOAL discharge claim.

## Review Gates

- Preserve every inventoried Sage surface by mapping it to a project mathematical surface, a named constructor path, a mathematically justified non-mapping, or a tracked decision.
- Place every method at the highest category where the operation is mathematically well-defined; subcategories inherit methods from supercategories.
- Every method row must contain a mathematical sentence that explains why the method
  exists before it names the Sage implementation. Use the form: "In [category],
  [construction exists/has property]." Examples: "In an additive category,
  `Hom(X,Y)` is an abelian group and composition is bilinear"; "In an abelian category,
  kernels and cokernels exist."
- State caller category, input data, hypotheses, return object or codomain, and source evidence before implementation depends on the row.
- Reject nonmathematical targets, raw Sage implementation containers, variadic option bags, and smoke-driven interface weakening.
- Route unresolved mathematical ownership, typing, or source-coverage gaps to tracked decisions or tasks before implementation proceeds.

## Source Coverage Ledger

- Sage environment checked: SageMath 10.7, installed source under `/home/dzack/miniforge3/envs/sage/lib/python3.12/site-packages`.
- Local inventory checked: `category_specs/lattices/docs/SAGE_INVENTORY.md`.
- Installed Sage source files checked or named by the local inventory:
  - `sage`
  - `sage/modules/all.py`
  - `sage/modules/free_quadratic_module.py`
  - `sage/modules/fg_pid/fgp_module.py`
  - `sage/modules/fg_pid/fgp_element.py`
  - `sage/modules/fg_pid/fgp_morphism.py`
  - `sage/modules/free_module_homspace.py`
  - `sage/categories/homset.py`
  - `sage/categories/homsets.py`
  - `sage/modules/torsion_quadratic_module.py`
  - `sage/modules/free_quadratic_module_integer_symmetric.py`
  - `sage/quadratic_forms/all.py`
  - `sage/quadratic_forms/quadratic_form.py`
  - `sage/quadratic_forms/quadratic_form__evaluate.pyx`
  - `sage/quadratic_forms/constructions.py`
  - `sage/quadratic_forms/random_quadraticform.py`
  - `sage/quadratic_forms/binary_qf.py`
  - `sage/quadratic_forms/bqf_class_group.py`
  - `sage/quadratic_forms/ternary_qf.py`
  - `sage/quadratic_forms/extras.py`
  - `sage/quadratic_forms/special_values.py`
  - `sage/quadratic_forms/quadratic_form__variable_substitutions.py`
  - `sage/quadratic_forms/quadratic_form__local_normal_form.py`
  - `sage/quadratic_forms/quadratic_form__count_local_2.py`
  - `sage/quadratic_forms/quadratic_form__local_density_congruence.py`
  - `sage/quadratic_forms/quadratic_form__local_density_interfaces.py`
  - `sage/quadratic_forms/quadratic_form__theta.py`
  - `sage/quadratic_forms/quadratic_form__neighbors.py`
  - `sage/quadratic_forms/quadratic_form__reduction_theory.py`
  - `sage/quadratic_forms/quadratic_form__ternary_Tornaria.py`
  - `sage/quadratic_forms/quadratic_form__siegel_product.py`
  - `sage/quadratic_forms/quadratic_form__mass.py`
  - `sage/quadratic_forms/quadratic_form__mass__Siegel_densities.py`
  - `sage/quadratic_forms/quadratic_form__mass__Conway_Sloane_masses.py`
  - `sage/quadratic_forms/quadratic_form__local_representation_conditions.py`
  - `sage/quadratic_forms/quadratic_form__split_local_covering.py`
  - `sage/quadratic_forms/qfsolve.py`
  - `sage/quadratic_forms/quadratic_form__equivalence_testing.py`
  - `sage/quadratic_forms/genera/all.py`
  - `sage/quadratic_forms/genera/genus.py`
  - `sage/quadratic_forms/genera/normal_form.py`
  - `sage/quadratic_forms/genera/spinor_genus.py`
  - `sage/geometry/all.py`
  - `sage/geometry/toric_lattice.py`
  - `sage/geometry/toric_lattice_element.pyx`
  - `sage/categories/lattice_posets.py`
  - `sage/categories/finite_lattice_posets.py`
  - `sage/groups/matrix_gps/isometries.py`
- Category-provider boundary check:
  - Searched: installed Sage 10.7 category source tree under
    `/home/dzack/miniforge3/envs/sage/lib/python3.12/site-packages/sage/categories`
    via `tree -L 2`, plus source reads of `lattice_posets.py` and
    `finite_lattice_posets.py`.
  - Found: Sage's category provider with the overlapping word "lattice" is
    order-theoretic `LatticePosets()` / `FiniteLatticePosets()`; the former inventory
    tokens `bilinear_modules.py`, `free_bilinear_modules.py`, `lattices.py`,
    `rational_lattices.py`, `torsion_bilinear_modules.py`, and
    `discriminant_quadratic_forms.py` are not installed Sage category-provider paths in
    this Sage tree.
  - Conclusion: inference from the installed Sage source tree — algebraic
    formed-module/lattice categories are project-local vocabulary here, while Sage's
    category-level lattice provider is the order-theoretic name-collision boundary
    mapped below.
  - Confidence: High for this installed Sage 10.7 environment.
  - Gaps: Does not claim anything about future Sage releases or out-of-tree Sage
    branches.
- Import probe caveat: direct `sage -python` imports of several `sage.categories.*` modules raised `ImportError: cannot import name Category`; completeness work therefore uses installed source files and inventories as the durable source surface unless that environment issue is separately resolved.
- Completeness status: this ledger records the checked source corpus for installed
  Sage 10.7 plus the active local category-spec surfaces. The source-visibility
  reconciliation formerly routed through
  `[[TASK-MAPPING-DOC-COMPLETENESS-RESEARCH]]` is complete; the remaining lattice
  obligation is the final symbol-by-symbol semantic audit of every relevant Sage
  lattice/form/module surface, not a live unknown about the absent retired
  `src/lattices/categories/...` paths.

## Sage 10.7 Source Reconciliation

This pass reconciles the local inventory against installed Sage 10.7 and the current
lattice-redesign doctrine. The mapping below is authoritative for Sage surfaces that are
missing from, inherited into, or only partially represented in the converted table.
Current Sage remains implementation evidence only: no row imports Sage's
ambient-vector-space lattice convention into the public semantics.

### Definition Grounding

- Local doctrine checked: `category-abc-spec.md`,
  `lattice-interface-style-guide.md`, `lattice-redesign-corrections-spec.md`,
  `category_specs/lattices/docs/SAGE_INVENTORY.md`,
  `category_specs/lattices/__init__.py`,
  `category_specs/forms/chain.py`,
  `category_specs/forms/subcategories/integral.py`,
  `category_specs/forms/subcategories/free_bilinear.py`,
  `category_specs/forms/subcategories/torsion_quadratic_modules.py`,
  `category_specs/lattices/subcategories/constructions/dual_objects.py`,
  `category_specs/lattices/subcategories/constructions/discriminant_groups.py`,
  and `category_specs/modules/subcategories/finitely_presented_over_pid.py`.
- Sage 10.7 source checked: `sage/modules/free_quadratic_module.py`,
  `sage/modules/free_quadratic_module_integer_symmetric.py`,
  `sage/modules/fg_pid/fgp_module.py`,
  `sage/modules/fg_pid/fgp_element.py`,
  `sage/modules/fg_pid/fgp_morphism.py`,
  `sage/modules/torsion_quadratic_module.py`,
  `sage/quadratic_forms/all.py`,
  `sage/quadratic_forms/quadratic_form.py`,
  `sage/quadratic_forms/quadratic_form__evaluate.pyx`,
  `sage/quadratic_forms/constructions.py`,
  `sage/quadratic_forms/random_quadraticform.py`,
  `sage/quadratic_forms/binary_qf.py`,
  `sage/quadratic_forms/bqf_class_group.py`,
  `sage/quadratic_forms/ternary_qf.py`,
  `sage/quadratic_forms/extras.py`,
  `sage/quadratic_forms/special_values.py`,
  `sage/modules/all.py`,
  `sage/quadratic_forms/genera/all.py`,
  `sage/geometry/all.py`,
  `sage/categories/homset.py`,
  `sage/categories/homsets.py`,
  `sage/categories/lattice_posets.py`,
  `sage/categories/finite_lattice_posets.py`,
  `sage/quadratic_forms/quadratic_form__equivalence_testing.py`,
  `sage/quadratic_forms/quadratic_form__local_field_invariants.py`, and
  `sage/groups/matrix_gps/isometries.py`.
- Mathematical model: lattices are presented free modules with forms. The selected
  generators are presentation data. A generator or basis change creates a distinct
  object, possibly with an isometry witness, not the same object by equality.
- Element divisibility is the submodule or ideal generated by
  `{b(v,w) : w in M}` in the form codomain. Coordinate gcds are at most
  implementation witnesses under extra hypotheses and are not the definition.

### Constructor and Presentation Reconciliation

| Sage surface | Source evidence | Spec owner or classification | Reconciliation |
| --- | --- | --- | --- |
| `FreeQuadraticModule(base_ring, rank, inner_product_matrix, sparse=False, inner_product_ring=None)` and `FreeQuadraticModule_generic.__init__(base_ring, rank, degree, inner_product_matrix, sparse=False)` | `free_quadratic_module.py:86`, `310`; Sage docs `free_quadratic_module.html` | `Modules(R).Free().FiniteRank().WithForms().Bilinear()` constructor path | Admit the public factory as an interop-backed constructor from a commutative ring `R`, finite rank `n`, and a bilinear form matrix coercible to an `n x n` matrix over `R`; the matrix is not required by Sage docs/source to be symmetric or definite. Selected generators/presentation data are supplied by the returned ambient/subobject route. Sage canonicalizes the form by coercing `rank` to `int`, rebuilding `inner_product_matrix` in `MatrixSpace(R,n)`, making that matrix immutable, caching by `(R, n, matrix, sparse)`, and selecting field, PID, integral-domain, or generic ambient backend from the base-ring category. These canonicalization, cache, backend-class, `sparse`, ambient `degree`, and ambient-space basis facts are runtime/interop data, not public lattice constructor semantics. The currently unsupported `inner_product_ring` branch is an explicit missing form-codomain implementation obligation because Sage raises before constructing when it is supplied. |
| `QuadraticSpace(K, dimension, inner_product_matrix, sparse=False)` and `InnerProductSpace` | `free_quadratic_module.py:190`, `223`; Sage docs `free_quadratic_module.html` | field-valued free bilinear module constructor path, not lattice-owned | Classify as the field-only free bilinear constructor shape. Public input data are a field `K`, finite dimension, and bilinear matrix over `K`; the source rejects nonfields and non-boolean `sparse`, then delegates to `FreeQuadraticModule`. `sparse` remains backend storage. The returned object is a free bilinear module over a field, with lattice promotion only when a later integral presentation and nondegenerate symmetric integral hypotheses are witnessed. |
| Package export routes `sage.modules.all.{FreeModule, VectorSpace, span, FreeQuadraticModule, QuadraticSpace, InnerProductSpace, vector, free_module_element, zero_vector, random_vector, linear_transformation, FilteredVectorSpace, MultiFilteredVectorSpace, IntegralLattice, TorsionQuadraticForm}`, `sage.quadratic_forms.genera.all.{Genus, LocalGenusSymbol, is_GlobalGenus}`, and `sage.geometry.all.ToricLattice` | `modules/all.py:16`, `18`, `24`, `31`, `37`; `free_module_element.pyx:157`, `618`, `714`, `792`; `vector_space_morphism.py:338`; `filtered_vector_space.py:402`; `multi_filtered_vector_space.py:100`; `quadratic_forms/genera/all.py:8`; `geometry/all.py:14` | public import/export boundary plus already-owned module helper routes, not new constructor owners | Preserve as package import evidence only. Export aliases do not create additional mathematical surfaces or change method ownership. `FreeModule`, `VectorSpace`, and `span` stay with ordinary free-module and vector-space construction. `vector` and `free_module_element` are aliases for module-element construction from finite entry data over an inferred or named base ring; `zero_vector` is the zero element in a named finite-rank free module; `random_vector` is sample-data generation using the base ring's random-element method; none are lattice constructors or proof witnesses. `linear_transformation` is a constructor for a morphism in the category of finite-dimensional vector spaces over a field, supplied by a matrix, explicit domain/codomain plus matrix, basis-image data, or a callable on basis elements. If the same syntax is used for finite free modules over a non-field, the mapping must state the module category and the finite free presentation hypotheses separately. `FilteredVectorSpace` and `MultiFilteredVectorSpace` are filtered vector-space structures with filtration data, not formed lattices. This lattice mapping records only that Sage's public module namespace can also expose already-mapped formed-module, integral-lattice, torsion-form, genus, and toric-lattice constructors. |
| Non-`ToricLattice` `sage.geometry.all` exports: wildcard `sage.geometry.polyhedron.all` and `sage.geometry.hyperbolic_space.all` imports; `PolyhedralComplex`; `Cone`, `random_cone`, `cones`; `Fan`, `FaceFan`, `NormalFan`, `Fan2d`, `FanMorphism`; `LatticePolytope`, `NefPartition`, `ReflexivePolytope`, `ReflexivePolytopes`, `lattice_polytope`; `toric_plotter`; `VoronoiDiagram`; `RibbonGraph`; `HyperplaneArrangements`, `OrderedHyperplaneArrangements`, `hyperplane_arrangements` | `geometry/all.py:1-20`; Sage discrete-geometry docs; `[[TASK-INTEGRATE-TORIC-VARIETIES-WITH-LATTICE-CATEGORY]]` | compatibility/interop/display/runtime surface not admitted as algebraic lattice API | These exports are geometry, polyhedral, fan, toric-plotting, hyperplane-arrangement, Voronoi, ribbon-graph, or hyperbolic-space routing context. They may use lattice points, cones in lattices, or toric character/cocharacter conventions as representation data, but they do not construct algebraic formed lattices and do not create `Lattices(ZZ)` constructor ownership. The existing toric source-admission result routes character and cocharacter groups through ordinary free-module and formed-lattice vocabulary with explicit identity Gram form when a coordinate presentation supplies it; cones, fans, lattice polytopes, fan morphisms, toric varieties, hyperplane arrangements, Voronoi diagrams, and hyperbolic/polyhedral objects are geometry/polyhedral owners. Migration consequence: keep these import aliases out of `Lattices(ZZ).Constructors()` and route any admitted public surface through the geometry/polyhedral spec that owns the fan, polytope, hyperbolic, arrangement, graph, or toric object. |
| `FreeQuadraticModule_generic_pid.zero_submodule()` | `free_quadratic_module.py:680` | module subobject construction | Recover as the zero subobject with inclusion into the ambient free bilinear module. It is not a lattice constructor; it is the initial submodule object in the subobject category, inheriting the restricted form. |
| `FreeQuadraticModule_generic` inherited provider base `free_module.FreeModule_generic`, `FreeQuadraticModule_generic_pid` inherited provider base `free_module.FreeModule_generic_pid`, and `FreeQuadraticModule_generic_field` inherited provider base `free_module.FreeModule_generic_field` | `free_quadratic_module.py:258`, `588`, `696`; `free_module.py:1944`, `3870`, `4499`; Sage free-module and free-quadratic-module docs | inherited free-module backend/provider strata for formed free modules | Preserve as Sage realization evidence for ordinary finite-rank free-module, PID-module, and field-vector-space behavior under an added bilinear form. These base classes provide generic module structure; the quadratic subclasses add inner-product matrix data and formed-module methods. They do not create lattice-local methods, constructors, or public inheritance vocabulary. Public ownership remains at the relevant free finite-rank module and formed-module categories, with `Lattices(R)` receiving only the downstream integral symmetric nondegenerate refinements. |
| `FreeQuadraticModule_ambient.__init__`, `FreeQuadraticModule_ambient_domain.__init__`, `FreeQuadraticModule_ambient_pid.__init__`, `FreeQuadraticModule_ambient_field.__init__`, `FreeQuadraticModule_submodule_with_basis_pid.__init__`, `FreeQuadraticModule_submodule_pid.__init__`, `FreeQuadraticModule_submodule_with_basis_field.__init__`, `FreeQuadraticModule_submodule_field.__init__` | `free_quadratic_module.py:827`, `952`, `1044`, `1130`, `1248`, `1421`, `1519`, `1641`; Sage free-quadratic-module docs/source route ordinary construction through `FreeModule`, `VectorSpace`, `span`, and `span_of_basis` | backend parent refinements for free bilinear modules over rings, PIDs, and fields | Preserve as Sage presentation strata only. The public category surface is the same free finite-rank bilinear module or subobject/selected-basis object with explicit base ring, generators or selected basis, and form data; exact Sage subclass selection is backend realization, not public vocabulary. Ambient constructors store base ring or field, rank or dimension, storage sparsity, and inner-product matrix. Subobject constructors store ambient object, generators or selected basis, echelonization/check data, and inherited inner-product matrix. These are not `Lattices(R).Constructors()` names. |
| `FreeQuadraticModule_generic._dense_module`, `FreeQuadraticModule_generic._sparse_module`, `FreeQuadraticModule_ambient._dense_module`, `FreeQuadraticModule_ambient._sparse_module`, `FreeQuadraticModule_generic._inner_product_is_dot_product`, `FreeQuadraticModule_generic._inner_product_is_diagonal` | `free_quadratic_module.py:335`, `352`, `526`, `550`, `904`, `922` | backend storage and optimization predicates | Do not admit as mathematical methods. Dense/sparse conversion is storage representation; the inner-product predicates are backend optimization guards for special Gram data. `_inner_product_is_dot_product` tests whether the ambient form is the dot product, while `_inner_product_is_diagonal` compares the inner-product matrix to the diagonal matrix with the same diagonal. |
| `IntegralLattice(data, basis=None)` / `FreeQuadraticModule_integer_symmetric` | `free_quadratic_module_integer_symmetric.py:73`, `625`; Sage docs `free_quadratic_module_integer_symmetric.html` | `Lattices(ZZ).Constructors().IntegralLattice` and richest correct meet | Admit the original Sage constructor name as named-only overloads for the finite source descriptor set: `gram_matrix` is a symmetric rational matrix giving the ambient bilinear form; `rank` is an integer Euclidean-rank descriptor whose source branch builds the identity form over `ZZ`; `cartan_type` is anything accepted by `CartanMatrix(data)` for a root lattice; and `hyperbolic_plane` is exactly `"U"` or `"H"` with Gram matrix `[[0,1],[1,0]]`. Optional `basis` is selected-generator/presentation data: if absent, Sage uses the standard basis of the ambient form; if present, rows or module elements span the lattice inside the ambient quadratic space. Source checks symmetry before constructing the ambient `FreeQuadraticModule(ZZ, n, inner_product_matrix=...)`; `FreeQuadraticModule_integer_symmetric.__init__` then rejects degenerate or nonintegral Gram presentations. The internal `ambient`, `basis`, and `inner_product_matrix` triple is backend presentation evidence only; public identity is the selected generator presentation with integral symmetric nondegenerate form data. |
| `IntegralLatticeDirectSum(Lattices, return_embeddings=False)` | `free_quadratic_module_integer_symmetric.py:262`; Sage docs `free_quadratic_module_integer_symmetric.html` | orthogonal direct-sum construction category over integral lattices | Recover as a direct-sum construction on a finite list of integral formed objects. Source requires every input be a Sage integral-lattice instance, copies the ambient inner-product matrices into a block-diagonal ambient form, block-embeds each selected basis into the total ambient degree, and constructs the direct-sum lattice from that block presentation. The public return object is the orthogonal direct sum with block form and canonical summand inclusions. Sage's `return_embeddings` boolean is not a public option bag; embeddings are structure maps/inclusions of the construction result, computed by mapping each summand basis to the corresponding block of the direct-sum basis. |
| `IntegralLatticeGluing(Lattices, glue, return_embeddings=False)` | `free_quadratic_module_integer_symmetric.py:372`; Sage docs `free_quadratic_module_integer_symmetric.html` | lattice overlattice/gluing construction from discriminant-group data | Recover as an overlattice construction of the direct sum. Input data are integral lattices and finite rows of glue generators whose components lie in the corresponding discriminant groups `A_{L_i}`; the source verifies each glue row has one component per lattice and coerces every component through the corresponding `A_{L_i}`. For each glue row, Sage forms a rational lift in the direct sum by `sum(phi_i(g_i.lift()*g_i.order())/g_i.order())`, then calls `direct_sum.overlattice(generators)`. The return object is the glued overlattice with summand embeddings obtained by solving the direct-sum basis against the glued basis. The `glue` lists are discriminant-group elements, not raw vectors or ambient coordinates, and `return_embeddings` selects whether the construction returns the structure maps already owned by the gluing/direct-sum construction. |
| Implemented lattice constructor collector routes | `category_specs/lattices/__init__.py:LatticesCategory._Constructors`; `category_specs/lattices/smoketest.sage`; `tests/category_specs/test_constructor_provenance.py`; `constructorNameInventories` above | implemented category-owned constructor overloads after mapping | The canonical admitted Sage constructor names for the lattice collector are `IntegralLattice`, `IntegralLatticeDirectSum`, and `IntegralLatticeGluing`. Current code exposes these names on `Lattices(ZZ).Constructors()` as named-only overloads: `IntegralLattice` accepts exactly one of `gram_matrix`, `rank`, `cartan_type`, or `hyperbolic_plane`, with optional `basis`; `IntegralLatticeDirectSum` accepts `lattices` plus the Sage-backed `return_embeddings` branch; and `IntegralLatticeGluing` accepts `lattices`, `glue`, and the `return_embeddings` branch. The public smoke exercises `Lattices(ZZ).Constructors().IntegralLattice`, `.IntegralLatticeDirectSum`, and `.IntegralLatticeGluing`, while the constructor-provenance regression verifies the owner route and Sage-named method records. Remaining audit work is semantic review of every relevant Sage lattice/form/module surface against inventory and mapping, not a missing constructor-collector implementation for these three admitted names. |
| `TorsionQuadraticForm(q)` | `torsion_quadratic_module.py:20` | `Lattices(ZZ).DiscriminantGroups()` or forms-owned torsion quadratic constructor | Admit as a closed constructor from a symmetric rational quadratic Gram matrix and quotient-valued codomain. It must validate the quotient codomain (`QQ/ZZ` or `QQ/2ZZ`) explicitly instead of exposing Sage's denominator-clearing path. |
| `TorsionQuadraticModule(V, W, modulus, modulus_qf, gens)` | `torsion_quadratic_module.py:190` | `Modules(R).WithForms().Quadratic().Torsion()`; lattice discriminant groups when built as `coker(L -> L^#)` | Preserve through two named routes: a generic torsion formed-module constructor from quotient data, and the lattice-owned discriminant descent path. Source caller data are a free symmetric formed cover `V`, a same-rank submodule `W <= V`, optional selected quotient generators `gens`, and explicit value codomains `modulus` for `b: V/W x V/W -> QQ / modulus ZZ` and `modulus_qf` for `q: V/W -> QQ / modulus_qf ZZ`. The source implementation is ZZ-only and requires equal ranks and symmetric cover form; public project construction must expose named quotient and discriminant-descent data, not Sage's positional constructor or a broad `check` option. `modulus` and `modulus_qf` are codomain data, not option-bag public API. |
| `TorsionQuadraticModule.__classcall__`, `gens()`, and `TorsionQuadraticModuleElement` | `torsion_quadratic_module.py:230`, `520`, `90` | torsion formed-module cached construction, selected generator presentation, and element owner | `__classcall__` is Sage normalization/validation for the same quotient-valued torsion formed-module constructor, not an additional public constructor. With checking enabled it verifies equal rank, ZZ base, symmetric cover form, and that supplied `gens` generate `V/W`; it computes `modulus = (V,W)` and `modulus_qf = gcd({(w,w) : w in W}, 2*modulus)` when codomains are not supplied. `gens()` returns the user-selected quotient generators when present, otherwise Smith-form generators; this is equipped presentation data, not an invariant of the torsion formed module. `TorsionQuadraticModuleElement` owns torsion-form element evaluation surfaces after the parent has fixed `V/W`, selected generators, and quotient-valued form codomains. |
| `TorsionQuadraticModule.__init__`, `_repr_`, and `_module_constructor(V, W, check=False)` | `torsion_quadratic_module.py:279`, `300`, `321` | backend initialization, display, and quotient-constructor hook | Do not admit as public lattice/discriminant API. `__init__` stores the already-normalized quotient data, optional selected generators, and value moduli; `_repr_` is display combining base ring, inherited Smith invariants, value codomain, and quadratic Gram presentation; `_module_constructor` is Sage FGP quotient plumbing for constructing `V/W`, inheriting the caller's value moduli when `check=False` and recomputing them when `check=True`. Public construction remains the mapped torsion formed-module constructor and discriminant descent path; public quotient/subobject construction should name whether codomain data are inherited, recomputed, or supplied as proof data rather than exposing `_module_constructor` or `check`. |
| `QuadraticForm(...)`, `DiagonalQuadraticForm(R, diag)`, `QuadraticForm.from_polynomial(poly)`, `polynomial(names='x')`, `coefficients()`, `sum_by_coefficients_with(right)`, `change_ring(R)` | `quadratic_form.py:516`, `570`, `583`, `593`, `927`, `1301`, `1353`, `1482`, `1541`, `1750`; Sage docs/source `quadratic_form.py` | quadratic-form presentation and conversion surfaces in the forms subtree, not lattice-owned constructors | These are constructors or conversions for a finite-variable quadratic-form presentation. The admitted finite input shapes are `(R, n, entries)` with `n >= 0` and `n(n+1)/2` upper-triangular entries, a homogeneous degree-two polynomial, `(R, M)` for a symmetric even-diagonal matrix over `R`, a symmetric even-diagonal matrix `M` with inferred base ring, and a diagonal coefficient list through `DiagonalQuadraticForm(R, diag)`. Recover the formed object through a forms-owned quadratic constructor/promotion path; refine to `Lattices(ZZ)` only when the resulting associated bilinear object has the integral, symmetric, nondegenerate, finite-rank free hypotheses. `unsafe_initialization`, externally supplied automorphism counts, and externally supplied determinants are audit/runtime cache data, not constructor semantics. |
| `QuadraticForm.__call__(v)`, `QFEvaluateVector(Q, v)`, `QFEvaluateVector_cdef(Q, v)`, `QFEvaluateMatrix(Q, M, Q2)`, `QFEvaluateMatrix_cdef(Q, M, Q2)` | `quadratic_form.py:980`, `1063`, `1073`; `quadratic_form__evaluate.pyx:4`, `47`, `65`, `113`; Sage docs/source `quadratic_form.py` | quadratic-form evaluation and pullback surfaces; Cython helpers are backend-only | Evaluation at a vector/list/tuple returns a scalar in the base ring; evaluation at a matrix returns the pulled-back quadratic-form presentation `M^T Q M`. Public project use belongs to a forms-owned evaluation/pullback operation with explicit domain vector or linear-map data and returned scalar or pulled-back form. `QFEvaluateVector` and `QFEvaluateMatrix` remain internal fast Python wrappers called by `__call__`; `QFEvaluateVector_cdef` and `QFEvaluateMatrix_cdef` are the internal Cython routines behind those wrappers. None are public lattice methods or constructor names. |
| `QuadraticForm.__pari__`, `QuadraticForm._pari_init_`, `QuadraticForm.__getitem__`, `QuadraticForm.__setitem__`, and `QuadraticForm._is_even_symmetric_matrix_` | `quadratic_form.py:712`, `724`, `787`, `812`, `1093`; Sage docs/source `quadratic_form.py` | quadratic-form presentation, mutable interop, PARI conversion, and constructor-validation backend surfaces | `__getitem__` reads upper-triangular coefficient presentation data; `__setitem__` mutates that Sage presentation and therefore must not become a public category method unless returned as a new presentation with an explicit witness. `__pari__` and `_pari_init_` are PARI Hessian-matrix conversion hooks. `_is_even_symmetric_matrix_` is constructor validation for symmetric even-diagonal matrix input over a chosen base ring. These remain forms-owned interop/backend surfaces, not lattice-owned API. |
| `BezoutianQuadraticForm(f, g)`, `HyperbolicPlane_quadratic_form(R, r=1)` | `all.py:23`; `constructions.py:14`, `69`; Sage docs/source `constructions.py` | forms-owned quadratic-form construction helpers, not lattice constructors | `BezoutianQuadraticForm(f, g)` requires two univariate polynomial inputs over the same coefficient ring `R`, rejects non-polynomial inputs and unequal base rings, builds the polynomial `(f(x)g(y)-f(y)g(x))/(y-x)`, sets `n=max(deg f, deg g)`, and returns an `n`-variable `QuadraticForm(R, n)` whose off-diagonal coefficients are doubled to match Sage's upper-triangular quadratic-form convention. `HyperbolicPlane_quadratic_form(R, r=1)` coerces `r` to `ZZ`, requires `r >= 1`, constructs the base form `xy` as `QuadraticForm(R, 2, [0, 1, 0])`, and returns the orthogonal direct sum of `r` copies. Public admission belongs to forms-owned constructors naming polynomial source data or hyperbolic-plane multiplicity and returned quadratic-form object; these are not `Lattices(R).Constructors()` routes unless a later promotion theorem supplies finite-free, integral, symmetric, and nondegenerate lattice hypotheses for the returned formed object. |
| `random_quadraticform`, `random_quadraticform_with_conditions`, `random_ternaryqf`, `random_ternaryqf_with_conditions` | `all.py:9`; `random_quadraticform.py:16`, `76`, `127`, `166`; Sage source `random_quadraticform.py` | sample-data and fixture-generation helpers, not mathematical constructors | `random_quadraticform(R, n, rand_arg_list=None)` requires `R in Rings()`, forms `n(n+1)/2` upper-triangular coefficients by calling `R.random_element()` or `R.random_element(*rand_arg_list)`, rejects more than three randomness arguments, and returns `QuadraticForm(R, n, coeffs)`. The condition variant repeatedly samples until every supplied predicate returns true, accepting predicates either as intended `c(Q)` callables or through Sage's attempted `Q.c()` branch; the source records no termination proof or canonical distribution beyond the delegated ring random-element calls. `random_ternaryqf(rand_arg_list=None)` similarly draws six coefficients from `ZZ.random_element` and returns `TernaryQF(coeffs)`, with the condition variant using the same rejection-sampling loop. Public admission is a representation/sample-data surface, not a constructor: any project fixture generator must name the formed-object category, finite rank or ternary-form specialization, scalar ring, coefficient distribution/randomness source, predicates, and termination/resource convention explicitly. The ternary variants remain part of the separate ternary-form obligation below, not `Lattices(ZZ).Constructors()`. |
| `BinaryQF.__init__`, `BinaryQF.principal`, `BinaryQF.from_polynomial`, `BinaryQF.polynomial`, `BinaryQF.__getitem__`, `BinaryQF.content` | `binary_qf.py:70`, `100`, `182`, `549`, `523`, `271`, `508`; Sage source/docstrings | explicit missing-category/spec obligation for specialized integer binary quadratic forms | These are constructor, presentation, and coefficient-content surfaces for the integer binary form `ax^2 + bxy + cy^2`, stored as the coefficient triple `(a,b,c)` over `ZZ`. Sage admits the finite constructor shapes: three coefficient inputs coerced to `ZZ`; one length-three list or tuple; the scalar `0`, normalized to `(0,0,0)`; a homogeneous degree-two bivariate `MPolynomial` over `ZZ`, read through the coefficients of `x^2`, `xy`, and `y^2`; and PARI `t_QFI`/`t_QFR`/`t_QFB` objects, read from their first three components. `BinaryQF.principal(D)` coerces `D` to `ZZ`, requires `D mod 4` to lie in `{0,1}`, and returns the principal form with coefficients `(1, D mod 4, ((D mod 4)-D)/4)`. `from_polynomial(poly)` accepts a bivariate polynomial ring, requires every monomial to have degree two, coerces the `x^2`, `xy`, and `y^2` coefficients to `ZZ`, and delegates to the coefficient constructor; `polynomial()` returns the cached homogeneous polynomial over `ZZ['x,y']`; `__getitem__` exposes the coefficient triple with tuple indexing/slicing; and `content()` is `gcd(a,b,c)`. Do not admit any of these as `Lattices(ZZ)` constructors or aliases for generic `QuadraticForm(...)`: a forms-subtree binary-form category/spec must own the object, coefficient order, principal-discriminant constructor, polynomial-presentation conversion, coefficient-projection surface, content invariant, and discriminant hypotheses before downstream lattice promotion is considered. |
| `BinaryQF.__call__`, `BinaryQF.__add__`, `BinaryQF.__sub__`, `BinaryQF.__neg__`, `BinaryQF.__mul__`, `BinaryQF.matrix_action_left`, `BinaryQF.matrix_action_right` | `binary_qf.py:224`, `296`, `409`, `435`, `463`, `1526`, `1549`; Sage source/docstrings | explicit missing-category/spec obligation for binary-form operations; matrix actions are backend/witness data until typed | Evaluation is the binary-form value map: Sage accepts either two positional coordinate values or one pair/list/tuple/vector, unpacks to `(x,y)`, and returns `(a*x + b*y)*x + c*y^2`. Addition, subtraction, and negation are coefficientwise operations on two `BinaryQF` objects or one object, returning `BinaryQF([a1+a2,b1+b2,c1+c2])`, `BinaryQF([a1-a2,b1-b2,c1-c2])`, and `BinaryQF([-a,-b,-c])`. Multiplication has two source branches: if the right operand is another `BinaryQF`, Sage calls PARI `qfbcompraw` and returns the unreduced raw Gauss composition; if the right operand is a `2 x 2` matrix parent, Sage computes the right action by evaluating the polynomial at the two column-style substitutions and returns coefficients `A=Q(aa,cc)`, `C=Q(bb,dd)`, `B=Q(aa+bb,cc+dd)-A-C`. `matrix_action_left(M)` uses the rows of `M` and implements `Q(ax+cy,bx+dy)`; `matrix_action_right(M)` uses the columns of `M` and implements `Q(ax+by,cx+dy)`. Public project use requires a binary-form category/spec with typed two-coordinate evaluation, coefficientwise additive structure, raw-versus-reduced Gauss composition, and explicit left/right matrix-action witnesses, including the acting matrix group or monoid and reduction/equivalence convention. These are not lattice morphism constructors, generic lattice isometries, or raw ambient-matrix APIs. |
| `BinaryQF.discriminant`, `BinaryQF.determinant`/`BinaryQF.det`, `BinaryQF.has_fundamental_discriminant`, `BinaryQF.is_primitive`, `BinaryQF.is_zero`, `BinaryQF.is_positive_definite`/`BinaryQF.is_posdef`, `BinaryQF.is_negative_definite`/`BinaryQF.is_negdef`, `BinaryQF.is_indefinite`/`BinaryQF.is_indef`, `BinaryQF.is_singular`, `BinaryQF.is_nonsingular` | `binary_qf.py:591`, `605`, `634`, `637`, `659`, `705`, `1217`, `1233`, `1235`, `1250`, `1252`, `1264`, `1266`, `1281`; Sage source/docstrings | explicit missing-category/spec obligation for binary-form invariants and predicates | These are invariant and predicate surfaces for the specialized integer binary form `ax^2 + bxy + cy^2`, stored as `(a,b,c)` over `ZZ`. `discriminant()` returns `D=b^2-4ac`; `determinant()` and `det` return the rational determinant of the associated half-matrix `[[a,b/2],[b/2,c]]`, namely `-D/4`; and `has_fundamental_discriminant()` delegates to the discriminant's fundamental-discriminant predicate. `is_primitive()` is the coefficient-content condition `gcd(a,b,c)=1`, while `is_zero()` is the identically-zero predicate via zero content. Definiteness and singularity are binary-form discriminant predicates: positive definite means `D<0` and `a>0`, negative definite means `D<0` and `a<0`, indefinite means `D>0`, singular means `D=0`, and nonsingular means `D != 0`; the short aliases are source-level synonyms only. Public project use requires a forms-subtree binary-form category/spec that names the coefficient triple convention, discriminant convention, rational half-matrix determinant convention, content/primitive predicate, zero predicate, and discriminant/sign definiteness predicates. These do not override lattice determinant, lattice signature, or finite-free formed-object definiteness owners; any promotion from a binary-form presentation to a lattice/formed module needs a separate bridge theorem identifying the associated bilinear form and determinant/signature convention. |
| `BinaryQF.is_weakly_reduced`, `BinaryQF.is_reducible`, `BinaryQF._reduce_indef`, `BinaryQF.reduced_form`, `BinaryQF._RhoTau`, `BinaryQF._Rho`, `BinaryQF._Tau`, `BinaryQF.cycle`, `BinaryQF.is_equivalent`, `BinaryQF.is_reduced`, `BinaryQF.complex_point` | `binary_qf.py:721`, `745`, `767`, `835`, `981`, `1004`, `1027`, `1043`, `1296`, `1426`, `1503`; Sage source/docstrings | explicit missing-category/spec obligation for binary-form reduction, equivalence, and complex-root surfaces; underscore operators are backend witnesses | These are reduction, equivalence, and root surfaces for the specialized integer binary form `ax^2+bxy+cy^2`, not lattice equality or generic lattice isometry. `is_weakly_reduced()` is defined only for negative discriminant and tests `|b| <= a <= c`. `is_reducible()` tests whether the discriminant is a square, i.e. whether the form factors into two linear forms, and is explicitly unrelated to reduction theory. `is_reduced()` requires nonzero discriminant and uses Sage's case split: positive definite forms satisfy the classical boundary convention `-a < b <= a < c` or `0 <= b <= a = c`; negative definite forms reduce by the positive form `(-a,b,-c)`; and indefinite forms satisfy the source's three inequalities involving `D`, `a`, `b`, and `c`, including the `a=0` and `c=0` branches. `reduced_form(transformation=False, algorithm='default')` returns a reduced equivalent form and optionally a right-action transformation matrix `M` with `self * M == reduced`; the default chooses Sage for reducible forms and PARI otherwise, Sage's own reducer only handles indefinite forms, PARI rejects reducible forms, and negative definite PARI reduction is implemented by reducing `-self` after a diagonal sign change. `_reduce_indef`, `_RhoTau`, `_Rho`, `_Tau`, and `cycle(proper=False)` are Buchmann-Vollmer backend operators for reduced indefinite nonsquare-discriminant cycles; `cycle(proper=True)` modifies the cycle by the source Tau rule. `is_equivalent(other, proper=True)` requires another `BinaryQF` with the same discriminant, distinguishes proper from improper equivalence, uses reduced cycles for indefinite nonsquare discriminants, uses the Conway-Sloane congruence branch for square discriminants, and compares reduced forms plus the swapped-coefficient improper branch in the definite case. `complex_point()` is defined only for negative discriminant and returns the upper-half-plane root of `a*x^2+b*x+c`. Public admission belongs to a binary-form reduction/equivalence spec naming discriminant sign and square/nonsquare hypotheses, proper-versus-improper convention, returned reduced representative, optional transformation witness, and complex-root codomain; these surfaces do not define lattice equality, lattice Aut/Hom, generic short-vector reduction, or raw matrix-action API. |
| `BinaryQF.small_prime_value`, `BinaryQF.solve_integer`, `BinaryQF_reduced_representatives` | `binary_qf.py:1572`, `1608`, `1826`; Sage source/docstrings | explicit missing-category/spec obligation for binary-form representation problems and class-representative enumeration | `small_prime_value(Bmax=1000)` is a primitive positive-definite binary-form represented-prime search: Sage scans boxes with `B` increasing by `10`, evaluates `Q(x,y)` for `x in [-B,B)` and `y in [0,B)`, returns the least prime found, and raises if no prime is found before `B >= Bmax`; the bound is a search/resource parameter, not a mathematical invariant. `solve_integer(n, *, algorithm='general', _flag=2)` solves `Q(x,y)=n` over integers and returns either one integer pair, `None`, or for internal PARI flags `1` and `3` a list of pairs; negative definite forms are routed to `-Q` and `-n`, square-discriminant forms use a source-coded linear-factor/unimodular-change branch, `algorithm='cornacchia'` requires `a=1`, `b=0`, `c>0`, and the general branch delegates to PARI `qfbsolve`. `_flag` is internal PARI return-shape control and should not become public API. `BinaryQF_reduced_representatives(D, primitive_only=False, proper=True)` coerces `D` to `ZZ`, rejects `D mod 4 in {2,3}`, enumerates lexicographically sorted reduced representatives for proper or improper equivalence classes, filters imprimitive forms only when requested and relevant, uses separate positive-square, positive-nonsquare, and definite enumeration loops, and filters duplicate improper/proper classes by `is_equivalent`. Public admission belongs to typed binary-form representation and class-enumeration surfaces naming discriminant, primitive-only convention, proper/improper equivalence convention, represented integer, algorithm, search bound, and returned witness object. These are not lattice constructors, lattice vector enumeration, generic representation-density surfaces, or class groups until the separate `BQFClassGroup` owner row supplies the parent/group structure. |
| `BinaryQF._pari_init_`, `BinaryQF.__hash__`, `BinaryQF.__eq__`, `BinaryQF.__ne__`, `BinaryQF.__lt__`, `BinaryQF._repr_`, `BinaryQF._latex_` | `binary_qf.py:158`, `326`, `339`, `370`, `388`, `477`, `494`; Sage source/docstrings | compatibility/display/runtime not admitted as mathematical API | PARI conversion, hashing, equality protocol, lexicographic ordering, display, and LaTeX rendering remain runtime/interop surfaces. A future binary-form object may need equality and display behavior, but this Sage protocol surface does not determine lattice or forms-subtree mathematical owners. |
| `BinaryQF.form_class`, `BQFClassGroup`, `BQFClassGroup.__init__`, `BQFClassGroup._element_constructor_`, `BQFClassGroup.zero`, `BQFClassGroup.random_element`, `BQFClassGroup.discriminant`, `BQFClassGroup.order`/`cardinality`, `BQFClassGroup.abelian_group`, `BQFClassGroup.gens`, `BQFClassGroup._coerce_map_from_` | `binary_qf.py:1808`; `bqf_class_group.py:99`, `125`, `147`, `170`, `187`, `245`, `257`, `287`, `290`, `319`, `339`; Sage source/docstrings | explicit missing-category/spec obligation for negative-discriminant primitive positive-definite binary-form class groups | `BinaryQF.form_class()` sends a form to `BQFClassGroup(self.discriminant())(self)`, so the public operation is class projection from a primitive positive-definite binary form to the class group of its discriminant. `BQFClassGroup(D, check=True)` coerces `D` to `ZZ`, requires a nonzero discriminant with `D mod 4 in {0,1}`, and currently rejects `D > 0`; the Sage parent therefore represents only the positive-definite half of the negative-discriminant class group, while the full negative-discriminant form class group is source-described as two isomorphic copies for positive and negative definite forms. `_element_constructor_` admits `0`, an already same-parent class element, or a `BinaryQF`, and the element constructor separately enforces matching discriminant, primitivity, positive definiteness, and reduction. `zero()` is the class of `BinaryQF.principal(D)`. `random_element()` samples an odd prime `a` with Kronecker symbol `1`, solves `b^2 = D mod 4a`, sets `c=(b^2-D)/(4a)`, and returns the resulting class with no strict distribution theorem. `order()`/`cardinality` is the class number via `quadratic_order_class_number(D)`, while `abelian_group()` uses PARI `quadclassunit` to build an `AdditiveAbelianGroupWrapper` whose generators feed `gens()`. `_coerce_map_from_` exposes only the natural projection when the other parent's discriminant is a square multiple of this one, through `BQFClassGroupQuotientMorphism`; otherwise it falls back to Sage coercion failure. Public admission requires an arithmetic binary-form class-group category/spec naming the negative discriminant support, positive-definite/primitive representative condition, principal class, Gauss-composition group law, class-number backend, finite abelian wrapper/presentation, generator convention, random-sampling theorem status, and discriminant-quotient projection. These are not lattice automorphism groups, lattice discriminant groups, or generic Hom/Aut categories. |
| `BQFClassGroup.__hash__`, `BQFClassGroup._repr_` | `bqf_class_group.py:223`, `234`; Sage source/docstrings | compatibility/display/runtime not admitted as mathematical API | Parent hashing and display remain runtime surfaces for Sage interop. |
| `BQFClassGroup_element`, `BQFClassGroup_element.__init__`, `BQFClassGroup_element.form`, `BQFClassGroup_element._neg_`, `BQFClassGroup_element._add_`, `BQFClassGroup_element._sub_`, `BQFClassGroup_element.__mul__`/`__rmul__`, `BQFClassGroup_element.order`, `BQFClassGroup_element.is_zero`, `BQFClassGroupQuotientMorphism`, `BQFClassGroupQuotientMorphism.__init__`, `BQFClassGroupQuotientMorphism._call_` | `bqf_class_group.py:368`, `393`, `419`, `436`, `456`, `472`, `489`, `518`, `629`, `589`, `650`, `703`, `725`; Sage source/docstrings | explicit missing-category/spec obligation for class-group element and discriminant-quotient morphism APIs | `BQFClassGroup_element` is a class of primitive positive-definite binary quadratic forms for the parent discriminant, stored by Sage as the reduced representative. `form()` returns that reduced representative, using the uniqueness of reduced representatives for `D < 0`. `_neg_` represents inverse by `[a,-b,c]`; `_add_` composes reduced representatives using binary-form Gauss composition and re-reduces through the element constructor; `_sub_` is addition with inverse; `__mul__` and `__rmul__` are integer multiples under repeated composition via Sage's generic group multiple routine; `order()` uses the parent class number as a known multiple; and `is_zero()` is the principal-class predicate, distinct from Python truthiness/display protocol. `BQFClassGroupQuotientMorphism(G,H)` is defined only when `disc(G) = f^2 disc(H)` for an integer square quotient; its evaluation composes the source representative with the principal form of the target discriminant and coerces to the target group, equivalently finding a representative satisfying `f^2 | a` and `f | b` and substituting `x -> x/f`. Preserve these as arithmetic-form class-group element, scalar-multiple, order, principal-class, and quotient-homomorphism obligations; do not merge them with lattice elements, lattice discriminant-group elements, module Hom elements, or lattice Aut/Hom surfaces without a recorded bridge theorem. |
| `BQFClassGroup_element.__eq__`, `BQFClassGroup_element.__ne__`, `BQFClassGroup_element.__lt__`, `BQFClassGroup_element.__bool__`, `BQFClassGroup_element._repr_`, `BQFClassGroup_element.__hash__` | `bqf_class_group.py:520`, `540`, `556`, `574`, `604`, `616`; Sage source/docstrings | compatibility/display/runtime not admitted as mathematical API | Element equality compares stored reduced representatives, ordering is lexicographic on coefficient lists, truthiness is non-principal-class testing, and display/hash are Sage protocol surfaces. The mathematical principal-class predicate is preserved through `is_zero()`; protocol behavior is not a lattice method. |
| `TernaryQF.__init__`, `TernaryQF.coefficients`, `TernaryQF.coefficient`, `TernaryQF.polynomial`, `TernaryQF.quadratic_form`, `TernaryQF.matrix`, `TernaryQF.__call__`, `TernaryQF.__neg__`, `TernaryQF.scale_by_factor` | `ternary_qf.py:49`, `78`, `101`, `129`, `150`, `246`, `265`, `189`, `397`, `464`; Sage source/docstrings | explicit missing-category/spec obligation for specialized integer ternary quadratic forms and their presentation actions | `TernaryQF` is the specialized integral ternary form `a*x^2 + b*y^2 + c*z^2 + r*yz + s*xz + t*xy`, stored as six `ZZ` coefficients `(a,b,c,r,s,t)`. The constructor source documents a list/tuple of six entries and the implementation coerces each entry to `ZZ` after a length-six check; `coefficients()` returns that ordered six-tuple and `coefficient(n)` projects the `0 <= n <= 5` presentation coordinate. `polynomial(names='x,y,z')` returns the associated homogeneous polynomial over `ZZ` in named variables. `quadratic_form()` converts to generic `QuadraticForm(ZZ, 3, [a,t,s,b,r,c])`, while `matrix()` returns the Hessian matrix `[[2a,t,s],[t,2b,r],[s,r,2c]]`. `__call__(v)` has three source branches: a length-three vector/list/tuple returns the represented integer, a `3 x 3` matrix returns the transformed `TernaryQF` with coefficient extraction from `v^T H v`, and a `3 x n` matrix with `n != 3` returns a generic `QuadraticForm(ZZ, v^T H v)`; non-three-row matrices and non-vector/matrix inputs are rejected. `__neg__` changes all six coefficients, and `scale_by_factor(k)` returns a `TernaryQF` exactly when `k*content(self)` lies in `ZZ`, otherwise it requires `k.parent()` to be a Sage ring and returns a generic three-variable `QuadraticForm` over that ring. Public admission requires a ternary-form category/spec naming the six-coefficient convention, Hessian convention, homogeneous-polynomial presentation, generic-`QuadraticForm` bridge, matrix-substitution codomain split, coefficientwise sign change, and scalar value-rescaling codomain split. These are not `Lattices(ZZ)` constructors, ordinary free-module methods, or lattice morphism APIs without a separate promotion theorem from a ternary-form presentation to a formed module/lattice. |
| `TernaryQF.disc`, `TernaryQF.is_definite`, `TernaryQF.is_positive_definite`, `TernaryQF.is_negative_definite`, `TernaryQF.is_primitive`, `TernaryQF.primitive`, `TernaryQF.content`, `TernaryQF.level`, `TernaryQF.reciprocal`, `TernaryQF.reciprocal_reduced`, `TernaryQF.divisor`, `TernaryQF.adjoint`, `TernaryQF.omega`, `TernaryQF.delta`, `TernaryQF.xi`, `TernaryQF.xi_rec` | `ternary_qf.py:297`, `314`, `340`, `371`, `417`, `439`, `609`, `661`, `505`, `528`, `547`, `583`, `626`, `641`, `937`, `974`; Sage source/docstrings | explicit missing-category/spec obligation for ternary-form arithmetic invariants | `disc()` is the determinant of the Hessian matrix divided by `2`, implemented as `4abc + rst - ar^2 - bs^2 - ct^2`. Definiteness is a ternary-form Sylvester-style source predicate: `a != 0`, `4ab-t^2 > 0`, nonzero discriminant, and sign agreement between `a` and the discriminant, with positive and negative definite variants requiring `a>0,disc>0` or `a<0,disc<0`. `content()` is `gcd(a,b,c,r,s,t)`, `is_primitive()` is content `1`, and `primitive()` divides all six coefficients by the content. `adjoint()` is the ternary form whose Hessian is twice the classical adjoint of the Hessian matrix; `divisor()` is the gcd of the adjoint coefficient expressions with off-diagonal entries doubled; `level()` is `4*disc()/divisor()` using integer division. `reciprocal()` is `adjoint().primitive().scale_by_factor(content())`, `reciprocal_reduced()` Eisenstein-reduces that reciprocal, `omega()` is the content of the adjoint of the primitive form, and `delta()` is `adjoint().omega()`, equivalently the reciprocal's omega. `xi(p)` is a Dickson genus-character surface allowing `p=-1`, with `p=4` normalized to `-1` and `p=8` normalized to `2`, rejecting invalid characters by omega divisibility and using `basic_lemma`; `xi_rec(p)` applies `xi` to the reciprocal. Public admission belongs to a source-grounded ternary-form arithmetic spec with integrality, nonsingularity where required by the invariant, Hessian/discriminant convention, primitive/content convention, adjoint/reciprocal construction, level/divisor formula, and Dickson character hypotheses. These are not lattice parity, discriminant-form, or genus-symbol owners by themselves. |
| `TernaryQF.is_eisenstein_reduced`, `TernaryQF.reduced_form_eisenstein`, `TernaryQF.pseudorandom_primitive_zero_mod_p`, `TernaryQF.find_zeros_mod_p`, `TernaryQF.find_p_neighbor_from_vec`, `TernaryQF.find_p_neighbors`, `TernaryQF.basic_lemma`, `find_all_ternary_qf_by_level_disc`, `find_a_ternary_qf_by_level_disc` | `ternary_qf.py:675`, `747`, `783`, `820`, `847`, `898`, `925`, `1956`, `1993`; Sage source/docstrings | ternary-form algorithm backend evidence or missing ternary-form category/spec obligation | `is_eisenstein_reduced()` is the eight-condition Eisenstein reduction predicate on coefficients `(a,b,c,r,s,t)`. `reduced_form_eisenstein(matrix=True)` returns the unique Eisenstein-reduced equivalent positive ternary form, and when `matrix=True` also returns a unimodular transformation matrix `M` with `self(M) == reduced`; the boolean is a backend witness selector, so public project vocabulary should expose a typed optional transformation witness rather than a public option bag. `pseudorandom_primitive_zero_mod_p(p)` and the Cython helper require an odd prime `p` not dividing the discriminant and return a zero `(x,y,1)` modulo `p` without distribution or termination theorem beyond the loop. `find_zeros_mod_p(p)` has a special `p=2` branch and otherwise uses the pseudorandom zero to enumerate all zeros for odd `p`. `find_p_neighbor_from_vec(p, v, mat=False)` requires a vector `v` with `Q(v)=0 mod p` that is a nonsingular point of the conic; it returns the reduced `p`-neighbor and, with `mat=True`, a transformation matrix witness rescaled by its determinant. `find_p_neighbors(p, mat=False)` maps that construction over all zeros modulo `p`. `basic_lemma(p)` returns a represented number coprime to `p`. `find_all_ternary_qf_by_level_disc(N,d)` and `find_a_ternary_qf_by_level_disc(N,d)` search reduced ternary forms by level and discriminant under the source condition that `N | 4d` and `d | N^2`, returning all forms, one form, `None`, or raising when the backend proves no such forms. Project use requires typed ternary reduction, local/modular zero, p-neighbor, represented-value, search-result, and transformation-witness objects; the `matrix` and `mat` booleans remain backend witness selectors. |
| `TernaryQF.symmetry`, `TernaryQF.automorphism_symmetries`, `TernaryQF.automorphism_spin_norm`, `TernaryQF._automorphisms_reduced_fast`, `TernaryQF._automorphisms_reduced_slow`, `TernaryQF.automorphisms`, `TernaryQF._number_of_automorphisms_reduced`, `TernaryQF.number_of_automorphisms` | `ternary_qf.py:989`, `1025`, `1065`, `1255`, `1635`, `1674`, `1740`, `1917`; Sage source/docstrings | ternary-form Aut backend evidence; not lattice Aut ownership | `symmetry(v)` returns the rational reflection-style matrix with source expression `I - v.column()*matrix(v)*H/Q(v)`, sending `v` to `-v` and fixing the `H`-orthogonal complement, so it is a matrix witness tied to the ternary-form Hessian presentation. `automorphism_symmetries(A)` decomposes a nonidentity automorphism matrix into two such symmetry vectors; `automorphism_spin_norm(A)` multiplies the represented values of those vectors and returns the squarefree part, with identity spin norm `1`. `_automorphisms_reduced_fast()` and `_number_of_automorphisms_reduced()` use the sixteen Eisenstein-boundary conditions from `_border`/`_borders` as a reduced-definite-form lookup table; `_automorphisms_reduced_slow()` searches `3 x 3` matrices with entries in `{-1,0,1}`, determinant `1`, and finite-order tests because Eisenstein-reduced forms are Minkowski reduced. `automorphisms(slow=True)` and `number_of_automorphisms(slow=True)` are implemented only for definite forms, reduce positive definite forms by Eisenstein reduction, handle negative definite forms through `-self`, cache the result, and use `slow` only as a backend strategy selector. Public project Aut semantics must route through typed ternary-form automorphism elements and containment when the object is promoted to a formed/lattice category; Sage's raw matrices, boundary tables, caches, and `slow` selector remain backend evidence. |
| `TernaryQF.__hash__`, `TernaryQF._repr_`, `TernaryQF.__eq__`, `TernaryQF._border`, `TernaryQF._borders` | `ternary_qf.py:116`, `168`, `566`, `1086`, `1196`; Sage source/docstrings | compatibility/display/runtime and private helpers not admitted as mathematical API | Hashing is the hash of the six-coefficient tuple, display renders the two-line coefficient presentation, equality compares exact six-coefficient tuples after a `TernaryQF` type check, and `_border`/`_borders` expose private Eisenstein-boundary predicates for automorphism helper tables. These protocol/helper surfaces do not determine public lattice or forms-subtree method owners. |
| `red_mfact`, `_reduced_ternary_form_eisenstein_with_matrix`, `_reduced_ternary_form_eisenstein_without_matrix`, `primitivize`, `evaluate`, `_find_zeros_mod_p_2`, `pseudorandom_primitive_zero_mod_p`, `_find_zeros_mod_p_odd`, `_find_zeros_mod_p`, `_find_all_ternary_qf_by_level_disc`, `_find_a_ternary_qf_by_level_disc`, `extend`, `_find_p_neighbor_from_vec`, `_basic_lemma_vec`, `_basic_lemma` | `ternary.pyx:22`, `47`, `311`, `470`, `491`, `508`, `542`, `575`, `644`, `675`, `806`, `933`, `984`, `1089`, `1124`; Sage source/docstrings | backend-only Cython helper surface for ternary-form algorithms | These helpers implement coefficient-level Eisenstein reduction and transformation tracking, primitive normalization of modular vectors, coefficient-vector evaluation, `p=2` and odd-prime modular-zero enumeration, level/discriminant search, matrix extension, p-neighbor construction, and represented-value search for `TernaryQF`. They are not public project constructors, lattice methods, or category-owned operations; expose only the typed ternary-form algorithm surfaces that consume their witnesses. |
| `is_triangular_number(n, return_value=False)`, `extend_to_primitive(A_input)`, `least_quadratic_nonresidue(p)` | `all.py:14`; `extras.py:11`, `77`, `144`; Sage source/docstrings | integer-arithmetic helpers and module-presentation primitive-extension backend, not lattice API | `is_triangular_number` coerces `n` to `ZZ`, tests whether `8n+1` is a square, and with `return_value=True` returns `(bool, k)` where `k(k+1)/2=n` or `(False, None)`; the boolean flag is a witness-return selector, not a public option bag for lattice methods. `least_quadratic_nonresidue(p)` is a modular arithmetic helper for prime `p>2`, with a special no-nonresidue error at `p=2` and prime validation in the fallback branch. `extend_to_primitive(A_input)` accepts either a matrix or a list of same-ambient vectors, converts vector lists to row matrices, transposes tall matrices to the wide case, uses the Smith-form right transformation to append rows so the final square matrix has determinant equal to the gcd of the original minors, normalizes the determinant sign, and returns either a square matrix or a list of rows matching the input shape. Project use belongs to typed module-presentation or basis-extension algorithms with source submodule/generator data and a transformation/extension witness; it is not a standalone lattice constructor, lattice basis method, or silent raw-vector fallback. |
| `gamma__exact(n)`, `zeta__exact(n)`, `QuadraticBernoulliNumber(k, d)`, `quadratic_L_function__exact(n, d)`, `quadratic_L_function__numerical(n, d, num_terms=1000)` | `all.py:16`; `special_values.py:25`, `97`, `166`, `207`, `269`; Sage source/docstrings | analytic/arithmetic backend helpers for mass and class-number formulas | These are scalar special-value helpers, not methods on lattice objects or constructors for formed modules. `gamma__exact(n)` coerces `n` to `QQ`, returns factorial values at positive integers, `infinity` at nonpositive integers, symbolic `sqrt(pi)` multiples at half-integers, and raises for other denominators. `zeta__exact(n)` is defined for critical Riemann zeta values: negative arguments use Bernoulli numbers, positive even `n>1` use the standard Bernoulli/`\pi^n` formula, `n=1` returns `infinity`, `n=0` returns `-1/2`, and positive odd `n>1` raises. `QuadraticBernoulliNumber(k, d)` replaces `d` by its fundamental discriminant, sums Bernoulli-polynomial values weighted by the Kronecker symbol modulo `|d_1|`, and returns a rational/symbolic scalar. `quadratic_L_function__exact(n, d)` computes critical quadratic Dirichlet `L`-values for the primitive quadratic character attached to the fundamental discriminant of `d`: nonpositive `n` use the quadratic Bernoulli number, while positive `n` must match the parity determined by `kronecker(d_1, -1)`, otherwise Sage raises the corresponding critical-value error. `quadratic_L_function__numerical(n, d, num_terms=1000)` is a naive convergent Dirichlet-series approximation for `n >= 0`, using the parent real field of `n` when present and raising for negative `n`. Preserve these only as backend evidence for mass, class-number, genus, or local/global formula surfaces that explicitly require these scalar factors; public exposure should name the formula consuming them, the character/discriminant, critical-value or convergence hypotheses, precision/truncation data for the numerical route, and the scalar codomain. |
| `matrix()`, `Hessian_matrix()`, `Gram_matrix_rational()`, `Gram_matrix()`, `has_integral_Gram_matrix()`, `det()`, `Gram_det()`, `dim()`, `base_ring()` | `quadratic_form.py:1150`, `1164`, `1190`, `1214`, `1251`, `1451`, `1470`, `1495`, `1523`; Sage docs `quadratic_form.html` | representation split for quadratic-form presentations | These surfaces read presentation data: Hessian matrix for `2Q`, Gram matrix over `Frac(R)` or over `R` when it exists, determinant conventions, variable count, and scalar ring. The public lattice object should expose rank, scalar ring, and Gram/form data through the formed-module presentation; it should not treat Sage's Hessian/Gram conversion helpers as independent lattice operations. |
| `bilinear_map(v, w)` | `quadratic_form.py:1689`; Sage docs `quadratic_form.html` | forms-owned associated-bilinear-form recovery | Recover by polarization `2B(v,w)=Q(v)+Q(w)-Q(v+w)` in characteristic not `2`. This is the conversion from a quadratic form to its associated bilinear form, not a lattice method on its own; after recovery the bilinear formed object may promote to a lattice under the usual integral/nondegenerate/free hypotheses. |
| `gcd()`, `is_primitive()`, `primitive()`, `adjoint_primitive()` | `quadratic_form.py:1282`, `1393`, `1411`, `1434`; Sage docs `quadratic_form.html` | quadratic-form presentation content and primitive-adjoint operations | These are coefficient-content and adjoint-presentation operations over `ZZ`, not invariant lattice-object equality. Preserve as quadratic-form/form-presentation functionality; lattice-level use requires an explicit theorem or morphism witnessing how the transformed presentation relates to the original formed object. |
| `level()`, `level_ideal()`, `quadratic_form_from_invariants(F, rk, det, P, sminus)` | `quadratic_form.py:72`, `1586`, `1656`; Sage docs `quadratic_form.html` | quadratic-form local/global invariant backend evidence | `level` and `level_ideal` are PID quadratic-form invariants computed from the inverse Hessian conditions. `quadratic_form_from_invariants` constructs a rational quadratic form from rank, determinant, Hasse-prime set, and negative signature count, with current source support limited to `QQ`. Use these as backend evidence for sourced local/global invariant work; do not expose them as ordinary lattice constructors. |
| `rational_diagonal_form(return_matrix=False)`, `_rational_diagonal_form_and_transformation()` | `quadratic_form__local_field_invariants.py:33`, `192`; Sage docs/source `quadratic_form__local_field_invariants.py` | quadratic-form presentation normalization and isometry witness data | Preserve as a forms-owned diagonalization/equivalence presentation over the fraction field of the caller's base ring. The public method returns only the diagonalized form `D` unless `return_matrix=True`, when it returns `(D, T)` with `T.transpose() * self.matrix() * T = D.matrix()`; `_rational_diagonal_form_and_transformation()` is the cached backend that always returns `(D,T)`. Sage first tries PARI `qfgaussred`, falls back to row-clearing over the fraction field when PARI conversion or inversion fails, deep-copies the returned form, and makes the returned transformation immutable. Public vocabulary should expose a typed diagonal-presentation construction and optional isometry/coordinate-change witness, not the cache, PARI fallback, mutability convention, or boolean witness selector as lattice API. |
| `signature_vector()`, `signature()`, `compute_definiteness()`, `compute_definiteness_string_by_determinants()`, `is_positive_definite()`, `is_negative_definite()`, `is_indefinite()`, `is_definite()` | `quadratic_form__local_field_invariants.py:298`, `348`, `761`, `851`, `927`, `964`, `1001`, `1038`; Sage docs/source `quadratic_form__local_field_invariants.py` | ordered-real signature and definiteness surfaces for finite free symmetric form presentations | Route through the ordered-real-realization owner chosen by `[[DECISION-ORDERED-REAL-SIGNATURE-OWNER]]`. `signature_vector()` diagonalizes over the fraction field and returns `(p,n,z)` counting positive, negative, and zero diagonal entries, while `signature()` returns `p-n`. `compute_definiteness()` is implemented only over `ZZ`, `QQ`, and `RR`, caches one of `pos_def`, `neg_def`, `indefinite`, `zero`, or `degenerate`, treats degenerate forms as neither definite nor indefinite, and treats the zero-dimensional form as both positive and negative definite and as definite. `compute_definiteness_string_by_determinants()` is a determinant-ratio/Sylvester-style backend for the same finite set of base rings, with nondegenerate zero leading minors classified as indefinite. The existing lattice `signature_pair()` surface records nondegenerate inertia; project admission must state the zero-eigenvalue and zero-dimensional conventions explicitly rather than infer them from a bare integral-domain lattice. Cache strings, determinant-scan helper status, and source spelling quirks are runtime/backend data. |
| `hasse_invariant(p)`, `hasse_invariant__OMeara(p)`, `is_hyperbolic(p)`, `is_anisotropic(p)`, `is_isotropic(p)`, `anisotropic_primes()` | `quadratic_form__local_field_invariants.py:385`, `475`, `559`, `618`, `688`, `735`; Sage docs/source `quadratic_form__local_field_invariants.py` | local quadratic-form invariant backend evidence over `QQ_p` or `RR` | Preserve as local formed-object invariant evidence after explicit scalar extension/localization. `p=-1` denotes the real place. `hasse_invariant(p)` uses the Cassels convention `prod_{i<j}(a_i,a_j)_p` on rational diagonal coefficients; `hasse_invariant__OMeara(p)` uses the O'Meara convention `prod_{i<=j}(a_i,a_j)_p`. These are distinct named invariants and must not be merged behind a generic Hasse surface without a convention decision. `is_hyperbolic(p)` first rejects odd dimension, accepts the zero form, uses real signature `0` at `p=-1`, and at finite primes tests the determinant squareclass plus the Cassels Hasse criterion, with a separate `p=2` sign formula. `is_anisotropic(p)` is real definiteness at `p=-1`, dimension-based local classification for dimensions `1` through `4`, automatic false for dimension at least `5`, and an explicit source gap for dimension `0`, where Sage raises `NotImplementedError`; `is_isotropic(p)` is its boolean complement only where anisotropy is defined. `anisotropic_primes()` tests primes dividing `2*det(Q)` plus the real place. Public project vocabulary should name the local place, diagonal/form presentation, determinant squareclass, Hasse convention, hyperbolic/isotropic/anisotropic predicate, and dimension hypotheses; the zero-dimensional anisotropy convention remains a missing decision/theorem obligation rather than an admitted fallback. |
| `global_genus_symbol()`, `local_genus_symbol(p)`, `CS_genus_symbol_list(force_recomputation=False)` | `quadratic_form__genus.py:16`, `54`, `118`; Sage docs/source `quadratic_form__genus.py` | integral quadratic-form genus-symbol backend surfaces | These are bound `QuadraticForm` wrapper surfaces for Conway-Sloane genus symbols of `2Q`, implemented by passing `self.Hessian_matrix()` to `Genus` or `LocalGenusSymbol`, and they require the caller's base ring to be exactly `ZZ`. `global_genus_symbol()` returns the global genus symbol built from signature and local symbols of the Hessian presentation. `local_genus_symbol(p)` first requires `p` to be a positive prime; Sage explicitly does not support the Conway-Sloane `p=-1` or infinity local-symbol conventions here. Its local symbol data are triples `[m,n,d]` for odd `p`, with scale valuation, rank, and normalized determinant squareclass, and quintuples `[m,n,s,d,o]` for `p=2`, with even/odd type and oddity modulo `8`. `CS_genus_symbol_list(force_recomputation=False)` caches the list of local symbols at primes dividing `2*det(Q)` in increasing order, with the flag only forcing cache recomputation. Public project vocabulary should expose typed global and local genus-symbol objects for integral formed presentations/lattices, with Hessian-versus-form convention, prime/place, tuple data, determinant support, signature, and cache-free return object explicit. These are not lattice constructors; lattice-level genus use requires a theorem that relates the lattice form, Hessian convention, discriminant form, and signature hypotheses. |
| `genera(sig_pair, determinant, max_scale=None, even=False)`, `_local_genera(p, rank, det_val, max_scale, even)`, `_blocks(b, even_only=False)`, `Genus(A, factored_determinant=None)`, `LocalGenusSymbol(A, p)`, `is_GlobalGenus(G)`, `is_2_adic_genus(...)` | `genera/genus.py:41`, `127`, `226`, `325`, `377`, `411`, `464`; Sage docs/source `genera/genus.py` | genus-symbol construction, enumeration, and realizability backend surfaces for nondegenerate integral forms/lattices | Preserve as genus-object constructors and predicates, not lattice constructors. `genera(sig_pair, determinant, max_scale=None, even=False)` coerces the signature pair and determinant to `ZZ`, requires nonnegative signature entries, ignores the determinant sign, sets `max_scale` to the determinant when absent, enumerates local p-adic symbol possibilities for `p=2` and for primes dividing the determinant, takes their Cartesian product, constructs `GenusSymbol_global_ring(sig_pair, local_symbols, representative=None, check=True)`, filters by `is_GlobalGenus`, and sorts deterministically by local tuple data. `_local_genera(p, rank, det_val, max_scale, even)` has no input checks; it enumerates scale/rank sequences whose weighted scale sum is the determinant valuation, adds odd-prime determinant squareclasses `±1`, and for `p=2` delegates block-shape enumeration to `_blocks` then keeps only `is_2_adic_genus`-valid symbols while removing canonical duplicates. `_blocks(b, even_only=False)` enumerates viable 2-adic Jordan-block quintuples `[scale, rank, det, even_or_odd, oddity]` for the fixed scale/rank, with `even_only` suppressing odd blocks. `Genus(A, factored_determinant=None)` takes a nonsingular symmetric integral Gram matrix, uses `2*det(A)` or `2*factored_determinant` as the support, computes `signature_pair_of_matrix(A)`, builds local symbols by `p_adic_symbol(A,p,val)`, and returns a global genus symbol with representative `A`; `LocalGenusSymbol(A,p)` computes the determinant valuation at `p` and returns the corresponding local genus symbol. `is_GlobalGenus(G)` is a Conway-Sloane global realizability predicate checking local determinant squareclasses, 2-adic validity, accumulated oddity congruence modulo `8`, and p-excess/oddity contributions; `is_2_adic_genus` validates quintuple shape and Conway-Sloane 2-adic rank/determinant/oddity conditions, raising when given non-quintuples. Public category exposure must route through typed genus/species objects from signature, determinant, local symbols, and optional representative Gram matrix data; raw tuple lists, `even`, `max_scale`, `even_only`, factored determinant shortcuts, canonical-duplicate filtering, and helper names are finite source-mined backend inputs, not arbitrary public option bags. |
| `canonical_2_adic_compartments(...)`, `canonical_2_adic_trains(...)`, `canonical_2_adic_reduction(...)`, `basis_complement(B)`, `p_adic_symbol(A, p, val)`, `two_adic_symbol(A, val)`, `signature_pair_of_matrix(A)`, `is_even_matrix(A)`, `split_odd(A)`, `trace_diag_mod_8(A)` | `genera/genus.py:524`, `592`, `694`, `785`, `877`, `1106`, `825`, `945`, `976`, `1064`; Sage genus docs/source | local-symbol normalization, field-row-space complement, and matrix-to-symbol backend surfaces | These compute Conway-Sloane local symbol data from integral Gram matrices and normalize 2-adic symbol representatives. `basis_complement` is row-space linear-algebra plumbing for echelonized matrices over a field. They remain backend evidence for local genus-symbol objects. Public surfaces should expose local symbol construction from a typed formed object or Gram presentation, with prime, valuation, row-space, and witness data named explicitly, rather than exporting raw tuple-list normalizers or complement helpers. |
| `Genus_Symbol_p_adic_ring`, `__init__(prime, symbol, check=True)`, `__repr__()`, `_latex_()`, `__eq__(other)`, `__ne__(other)`, and methods `automorphous_numbers`, `canonical_symbol`, `gram_matrix`, `mass`, `_standard_mass`, `_species_list`, `prime`, `is_even`, `symbol_tuple_list`, `number_of_blocks`, `determinant`/`det`, `dimension`/`dim`/`rank`, `direct_sum`, `excess`, `scale`, `norm`, `level`, `trains`, `compartments`, plus `_gram_from_jordan_block(p, block, discr_form=False)` | `genera/genus.py:1203`, `1263`, `1295`, `1377`, `1428`, `1463`, `1503`, `1666`, `1732`, `1765`, `1817`, `1840`, `1900`, `1917`, `1943`, `1975`, `2004`, `2037`, `2067`, `2124`, `2197`, `2219`, `2245`, `2259`, `2285`, `3319`; Sage docs/source `genera/genus.py` | local p-adic genus-symbol object, local invariants, protocol surfaces, representative witnesses, and mass backend data | Admit only as local genus-symbol object vocabulary or backend data for a typed local formed/lattice genus surface. Source tuple data are Conway-Sloane Jordan-block invariants: for odd `p`, each block is `(m,n,d)` with scale valuation, rank, and determinant squareclass in `{1,u}` for a normalized nonsquare; for `p=2`, each block is `(m,n,s,d,o)` with scale valuation, rank, odd/even type, determinant class in `{1,3,5,7}`, and oddity modulo `8`. `__init__(prime, symbol, check=True)` coerces only the prime to `ZZ`, stores the raw symbol list, and leaves `check` as a no-op in this source, so public project admission must not present `check` as validation. `canonical_symbol()` is identity for odd `p`; at `p=2` it caches `canonical_2_adic_reduction(symbol)`, so equality compares canonical local symbols rather than object identity or raw input order. `__repr__` and `_latex_` are Conway-Sloane display renderings using 2-adic trains/compartments and odd-prime scale/determinant exponents; `__ne__` is the boolean complement of canonical equality. `symbol_tuple_list()` returns a deep copy of the stored tuple data; `number_of_blocks()` counts stored positive-dimensional Jordan blocks. `gram_matrix(check=True)` constructs a block-diagonal rational Gram representative by `_gram_from_jordan_block`, and the source `check` branch recomputes the p-adic symbol and asserts canonical equality; it is a backend witness check, not a public proof certificate. `_gram_from_jordan_block(p, block, discr_form=False)` has no input checks; it constructs ordinary or discriminant-form block Gram matrices using odd-prime identity/nonsquare diagonal blocks and 2-adic `U`, `V`, and one-dimensional `W` blocks, with `discr_form=True` inverting the scale. `mass()`, `_standard_mass()`, and `_species_list()` are Conway-Sloane local-mass formula backend data: `mass()` rejects dimension at most `1`, multiplies diagonal factors `M_p(species,p)` and cross terms, and at `p=2` applies type factors from type-II ranks and adjacent type-I pairs. `prime()`, `determinant()`, `dimension()`, `scale()`, `norm()`, and `level()` are local symbol invariants: determinant is the p-power scale product, dimension is the rank sum, scale is the first Jordan scale with rank-zero convention `0`, norm is scale except at `p=2` where it uses the first block's odd/even type, and level is the maximal Jordan scale with rank-zero convention `1`. `is_even()` is automatic for odd `p` and at `p=2` tests the first block's scale/type. `direct_sum(other)` is local direct sum over the same prime only, merging blocks by scale and combining ranks, determinant classes, 2-adic type, and oddity. `excess()` returns p-excess for odd `p` or 2-adic oddity using the source normalization warning about doubling conventions. `trains()` and `compartments()` are defined only for `p=2` and raise for odd primes. `automorphous_numbers()` is spinor-norm backend evidence already routed in the spinor row. Public category exposure should name a local genus-symbol object with prime/place, block tuple convention, representative/witness status, local determinant/rank/scale/norm/level/excess data, direct-sum operation, 2-adic train/compartment data where applicable, and local mass formula status; raw tuple lists, display strings, source `check`, cached canonical-symbol state, and helper block constructors are backend/interoperability data, not lattice methods. |
| `GenusSymbol_global_ring`, `__init__(signature_pair, local_symbols, representative=None, check=True)`, `__repr__()`, `_latex_()`, `__eq__(other)`, `__ne__(other)`, and methods `is_even`, `signature_pair`, `spinor_generators`, `signature`, `determinant`/`det`, `dimension`/`dim`/`rank`, `direct_sum`, `discriminant_form`, `rational_representative`, `_compute_representative`, `representative`, `representatives`, `local_symbols`, `local_symbol`, `_standard_mass`, `mass`, `level`, `scale`, `norm` | `genera/genus.py:2312`, `2350`, `2384`, `2421`, `2447`, `2494`, `2525`, `2539`, `2636`, `2724`, `2741`, `2763`, `2780`, `2811`, `2845`, `2888`, `2952`, `2982`, `3115`, `3129`, `3148`, `3187`, `3266`, `3281`, `3299`; Sage genus docs/source | global genus-symbol object, global invariants, representative/discriminant witnesses, mass/class-enumeration backends, and spinor backend surfaces | Preserve as a global genus-symbol object for nondegenerate integral formed objects/lattices, not as a lattice constructor. `__init__(signature_pair, local_symbols, representative=None, check=True)` records a signature pair and local p-adic genus symbols; with `check=True`, Sage requires every local symbol be a `Genus_Symbol_p_adic_ring`, all local dimensions equal `signature_pair[0] + signature_pair[1]`, any representative be symmetric, local symbols be strictly prime-sorted, and the first local symbol be 2-adic. A non-`ZZ` representative is coerced to `ZZ` and made immutable, while `signature_pair` and the local-symbol list are stored without copy or coercion. Display and LaTeX are protocol surfaces using the cached representative only for rank at most `20`. Source equality compares only the local-symbol list length and canonical local-symbol equality and does not compare `signature_pair`; public project equality must not inherit that omission without a convention decision. `is_even()` returns `True` in rank zero and otherwise delegates to the first local symbol. `signature_pair()`, `signature()`, `dimension()`/`rank`, and `determinant()` expose global signature, rank, and signed determinant, with determinant computed as `(-1)^n` times the product of local determinants. `direct_sum(other)` forms the direct sum genus by adding signatures and direct-summing local symbols over the union of primes, using default absent-prime symbols supplied by `local_symbol(p)`. `local_symbols()` returns deep copies; `local_symbol(p)` coerces `p` to `ZZ`, returns a deep copy when present, and for absent odd primes returns the unramified rank-`r` symbol `[[0, r, det.kronecker(p)]]` while asserting `p != 2`. `discriminant_form()` builds a finite torsion quadratic/discriminant form by applying `_gram_from_jordan_block(..., discr_form=True)` to every local Jordan block and taking a block diagonal `TorsionQuadraticForm`; this is the genus-attached discriminant-form witness, not a replacement for a concrete lattice's quotient `L^#/L`. `rational_representative()` constructs a rational Gram representative from rank, determinant, negative signature count, and the Cassels Hasse-invariant support of the local Gram matrices. `_compute_representative(LLL=True)` is an integral representative backend: it starts from `IntegralLattice(4*q).maximal_overlattice()`, applies 2-adic and odd-prime `local_modification`, handles the odd genus branch by doubling/halving the basis around the 2-adic symbol, optionally LLL-reduces positive, negative, or indefinite forms via Sage/Pari, asserts `Genus(L) == self`, and caches an immutable `ZZ` Gram matrix. `representative()` is lazy cached witness construction. `representatives(backend=None, algorithm=None)` enumerates class representatives with special rank-zero, rank-one, rank-two binary-form, indefinite-seed, spinor-neighbor, neighbor-iteration, and Magma branches; backend selection defaults to Magma only for definite rank greater than `6`, raises for unknown backend, and stores immutable Gram matrices. `_standard_mass()` is Conway-Sloane standard-mass formula data using gamma, zeta, exact quadratic `L`-values, fundamental discriminants, and missing Euler-factor compensation. `mass(backend='sage')` is defined only for definite genera, returns `1/2` in rank one, computes Sage mass by multiplying local correction factors, or delegates to Magma after sign-normalizing a representative; it raises for indefinite genera and unknown backends. `level()`, `scale()`, and `norm()` are products of local level/scale/norm data. `_proper_spinor_kernel`, `_improper_spinor_kernel`, `_proper_is_improper`, and `spinor_generators(proper)` remain routed by the spinor-genus backend row; public exposure requires a spinor-genus spec naming the local square-class group, determinant-prime support, real-place contribution, and representative/search certificate. |
| `_proper_spinor_kernel()`, `_improper_spinor_kernel()`, `_proper_is_improper()`, `SpinorOperator`, `SpinorOperator._repr_()`, `SpinorOperators(primes)`, `SpinorOperators.__init__(primes)`, `SpinorOperators.__reduce__()`, `SpinorOperators._repr_()`, `SpinorOperators.to_square_class(x, p)`, `SpinorOperators.delta(r, prime=None)` | `genera/genus.py:1503`, `2558`, `2597`, `2636`, `2677`; `genera/spinor_genus.py:35`, `50`, `78`, `95`, `113`, `132`, `144`, `191`; Sage docs/source `genera/genus.py` and `genera/spinor_genus.py` | internal spinor-genus backend, local square-class coordinate group, and protocol surfaces | Sage marks `spinor_genus.py` as internal use. Preserve these as backend evidence for spinor-genus representative enumeration and mass/class computations, not as lattice methods. `SpinorOperators(primes)` constructs an elementary 2-group of local unit square-class coordinates for a tuple of primes whose first entry must be `2`; the two 2-adic generators represent unit classes `3` and `5` modulo `8`, and each odd prime contributes one nonsquare-unit generator. `SpinorOperator` elements display as local square-class tuples with 2-adic classes in `{1,3,5,7}` and odd-prime classes `±1`; `_repr_`, `__reduce__`, and group `_repr_` are display/pickle protocol surfaces. `to_square_class(x,p)` coerces `x` to `QQ`, requires `x` nonzero, requires `p` to be a coordinate prime, requires `x` to be a p-adic unit, and returns the local unit square-class coordinate. `delta(r, prime=None)` coerces `r` to `ZZ`; with no prime it requires `r` be coprime to all coordinate primes and multiplies the local square-class coordinates, with `prime=-1` it uses the real sign, and with a coordinate prime it separates the prime-power and unit parts according to Conway-Sloane's diagonal embedding. `Genus_Symbol_p_adic_ring.automorphous_numbers()` supplies local generator square classes for spinor norms of proper p-adic integral automorphisms, using odd-prime diagonal-pair and nonsquare supplementation rules or 2-adic small-block supplementation rules; its generating list is explicitly not minimal. `_proper_spinor_kernel()` builds the product group over the local-symbol primes and the subgroup generated by the real `-1` contribution when the signature has enough mixed signs plus local `delta(r, prime=p)` images of automorphous numbers. `_improper_spinor_kernel()` either returns the proper kernel or adjoins the obstruction element from `_proper_is_improper()`. `_proper_is_improper()` uses a cached/random integral representative, searches for a vector whose norm has local valuations matching the genus norm at local-symbol primes, forms a squarefree product from remaining parity data, and tests its diagonal embedding against the proper kernel; this is randomized backend witness search, not a public predicate without an explicit certificate. `spinor_generators(proper)` returns primes not dividing the determinant whose diagonal embeddings generate the quotient of the spinor-operator group by the proper or improper kernel. Public exposure requires a later source-backed spinor-genus spec naming the spinor-norm group/action object, local square-class coordinates, determinant prime support, real-place contribution, proper versus improper convention, representative/search certificate, and genus hypotheses. |
| `collect_small_blocks(G)`, `p_adic_normal_form(G, p, precision=None, partial=False, debug=False)`, `_find_min_p(G, cnt, lower_bound=0)`, `_get_small_block_indices(G)`, `_get_homogeneous_block_indices(G)`, `_homogeneous_normal_form(G, w)`, `_jordan_odd_adic(G)`, `_jordan_2_adic(G)`, `_min_nonsquare(p)`, `_normalize(G, normal_odd=True)`, `_normalize_2x2(G)`, `_normalize_odd_2x2(G)`, `_partial_normal_form_of_block(G)`, `_relations(G, n)`, `_two_adic_normal_forms(G, partial=False)` | `genera/normal_form.py:98`, `129`, `293`, `354`, `389`, `441`, `554`, `630`, `734`, `758`, `851`, `978`, `1016`, `1110`, `1375`; Sage p-adic normal-form docs/source | p-adic Gram-matrix normal-form backend surfaces | `p_adic_normal_form(G, p, precision=None, partial=False, debug=False)` has caller-free input data: a symmetric `n x n` Gram matrix `G` over `\QQ` and an integer coerced to `ZZ` as `p`; Sage docs say `p` should be prime but source does not check primality. The source clears denominators, records the denominator valuation `d`, splits off the radical by Hermite-form rank data, computes the nondegenerate part over fixed-mod `Zp(p, prec=precision)`, and then stacks the radical kernel back into the transformation. If `precision` is absent, Sage chooses `det(G_nondeg).valuation(p) + 4`, with source comments requiring at least modulo `8` at `p = 2`. Return data are `(D, B)`, where `D` is the local Jordan/normal Gram matrix, with a zero block for the radical and denominator scale divided back by `p^d`, and `B` is the transformation witness satisfying `D = B * G * B.T` at the chosen finite precision. Odd-prime normal form is block diagonal with `p^k G_k` and `G_k` either identity or identity with final diagonal entry the smallest nonsquare modulo `p`; at `p = 2`, `partial=True` returns the MirMor partial block form from `U`, `V`, and one-dimensional `W_a`, `W_b` blocks, while the full branch applies additional two-adic relations. Public admission belongs to a typed local Gram-presentation normal-form or local-equivalence witness surface naming the prime/place, rational Gram presentation, finite precision, radical/degenerate status, denominator scale, partial/full status, normal form, and transformation witness. `collect_small_blocks(G)` is block-diagonal decomposition plumbing for already-normalized `1`- and `2`-dimensional blocks. The underscore helpers are implementation steps for pivot search, block indexing, homogeneous block normalization, Jordan decomposition, square-class normalization, two-adic relations, and normal-form assembly; they are not public lattice methods. `debug` is assertion-only runtime checking and remains backend-only. |
| `basis_of_short_vectors(show_lengths=False)`, `short_vector_list_up_to_length(len_bound, up_to_sign_flag=False)`, `short_primitive_vector_list_up_to_length(len_bound, up_to_sign_flag=False)` | `quadratic_form__automorphisms.py:21`, `97`, `221`; Sage docs `quadratic_form.html` | positive-definite `ZZ` quadratic-form short-vector enumeration backend surfaces | Preserve as PARI `qfminim`-backed short-vector enumeration evidence for positive-definite integral quadratic-form presentations on `\ZZ^n`. `basis_of_short_vectors(show_lengths=False)` is a cached backend search: it calls PARI `qfminim` with a source cap of `10000` vectors while increasing represented values until the returned short vectors span, then adds negatives for sorting, selects pivot columns, makes the selected basis vectors immutable, and returns a basis of `\ZZ^n`; `show_lengths=True` returns the same basis together with the corresponding `Q(v)` values. Unlike the bounded-list method, this source does not perform its own positive-definiteness check before calling PARI, so definiteness is a public hypothesis rather than a fallback. `short_vector_list_up_to_length(len_bound, up_to_sign_flag=False)` explicitly raises unless the caller is positive definite, returns `[]` for `len_bound <= 0`, converts Sage's strict condition `Q(v) < len_bound` into PARI's doubled Hessian bound `2 * (len_bound - 1)`, computes represented values from the PARI output, filters any too-long PARI vectors, returns lists indexed by integer represented value, appends the zero vector by hand when the bound is positive, and either returns both signs or one sign representative according to `up_to_sign_flag`. `short_primitive_vector_list_up_to_length` is the primitive sublist selected by `GCD(v) == 1`, so the zero vector is removed from the primitive length-zero bucket. Public admission belongs to a typed definite vector-enumeration or minimum/short-vector surface naming the formed object, integral presentation, strict bound, primitive predicate, sign quotient, represented values, and vector witnesses; PARI bound conversion, qfminim vector cap, cached basis search, and Sage option flags are backend/interoperability data. |
| `_compute_automorphisms()`, `automorphism_group()`, `automorphisms()`, `number_of_automorphisms()`, `set_number_of_automorphisms(num_autos)` | `quadratic_form__automorphisms.py:257`, `289`, `335`, `373`, `401`; Sage docs `quadratic_form.html` | definite `ZZ` formed-object Aut backend and unsafe audit surfaces | Sage computes PARI `qfauto` automorphisms only for definite quadratic forms over `ZZ`: `_compute_automorphisms()` raises `NotImplementedError` outside `ZZ`, raises `ValueError` for non-definite forms, and then caches the PARI automorphism count and PARI generator matrices. `automorphism_group()` coerces those generators into a matrix space over the caller base ring's fraction field and returns a raw `MatrixGroup`; `automorphisms()` enumerates matrices from that group, with docs checking `Q(M) == Q`. Public project Aut surfaces must route through typed automorphism elements in the Hom/End/Aut story, with source equation `M^T G M = G` or equivalently preservation of `Q`, not through raw matrix groups as the mathematical owner. `number_of_automorphisms()` is a cached cardinality invariant/backend metadata count for determinant `1` and `-1` automorphisms. `set_number_of_automorphisms(num_autos)` performs unchecked external initialization, can record nonsensical values, and remains audit-only via `list_external_initializations()`. Indefinite or non-`ZZ` automorphism computation is a missing backend/theorem obligation rather than an admitted fallback. |
| `swap_variables(r, s, in_place=False)`, `multiply_variable(c, i, in_place=False)`, `divide_variable(c, i, in_place=False)`, `elementary_substitution(c, i, j, in_place=False)`, `add_symmetric(c, i, j, in_place=False)` | `quadratic_form__variable_substitutions.py:20`, `75`, `117`, `245`, `318`; Sage docs `quadratic_form.html` | quadratic-form presentation coordinate-change surfaces | These are coordinate substitutions on the variable presentation of a quadratic form, not lattice constructors or object equality. Each method takes a bound caller presentation, integer coordinate indices, and when present a coefficient `c` expected in the caller base ring. With `in_place=False`, Sage copies the caller by `self.parent()(self.base_ring(), self.dim(), self.coefficients())`, mutates the copy, and returns a new `QuadraticForm`; with `in_place=True`, it mutates coefficient storage and returns no mathematical object. `swap_variables(r, s)` exchanges coordinate variables `x_r` and `x_s`. `multiply_variable(c, i)` applies `x_i -> c*x_i`, multiplying the diagonal coefficient by `c^2` and off-diagonal coefficients by `c`. `divide_variable(c, i)` applies `x_i -> x_i/c` when division is defined in the base ring, dividing the diagonal coefficient by `c^2` and off-diagonal coefficients by `c`. `elementary_substitution(c, i, j)` is the documented shear `x_i -> x_i + c*x_j`, witnessed in source by updating coefficients in the `j` coordinate: for `k` different from `i,j`, `a'_{j,k}=a_{j,k}+c a_{i,k}`; `a'_{j,j}=a_{j,j}+c a_{i,j}+c^2 a_{i,i}`; and `a'_{i,j}=a_{i,j}+2c a_{i,i}`. The docs also witness this via evaluating the original form on the elementary matrix. `add_symmetric(c, i, j)` is a local-normal-form compatibility wrapper that performs `x_j -> x_j + c*x_i` by delegating to `elementary_substitution(c, j, i)`, matching the old matrix-model operation that adds `c` times the `j`-th row/column to the `i`-th row/column. Public project semantics must expose these, if admitted, as typed presentation transformations with base-ring coefficient, source and target presentations, coordinate-transformation matrix/isometry witness when equivalence is claimed, and an explicit no-mutation public surface. The `in_place` flag, coefficient-storage updates, and compatibility wrapper naming are Sage interop/runtime data. |
| `scale_by_factor(c, change_value_ring_flag=False)`, `extract_variables(QF, var_indices)` | `quadratic_form__variable_substitutions.py:162`, `214`; Sage docs `quadratic_form.html` | scalar rescaling and coordinate sub-presentation construction for quadratic forms | `scale_by_factor(c)` multiplies every coefficient by a scalar from the fraction field of the value ring and attempts to coerce the scaled coefficient list back into the caller base ring; on success it returns a same-dimension `QuadraticForm` over that base ring. This changes the represented quadratic-form values and is therefore scalar rescaling of the form, not coordinate substitution, lattice equality, or an isometry. If coercion fails and `change_value_ring_flag=False`, Sage raises; if `change_value_ring_flag=True`, the installed source immediately raises that the branch is untested before the commented fraction-field widening code. Public admission belongs to a typed scalar-rescaling construction naming the scalar, source form, target value/base ring, and changed value form; the value-ring-widening flag is an explicit missing tested implementation/source obligation, not public API. `extract_variables(QF, var_indices)` is a caller-free helper taking a quadratic-form presentation `QF` and an ordered list of nonnegative coordinate indices, and returns the coordinate sub-presentation with coefficients `QF[var_indices[i], var_indices[j]]` in that order. It is not a categorical subobject unless an inclusion/projection or coordinate-subspace witness is supplied. |
| `find_entry_with_minimal_scale_at_prime(p)`, `local_normal_form(p)`, `jordan_blocks_by_scale_and_unimodular(p, safe_flag=True)`, `jordan_blocks_in_unimodular_list_by_scale_power(p)` | `quadratic_form__local_normal_form.py:27`, `82`, `233`, `357`; Sage docs `quadratic_form.html` | p-adic local presentation-normalization and Jordan-decomposition backend surfaces | These are `ZZ` quadratic-form algorithms for local Jordan decomposition at a positive prime `p`, not general lattice constructors or canonical equality. `find_entry_with_minimal_scale_at_prime(p)` scans the upper-triangular coefficient presentation, chooses an entry with minimal `p`-adic valuation, prefers diagonal entries by scan order, and subtracts `valuation(2, p)` from off-diagonal entries because scale is read from the associated symmetric/Hessian convention; it returns coordinate indices only. `local_normal_form(p)` rejects non-`ZZ` base rings and non-prime `p`, copies the caller, repeatedly selects a minimal-scale `1`- or `2`-dimensional block, uses Sage coordinate operations to cancel cross terms over `\ZZ`, raises on degenerate forms or failed cancellation, and returns only a new `ZZ` quadratic-form presentation arranged by increasing local scale/norm into Jordan blocks of size at most `2`. The source does not return the accumulated transformation matrix, so an equivalence/isometry witness is a missing public-witness obligation if the project exposes more than backend normal-form evidence. `jordan_blocks_by_scale_and_unimodular(p, safe_flag=True)` calls `local_normal_form(p)`, parses adjacent `1`- and `2`-blocks into cached pairs `(s_i, L_i)`, and rescales each component by `p^{-s_i}` so `L_i` represents a block-diagonal unimodular local component over `\ZZ_p`; the block decomposition is explicitly nonunique, `safe_flag=True` deep-copies cached data, and `safe_flag=False` exposes mutable cached objects. `jordan_blocks_in_unimodular_list_by_scale_power(p)` is defined only for forms over `ZZ`, with docs warning that for `p=2` the indexing works correctly only when the Gram matrix is integral; it rejects any negative scale exponent and returns a list indexed by nonnegative scale, filling absent scales with zero-dimensional forms. Public project vocabulary should expose a typed local formed object, prime/place, local Jordan-decomposition or genus-symbol data, scale exponents including the possibility of negative scales at the pair-list level, unimodular component presentations, nonuniqueness/status, and a transformation witness when equivalence is claimed. The pivot selector, cache-copy flag, mutable coefficient operations, zero-form padding, and list-indexing convenience are backend/runtime data, not lattice methods. |
| `count_congruence_solutions_as_vector`, `count_congruence_solutions`, `count_congruence_solutions__good_type`, `count_congruence_solutions__zero_type`, `count_congruence_solutions__bad_type`, `count_congruence_solutions__bad_type_I`, `count_congruence_solutions__bad_type_II`, `count_modp__by_gauss_sum`, `CountAllLocalTypesNaive`, `count_all_local_good_types_normal_form`, `count_modp_solutions__by_Gauss_sum`, `local_good_density_congruence_odd`, `local_good_density_congruence_even`, `local_good_density_congruence`, `local_zero_density_congruence`, `local_badI_density_congruence`, `local_badII_density_congruence`, `local_bad_density_congruence`, `local_density_congruence`, `local_primitive_density_congruence`, `local_density`, `local_primitive_density` | `quadratic_form__count_local_2.py:15`, `73`, `98`, `123`, `148`, `173`, `198`; `count_local_2.pyx:9`, `147`, `286`; `quadratic_form__local_density_congruence.py:19`, `56`, `142`, `319`, `394`, `467`, `630`, `776`, `834`, `902`; `quadratic_form__local_density_interfaces.py:11`, `68`; Sage docs/source `quadratic_form.html` | local representation-density backend surfaces | These compute local counts or rational local densities for `Q(x) = m` at a prime `p`. Direct congruence counters take `(p, k, m, zvec, nzvec)`, where `k > 0` fixes the modulus `p^k`, `zvec` is a coordinate-index list forced to vanish modulo `p`, and `nzvec` is a coordinate-index list whose indexed subvector must be nonzero modulo `p`; the vector-valued front end returns `[All, Good, Zero, Bad, BadI, BadII]`. The density front ends take `(p, m, Zvec=None, NZvec=None)` after p-integral local-normal-form preparation; for `p=2`, the source warns that these index sets must include all or none of the coordinates in each Jordan block. Preserve all of this as backend evidence for local representation and mass formulas. Public category API must name the represented value, prime/local place, modulus or primitive flag, congruence-index data, solution type, and count-versus-density return object; `Zvec`/`NZvec`, Cython helper names, Gauss-sum determinant shortcuts, and good/zero/bad helper names are not lattice methods. |
| `theta_series(Max=10, var_str='q', safe_flag=True)`, `theta_by_pari(Max, var_str='q', safe_flag=True)`, `theta_by_cholesky(q_prec)`, `theta_series_degree_2(Q, prec)`, and local helper `B_v1(v)` | `quadratic_form__theta.py:18`, `72`, `136`, `263`, `341`; Sage docs `quadratic_form.html` | theta-series, representation-count, and Siegel-coefficient backend surfaces | These are arithmetic invariant/backend surfaces for positive-definite integral quadratic-form presentations, not lattice constructors, equality methods, or ordinary presentation changes. `theta_series(Max, var_str, safe_flag)` coerces `Max` to an integer precision and delegates to `theta_by_pari`, except that the documented `Max='mod_form'` branch raises because Sage has not implemented the Sturm-bound/modular-form coefficient cutoff. `theta_by_pari(Max, var_str, safe_flag)` uses and caches `representation_number_list(Max)`: if `var_str` is empty it returns the coefficient vector whose `i`-th entry counts vectors with represented value `i`, while a nonempty `var_str` returns a power series over `ZZ` with precision `O(q^Max)`. `safe_flag` only controls whether the cached vector return is deep-copied; it is runtime/cache integrity data. `theta_by_cholesky(q_prec)` is an explicit real-Cholesky enumeration backend that returns a `q`-power series with coefficients through `q^q_prec` and uses floating precision plus an error check, so it is backend evidence rather than a canonical exact public algorithm selector. `theta_series_degree_2(Q, prec)` is a degree-2 theta surface for integral positive-definite forms: `prec` gives a discriminant cutoff, `prec=0` returns the empty dictionary, and otherwise Sage returns a dictionary keyed by `GL_2(\ZZ)`-reduced binary quadratic-form coefficient triples with integer coefficients computed from `vectors_by_length`; its nested helper `B_v1(v)` is local Hessian-pairing plumbing for this coefficient count. Public project admission must name the formed object, theta degree, precision or discriminant bound, coefficient target object, representation-count interpretation, and positive-definite/integral hypotheses; the power-series variable name, cache flag, numerical backend selector, verbose timing, local pairing helper, and unimplemented modular-form shortcut are interop/backend data or explicit missing theorem/implementation obligations. |
| `find_primitive_p_divisible_vector__random(p)`, `find_primitive_p_divisible_vector__next(p, v=None)`, `find_p_neighbor_from_vec(p, y, return_matrix=False)`, `neighbor_iteration(seeds, p, mass=None, max_classes=None, algorithm=None, max_neighbors=1000, verbose=False)`, nested `p_divisible_vectors(Q, max_neighbors)` strategy helpers, and `orbits_lines_mod_p(p)` | `quadratic_form__neighbors.py:17`, `56`, `142`, `252`, `327`, `332`, `341`, `380`; Sage docs `quadratic_form.html` | p-neighbor construction, finite-line orbit, and genus-enumeration backend surfaces | These are algorithmic surfaces for Kneser-style `p`-neighbor exploration of quadratic forms/lattices, not constructors for arbitrary lattices or ordinary equality methods. `find_primitive_p_divisible_vector__random(p)` samples nonzero coordinate vectors in `(\ZZ/p\ZZ)^n` until the caller value lies in `\ZZ` and is divisible by `p`, with a hard random-search failure after `1000` attempts. `find_primitive_p_divisible_vector__next(p, v=None)` deterministically enumerates normalized line representatives in `L/pL` whose last nonzero coordinate is `1`, returns the next representative with `Q(v)` integral and `p`-divisible, returns `None` at exhaustion, and treats malformed `v` only by printing a warning; this is line-enumeration backend state, not a public iterator contract. `find_p_neighbor_from_vec(p, y, return_matrix=False)` implements the documented neighbor `\ZZ y/p + L_y`, where `L_y = \{x \in L \mid b(x,y) \in p\ZZ\}` and `b(x,y)=q(x+y)-q(x)-q(y)`: the source coerces `p` to `ZZ`, requires `Q(y)` and the Hessian value `y^T G y` to be divisible by `p`, allows only `ZZ` or `QQ` base rings, treats the `QQ` half-integral branch as an integral-domain convention when the Hessian matrix is integral, corrects odd-prime witnesses whose square is not yet divisible by `p^2` using a primitive/maximality check, and applies the special `p=2` valuation and even/maximality branches before building the Hermite-basis matrix. With `return_matrix=True` Sage returns the transpose transformation matrix; otherwise it returns the transformed quadratic-form presentation with Gram/Hessian data `B G B^T` over the caller base ring. `orbits_lines_mod_p(p)` computes representatives of nonzero lines in `L/pL` under the automorphism group, transposing generators because Sage's automorphism group acts from the left while GAP's `OnLines` action is used on the right; the result is a list of vectors over `GF(p)`. `neighbor_iteration(seeds, p, mass=None, max_classes=None, algorithm=None, max_neighbors=1000, verbose=False)` takes a list of same-genus quadratic-form seeds, chooses `orbits` or `random` by the `p^n > 2^18` heuristic unless an algorithm string is supplied, selects one of three nested `p_divisible_vectors` generator helpers for orbit representatives, deterministic exhaustion, or random primitive-vector search, explores neighbors, reduces new classes by `lll()`, identifies classes by `is_globally_equivalent_to`, and stops by mass equality, `max_classes`, or exhausted waiting list; mass equality is the source-backed completion certificate, while warning/printing records incomplete enumeration. Public project admission belongs to typed p-neighbor and genus-enumeration algorithm surfaces naming the local prime, formed object, line/vector witness in `L/pL`, integrality/maximality/evenness hypotheses, neighbor object, optional transformation witness, orbit representatives under `Aut(L,q)`, isometry-class test, and mass/completion certificate. Random/next/orbits selectors, nested generator helpers, search caps, warning text, verbosity, raw strategy strings, mutable iterator state, and GAP/PARI/Hermite implementation details are backend controls or interop evidence, not lattice methods. |
| `reduced_binary_form1()`, `reduced_binary_form()`, `minkowski_reduction()`, `minkowski_reduction_for_4vars__SP()`, `reduced_ternary_form__Dickson()` | `quadratic_form__reduction_theory.py:13`, `88`, `142`, `277`, `72`; Sage docs `quadratic_form.html` | quadratic-form presentation reduction and normal-form backend surfaces | These are basis/presentation-normalization algorithms returning a reduced quadratic-form presentation together with a transformation matrix witness; they are not lattice equality, constructors, or canonical isometry-class representatives without the named convention and hypotheses. `reduced_binary_form1()` is restricted to dimension `2`, works within the proper class by composing determinant-one transformations over the caller base ring, and reduces `ax^2 + bxy + cy^2` until `|b| <= a <= c` with the source/doc boundary sign convention; non-binary callers raise `TypeError`. `reduced_binary_form()` is not restricted to binary forms: it loops through every pair of coordinates in the caller dimension, orders diagonal entries weakly, applies shear transformations when `|Q[i,j]| > Q[i,i]`, and returns the resulting presentation plus accumulated matrix. Public exposure of that surface must say it is a pairwise binary-style presentation-reduction algorithm, not a standard canonical Minkowski or lattice reduction. `minkowski_reduction()` requires positive definiteness, rejects dimensions greater than `4`, searches vectors with coefficients in `{-1,0,1}` as allowed by the dimension bound, and returns `(Q', M)` with `Q'` computed from `M^T Q M`; its public meaning is a typed positive-definite reduction algorithm with dimension and reduced-condition convention. `minkowski_reduction_for_4vars__SP()` is a separate Schulze-Pillot four-variable variant: the source checks exactly four variables, performs the same short-vector replacement loop with extra off-diagonal tie criteria, then applies diagonal ordering and sign-normalization steps via variable swaps and sign changes before returning the transformed form and matrix; the source does not independently check positive definiteness even though the mathematical convention is positive-definite reduction. `reduced_ternary_form__Dickson()` is a documented Dickson reduced-ternary-form surface, but installed Sage immediately raises `NotImplementedError`, so project exposure is an explicit missing ternary-reduction theorem/implementation obligation. Public project admission belongs to typed reduction/normal-form algorithm surfaces naming caller formed object, dimension, definiteness and base-ring hypotheses, reduced-condition convention, whether the transformation is proper/determinant-one, return presentation, and transformation witness. Deep copies, in-place variable swaps/sign changes inside the algorithm, rounding/flooring details, and the failed Dickson stub are backend/runtime evidence rather than admitted ordinary lattice methods. |
| `disc()`, `content()`, `adjoint()`, `antiadjoint()`, `is_adjoint()`, `reciprocal()`, `omega()`, `delta()`, `level__Tornaria()`, `discrec()`, `hasse_conductor()`, `clifford_invariant()`, `clifford_conductor()`, `basiclemma()`, `basiclemmavec()`, `xi()`, `xi_rec()`, `lll()` | `quadratic_form__ternary_Tornaria.py:35`, `63`, `110`, `138`, `167`, `186`, `212`, `227`, `242`, `269`, `288`, `314`, `351`, `396`, `411`, `454`, `484`, `510`; Sage docs `quadratic_form.html` | Tornaria arithmetic, reciprocal, local-character, and reduction backend surfaces | Preserve these as Tornaria/Dickson arithmetic evidence for integral quadratic-form presentations, especially ternary forms. `disc()` uses the Tornaria convention `det(B)/2` in odd dimension and `(-1)^(dim/2) det(B)` in even dimension for `2Q(x)=x^t B x`; `content()` is coefficient gcd and the source warns that it is only valid over Euclidean domains, probably `\ZZ`. `adjoint()` returns the classical adjugate-matrix form, multiplying by `2` in odd dimension; `antiadjoint()` inverts the adjoint construction only when the required `(dim-1)`-st root of `disc` coerces into the base ring, using the extra `1/4` odd-dimensional scaling branch, and otherwise raises `not an adjoint`. `is_adjoint()` is this antiadjoint-existence predicate. `reciprocal()`, `omega()`, `delta()`, `level__Tornaria()`, and `discrec()` are adjoint/reciprocal-form constructions and invariants: reciprocal is the primitive adjoint rescaled to the original content, `omega` is the content of the primitive adjoint, `delta` is the reciprocal/adjoint omega, and Tornaria level is `abs(disc)/(omega*content^dim)`, agreeing with `level(B)` in even dimension and `level(B)/4` in odd dimension. `hasse_conductor()`, `clifford_invariant(p)`, and `clifford_conductor()` are local rational-equivalence and Clifford/Brauer invariants built from Hasse invariants, Hilbert symbols, dimension mod `8`, and primes dividing `2*level()`. `basiclemma(M)` and `basiclemmavec(M)` search diagonal and off-diagonal coefficients for CRT vector data producing a represented value coprime to `M`; `M=±1` returns the zero vector, and failure is a non-primitive-form obstruction rather than a best-effort result. `xi(p)` and `xi_rec(p)` are Dickson genus-character values, with validity conditions tied to `disc` in dimension `2` and `omega` in dimensions at least `3`, and `p=-1` or `2` handled by the Kronecker-symbol branch. `lll()` applies PARI Gram-LLL to the coefficient matrix and returns the transformed presentation, with no explicit transformation witness returned. Public project use must split these into typed invariant, adjoint/reciprocal construction, local-character/conductor, represented-value witness, and presentation-reduction surfaces with stated integrality, Euclidean-domain/base-ring, dimension, nondegeneracy, prime/place, modulus, and transformation/witness data. They are not lattice-owned constructors, ambient free-module methods, or a single generic ternary arithmetic API. |
| `representation_number_list(B)`, `representation_vector_list(B, maxvectors=10**8)`, `is_zero(v, p=0)`, `is_zero_nonsingular(v, p=0)`, `is_zero_singular(v, p=0)` | `quadratic_form__ternary_Tornaria.py:529`, `545`, `592`, `612`, `635`; Sage docs `quadratic_form.html` | bounded representation data and conic zero-locus backend surfaces | Preserve these as backend evidence for two distinct mathematical questions. `representation_number_list(B)` returns PARI-backed counts for represented values `< B`, with Sage inserting the zero representation and doubling the nonzero-vector counts from `qfrep(B - 1, 1)`. `representation_vector_list(B, maxvectors=10**8)` is restricted by source and PARI to positive-definite forms; it returns coordinate-vector witnesses grouped by represented value `< B`, includes `[0]` at value `0`, pairs each nonzero vector with its negative, and raises when `maxvectors` is too small to enumerate the PARI result. The `is_zero` cluster tests whether a vector lies on the conic `Q(x)=0` exactly or modulo `p`; nonsingularity is the condition that the Gram-gradient row `v * Gram(Q)` is nonzero exactly or modulo `p`, and singularity is the zero-gradient branch. Public project vocabulary should expose typed bounded representation-count data, bounded representation-witness data with positivity and resource-cap hypotheses, and exact/local zero-locus or isotropy evidence with singular/nonsingular point classification. PARI vector-list conventions, raw coordinate lists, and `maxvectors` are backend enumeration controls, not ordinary lattice methods. |
| `siegel_product(u)`, `shimura_mass__maximal()`, `GHY_mass__maximal()`, `mass__by_Siegel_densities(odd_algorithm='Pall', even_algorithm='Watson')`, `Pall_mass_density_at_odd_prime(p)`, `Watson_mass_at_2()`, `Kitaoka_mass_at_2()`, `mass_at_two_by_counting_mod_power(k)` | `quadratic_form__siegel_product.py:36`; `quadratic_form__mass.py:35`, `51`; `quadratic_form__mass__Siegel_densities.py:26`, `116`, `174`, `258`, `341`; Sage docs/source `quadratic_form__siegel_product.py`, `quadratic_form__mass.py`, and `quadratic_form__mass__Siegel_densities.py` | genus and mass-formula backend surfaces | Preserve these as mass, local-density, and maximal-mass formula witnesses for integral quadratic-form presentations, not as lattice constructors or stable ordinary lattice methods. `siegel_product(u)` coerces `u` to `ZZ`, takes `n=self.dim()` and Hessian determinant `d=self.det()`, builds odd- or even-dimensional generic factors from fundamental discriminants, Bernoulli/quadratic Bernoulli data, square-root archimedean factors, and explicit `2Q` determinant-normalization compensations, omits generic factors at primes dividing `S=2*d*u`, reinserts local factors via `self.local_normal_form(p).local_density(p,u)`, and divides by `2` in dimension `2`; source comments and examples warn about determinant normalization and unreliable higher `2`-power behavior. `mass__by_Siegel_densities(odd_algorithm='Pall', even_algorithm='Watson')` is documented as broken; it computes a determinant-powered generic gamma/zeta/quadratic-`L` product and local correction factors at primes dividing `2*det(Q)`, with exactly one odd-prime branch (`Pall`) and two `p=2` branches (`Watson`, `Kitaoka`), raising `TypeError` for unsupported algorithm strings. `Pall_mass_density_at_odd_prime(p)` only checks `p > 2`, uses `jordan_blocks_by_scale_and_unimodular(p)` to form `(scale, dimension, determinant)` block data, multiplies Pall generic factors and cross-scale powers, prints the Jordan data, block masses, and final local factor to stdout, and returns a rational local mass density; prime validation and diagnostic output are backend/runtime behavior. `Watson_mass_at_2()` and `Kitaoka_mass_at_2()` both decompose 2-adic Jordan blocks into diagonal and non-diagonal dictionaries over scale ranges using a mutable zero-dimensional form initializer, then apply their named formula conventions; the docs warn that both need careful checking. `mass_at_two_by_counting_mod_power(k)` is a congruence-counting backend for local mass at `2` modulo `2^k`: docs require `k >= 1` and call it too slow, while source changes the form to `ZZ/2^kZZ`, counts matrices satisfying `Q1(M)=Q1`, and returns `1/2 * ct / 2^(k*n*(n-1)/2)` without its own explicit lower-bound check. `shimura_mass__maximal()` and `GHY_mass__maximal()` have written maximal-lattice formula docstrings, with Shimura described for totally real number fields and an odd-rank technical restriction and GHY described for number fields, but installed source bodies are `pass` and therefore return `None`; public project exposure is an explicit missing maximal-mass theorem/implementation obligation with maximality, base-field, rank-parity, and formula-reference hypotheses, not an admitted method. Public admission belongs to typed genus/mass invariant or local-density surfaces naming the formed object, integrality/maximality, base-field and rank hypotheses, prime/place, formula convention, selected finite backend algorithm, normalization convention, and rational return/certificate. Warning text, diagnostic printing, raw algorithm strings, mutable zero-form initializers, and congruence modulus scans are backend/runtime controls rather than ordinary lattice API. |
| `parity(allow_rescaling_flag=True)`, `is_even(allow_rescaling_flag=True)`, `is_odd(allow_rescaling_flag=True)`, `conway_species_list_at_odd_prime(p)`, `conway_species_list_at_2()`, `conway_octane_of_this_unimodular_Jordan_block_at_2()`, `conway_diagonal_factor(p)`, `conway_cross_product_doubled_power(p)`, `conway_type_factor()`, `conway_p_mass(p)`, `conway_standard_p_mass(p)`, `conway_standard_mass()`, `conway_mass()`, and `M_p(species, p)` | `quadratic_form__mass__Conway_Sloane_masses.py:14`, `110`, `129`, `148`, `217`, `301`, `386`, `426`, `459`, `479`, `508`, `544`, `584`; `genera/genus.py:3434`; Sage docs/source | Conway-Sloane genus-symbol and mass backend surfaces | These are Conway-Sloane local genus-symbol and mass-formula witnesses for `ZZ` quadratic-form presentations. `parity(allow_rescaling_flag=True)` is not the project lattice parity predicate: it is the Conway-Sloane parity of the `2`-adic Jordan component after optional similitude/rescaling to make the minimal-scale component unimodular; rank zero returns `"even"`, and `allow_rescaling_flag=False` raises when a negative scale would require rescaling before inspecting scale `0`. `is_even` and `is_odd` are thin wrappers around this convention. `conway_species_list_at_odd_prime(p)` requires an odd prime, returns `[0]` in rank zero, and otherwise returns one species per unimodular Jordan block using dimension parity, determinant Legendre symbols, the `p % 4 == 3`, `n % 4 == 2` sign branch, and the convention that species `0` is positive. `conway_species_list_at_2()` returns scalar `0` in rank zero, otherwise builds the `2`-adic species list from unimodular Jordan blocks, endpoint sentinel species `1` only when the first or last listed block is odd, adjacent odd-block binding, octane classes, and the `2t` value determined by evenness and dimension. `conway_octane_of_this_unimodular_Jordan_block_at_2()` assumes a full unimodular `2`-adic Jordan block in Sage's block-diagonal convention with `1 x 1` blocks first; even blocks use determinant modulo `8`, while odd blocks are diagonalized through the source's `2xy` and `2x^2+2xy+2y^2` branches and may raise runtime errors when the expected block convention fails. `conway_diagonal_factor(p)` and standalone `M_p(species, p)` are diagonal species-factor formulae: species `0` contributes `1`, positive odd species use the product over even exponents below the species, and even species add the signed factor `1 - sign(species) p^{-abs(species)/2}`. `conway_cross_product_doubled_power(p)` computes twice the cross-product exponent from ordered Jordan block dimensions; `conway_type_factor()` is the special `p=2` factor `2^(n11-n2)` using even-block dimensions and adjacent odd/odd pairs. `conway_p_mass(p)` combines diagonal, cross-product, and type factors; `conway_standard_p_mass(p)` is the generic local factor with even-rank discriminant squareclass `D = (-1)^s det(Q) 2^n`; `conway_standard_mass()` is the archimedean/global standard product using gamma, zeta, and the even-rank quadratic `L`-value branch; `conway_mass()` doubles the form to an integral-matrix presentation, corrects the standard mass at primes dividing `2*det(Q)`, caches `__conway_mass`, canonicalizes radicals, and returns the absolute rational mass. Public admission belongs to a typed Conway-Sloane genus-symbol/mass invariant surface naming the formed object, prime/place, Jordan decomposition convention, species/octane/type-factor data, optional rescaling hypothesis, standard-factor convention, and rational mass factor or global mass. Cache state, scalar-versus-list sentinel return quirks, raw helper lists, runtime block-convention errors, backend factor names, and symbolic-package dependencies are implementation evidence, not separate lattice methods. |
| `QuadraticFormLocalRepresentationConditions`, `QuadraticFormLocalRepresentationConditions.__repr__()`, `QuadraticFormLocalRepresentationConditions.__eq__(right)`, `squareclass_vector(p)`, `local_conditions_vector_for_prime(p)`, `is_universal_at_prime(p)`, `is_universal_at_all_finite_primes()`, `is_universal_at_all_places()`, `is_locally_represented_at_place(m, p)`, `is_locally_represented(m)` | `quadratic_form__local_representation_conditions.py:20`, `204`, `240`, `284`, `311`, `365`, `410`, `441`, `481`, `549`; Sage docs/source | local representation-condition object and predicate backend surfaces | These package integral local-representation obstruction data for a `ZZ` quadratic form, with the real place represented by `p = infinity`; non-`ZZ` base rings are rejected at construction. Dimension `0` and `1` are special cases: the zero-dimensional form stores no coefficient and represents only zero, while a one-dimensional form stores the diagonal coefficient and represents integral square multiples of it globally or nonnegative/`p`-adic-square quotients locally. In dimension at least `2`, the condition object always stores a real vector first, then scans primes dividing the level `N`; for each squareclass it calls the local normal form density on `m, m*p^2, ...` while `m < 4*N*p^2`, stores the first half-valuation threshold `k`, and stores `infinity` for a fully obstructed squareclass. Non-obstructing finite primes are omitted from `exceptional_primes` and reconstructed by dimension-dependent generic vectors. For `p > 2` the finite squareclasses are `[1, u, p, up]` where `u` is the least quadratic nonresidue; for `p = 2` they are `[1, 3, 5, 7, 2, 6, 10, 14]`; for `p = infinity`, `squareclass_vector(p)` returns `[1, -1]`. `local_conditions_vector_for_prime(p)` deep-copies stored exceptional vectors or returns generic vectors: in rank at least `3`, all finite squareclasses have threshold `0`; in rank `2`, odd primes represent only the first two generic squareclasses and `p=2` obstructs the odd-valuation squareclasses; in rank `0`, all nonzero finite squareclasses are obstructed; the source rank-one branch computes a vector but does not return it and falls through to `RuntimeError`, so direct rank-one calls to this helper are a backend bug, not public API. `is_universal_at_prime(p)` is generic-true for rank at least `3` away from exceptional primes, generic-false for rank at most `2` away from exceptional primes, and at infinity tests indefinite real sign availability; all-place universality is false in rank at most `2` and otherwise tests all exceptional finite primes plus infinity. `is_locally_represented_at_place(m, p)` rejects non-rational `m`, represents `0` everywhere, uses coefficient quotients in rank `1`, tests real sign slots at infinity, and at finite places chooses the squareclass whose quotient is a `p`-adic square and compares the stored threshold to half the valuation. `is_locally_represented(m)` additionally rejects negative valuation at non-exceptional numerator/denominator primes before checking every exceptional place. `__repr__` is display protocol for the dimension and vector conventions, and `__eq__` is protocol equality: non-condition objects compare false, ranks at most `2` require equal dimension, rank `0` conditions all compare equal, rank `1` compares coefficients, and higher ranks compare `exceptional_primes` plus vectors. Public ownership belongs to a typed local representation-condition or representation-problem surface naming the formed object, represented rational value, finite or real place set, squareclass representatives, threshold/obstruction vector, dimension convention, and boolean or equality witness. Display strings, private arrays, generic-vector sentinels, rank-one helper fallthrough, density-search bounds, and protocol equality are backend/runtime data rather than lattice methods. |
| `local_representation_conditions(recompute_flag=False, silent_flag=False)`, `is_locally_universal_at_prime(p)`, `is_locally_universal_at_all_primes()`, `is_locally_universal_at_all_places()`, `is_locally_represented_number_at_place(m, p)`, `is_locally_represented_number(m)` | `quadratic_form__local_representation_conditions.py:608`, `716`, `760`, `788`, `817`, `862`; Sage docs `quadratic_form.html` | module-level wrapper interop for local representation predicates | These are `QuadraticForm` bound compatibility wrappers around the condition object above. `local_representation_conditions(recompute_flag=False, silent_flag=False)` has no mathematical input beyond the caller form: it constructs and caches `self.__local_representability_conditions` when absent or when `recompute_flag=True`, returns the condition object unless `silent_flag=True`, and otherwise returns `None` after cache population. The universality wrappers take either one place `p` or no additional input and return booleans by forcing the cache with `silent_flag=True` before delegating to `is_universal_at_prime(p)`, `is_universal_at_all_finite_primes()`, or `is_universal_at_all_places()`. The represented-number wrappers take a rational/integer value `m`, and optionally a finite prime or `infinity`, and return booleans by delegating to the condition object's place or all-place represented-number predicate. Migration consequence: preserve these as Sage interop evidence for existing `QuadraticForm` method names, but project vocabulary should expose the typed representation-condition object or predicate owner above, not duplicate both the wrapper and condition-object methods. `recompute_flag`, `silent_flag`, private cache state, and bound-method naming are runtime/API compatibility data, not public mathematical parameters. |
| `cholesky_decomposition(bit_prec=53)`, `vectors_by_length(bound)`, `complementary_subform_to_vector(v)`, `split_local_cover()` | `quadratic_form__split_local_covering.py:21`, `111`, `261`, `356`; Sage docs `quadratic_form.html` | analytic/vector enumeration and split-covering backend surfaces | These are `QuadraticForm` presentation-level search and transformation helpers, originally described by Sage as routines for split local covering in four variables but implemented on arbitrary caller dimension where the numeric steps make sense. `cholesky_decomposition(bit_prec=53)` takes only a real precision parameter, requires the caller form to be over `ZZ`, `QQ`, or a real field and rejects a requested precision above a real base field's precision, then returns an upper-triangular real matrix built from `1/2` times the coefficient matrix. `vectors_by_length(bound)` coerces `max(bound, 0)` to `ZZ`, uses RDF epsilon and the Cholesky matrix to enumerate raw integer coordinate vectors grouped in a list indexed by rounded represented value `0 <= Q(v) <= bound`, and raises if floating roundoff exceeds the source tolerance. `complementary_subform_to_vector(v)` takes a nonzero integer coordinate list of length `dim(Q)`, uses `extend_to_primitive` to complete the column to a square integer matrix whose determinant is the gcd of its minors, transforms the presentation, performs divisibility-driven symmetric cancellations against the first coordinate using `d = Q(v)`, raises if cancellation fails, and returns only the extracted `(n-1)`-variable `ZZ` quadratic sub-presentation. `split_local_cover()` has no public input beyond the caller form: it reuses private `__split_local_cover` either as a cached found `QuadraticForm` or as the last represented length already checked, enumerates vectors by increasing represented value while expanding the search bound in batches of five, builds `QuadraticForm(ZZ, 1, [d]) + complementary_subform_to_vector(v)` for each candidate value `d`, and returns the first split `ZZ` form whose local-representation-condition object equals the original caller's. Migration consequence: preserve these as backend evidence for positive-definite coordinate-vector enumeration, complementary-presentation construction, and split-cover search; public exposure requires typed vector-enumeration and split-cover witness objects that state the presentation, bound or chosen vector, primitive/saturation status of the completed matrix, basis-change/isometry data or its absence, divisibility/cancellation hypotheses, local-condition equality criterion, and returned split form. Do not expose floating precision, RDF tolerance, cache state, raw coordinate lists, or the missing transformation witness as ordinary lattice API. |
| `is_globally_equivalent_to(other, return_matrix=False)`, `is_locally_equivalent_to(other, check_primes_only=False, force_jordan_equivalence_test=False)`, `has_equivalent_Jordan_decomposition_at_prime(other, p)`, `is_rationally_isometric(other, return_matrix=False)`, `_diagonal_isometry(V, W)`, and `_gram_schmidt(m, fixed_vector_index, inner_product)` | `quadratic_form__equivalence_testing.py:25`, `119`, `178`, `304`, `553`, `654`; Sage docs `quadratic_form.html` | integral equivalence, local genus-equivalence, rational isometry, and backend transformation-witness surfaces | `is_globally_equivalent_to` compares a bound caller with another Sage `QuadraticForm`; source rejects non-forms and non-definite callers, and the written docs/examples are positive-definite integral `ZZ` forms. It calls PARI `qfisom` on `other` against `self`, returns a boolean by default, and with `return_matrix=True` returns either `False` or a transformation matrix `M` satisfying `self(M) == other`. Public admission belongs to a definite integral formed-object isometry/equivalence surface with an optional explicit isometry witness; it is not generic equality and `return_matrix` is a backend witness selector. `is_locally_equivalent_to` is a regular genus/local-equivalence predicate over the real place and the relevant finite `\ZZ_p` places: source raises on degenerate determinant, rejects dimension or base-ring mismatch, rejects determinant, level, or signature mismatch, compares Conway-Sloane genus-symbol lists for `ZZ` forms by default, and otherwise applies the Jordan/O'Meara prime test at primes dividing `2*det`. Degenerate local equivalence is therefore an explicit missing local-equivalence theorem/implementation obligation rather than an admitted fallback. `check_primes_only` is unused Sage 10.7 compatibility data, and `force_jordan_equivalence_test` is a backend algorithm selector, not public API. `has_equivalent_Jordan_decomposition_at_prime(other, p)` is the single-prime `p`-adic Jordan-decomposition comparison after same-type and prime checks: for odd `p` it compares scale, dimension, and determinant squareclass; for `p = 2` it compares scale, dimension, norm valuations, determinant chains, Hasse invariants, and O'Meara conditions. Public ownership belongs to a typed local-isometry or genus-equivalence witness at a place, with the prime/place and local decomposition data explicit. `is_rationally_isometric` is the regular quadratic-form isometry predicate over `\QQ` or a number field: source rejects singular forms, requires equal base ring and dimension, requires determinant ratio squareclass, compares Hasse invariants at determinant-support primes, compares ordinary signatures over `\QQ`, and compares positive-entry counts at every real embedding over a number field. With `return_matrix=True`, Sage diagonalizes both forms, calls `_diagonal_isometry`, and returns the composed rational transformation witness. `_diagonal_isometry(V, W)` is a backend for diagonal rationally equivalent forms that builds a matrix `T` with `V.Gram_matrix() == T.T * W.Gram_matrix() * T`, using `solve` and iterative basis replacement; `_gram_schmidt(m, fixed_vector_index, inner_product)` orthogonalizes matrix columns from the fixed index onward. These helpers do not become public methods; expose only the typed isometry/equivalence surface and any returned witness. |
| `qfsolve(G)`, `qfparam(G, sol)`, `solve(c=0)` | `qfsolve.py:37`, `84`, `124`; Sage docs `qfsolve.html` | rational quadratic-equation solve, obstruction, and parametrization backend surfaces | `qfsolve(G)` is the PARI-backed homogeneous equation solver for a square Gram matrix `G` over `\QQ`, with equation `x G x^t = 0`. Sage 10.8 docs expose PARI's raw return union: integer obstruction, vector solution, or matrix for a totally isotropic subspace. The installed Sage 10.7 wrapper normalizes only PARI column output to a `QQ` vector and otherwise coerces the result to `ZZ`, so documented matrix output for degenerate totally isotropic cases is a version/source mismatch that project API must not inherit. The integer obstruction convention is source-backed: `-1` means real obstruction and a prime `p` means obstruction over `\QQ_p`. `qfparam(G, sol)` is the ternary/conic parametrization branch: input is a `3 x 3` matrix over `\QQ` plus a rational solution triple for `x G x^t = 0`, and the return object is a rank-3 vector over `\QQ[t]` parametrizing all conic solutions up to scaling. Bound `solve(c=0)` has caller `self` a `QuadraticForm` over `\QQ` and input `c` a rational value: for `c = 0` it delegates to `qfsolve(self.Gram_matrix())` and returns the solution vector or raises an obstruction; for `c != 0` it solves the augmented homogeneous Gram matrix with final diagonal entry `-c`, then either divides by the auxiliary coordinate `z` or, when `z = 0`, chooses a unit vector `e` with `B(x,e) != 0` and returns `e + ((c - Q(e))/(2B(x,e))) x`. Public admission belongs to a typed rational representation-problem surface naming the formed presentation, represented value, homogeneous versus inhomogeneous equation, solution vector, local-obstruction witness, and optional conic-parametrization witness. Do not expose the raw PARI return union, Sage exception text, installed-version normalization mismatch, or these routines as lattice/form constructors. |
| `list_external_initializations()` and `is_QuadraticForm(Q)` | `quadratic_form.py:49`, `676` | audit/deprecated interop surfaces | `list_external_initializations` is unsafe-initialization audit metadata, and `is_QuadraticForm` is a deprecated type predicate. Neither becomes project mathematical vocabulary; use category containment and mapped constructor/promotion paths instead. |
| `direct_sum`, `tensor_product`, `twist` on Sage lattices | `free_quadratic_module_integer_symmetric.py:871`, `1332`, `1653` | `Free + Bilinear` or `ModulesWithForms` construction categories | Admit at the higher formed-module owners. `discard_basis` is not admitted as a public option; changing presentation must be explicit and returns a new object plus any available witness. |
| `FreeQuadraticModule_integer_symmetric._mul_(other, switch_sides=False)` | `free_quadratic_module_integer_symmetric.py:671` | selected-generator presentation change / subobject-span construction, not public lattice multiplication | Sage multiplies the basis matrix on the left or right by a scalar or matrix and returns `sublattice(...)` for integral data, otherwise `span(...)`. The project surface must expose this, if admitted, as a named presentation/subobject construction with inclusion or basis-change witness data; it must not overload ordinary lattice multiplication or hide the scalar-vs-matrix input shape behind a variadic public operator. |
| `sublattice`, `overlattice`, `maximal_overlattice` | `free_quadratic_module_integer_symmetric.py:972`, `1008`, `1030` | subobject/overlattice construction categories | Admit as lattice construction categories using generator-defined subobjects and inclusion morphisms. Do not expose row-basis or ambient-span wording as public semantics. `maximal_overlattice(p)` stays `OverZZ`/algorithmic and evenness-hypothesis guarded. |
| `local_modification(M, G, p, check=True)` | `free_quadratic_module_integer_symmetric.py:1708`; Sage docs `free_quadratic_module_integer_symmetric.html` | p-adic genus/local-modification backend algorithm surface | Preserve as an algorithm witness for local genus modification, not a core lattice constructor. Input data are a `\ZZ_p`-maximal integral lattice `M`, a Gram matrix `G` for a lattice locally isomorphic to `M` over `\QQ_p`, and a prime `p`; source sets `d = p^valuation_p(denominator(G^-1))`, constructs the `p`-maximal overlattice of `IntegralLattice(G)`, compares p-adic normal forms for that lattice and `M`, and returns the sublattice `((M.span(B) & M) + d*M)` inside the ambient space of `M`. With `check=True`, Sage asserts equality between the result's p-adic genus symbol and the p-adic symbol of `G`; the flag is backend verification, not a public option. Public admission requires a theorem-backed genus/local-modification surface naming the local-equivalence, maximality, ambient-space, and away-from-`p` equality hypotheses rather than exposing raw Gram-matrix surgery as ordinary lattice API. |
| `is_FreeQuadraticModule(M)` | `free_quadratic_module.py:232`; Sage docs `free_quadratic_module.html` | deprecated interop predicate | Do not admit as project vocabulary. Membership is expressed by category containment and refined parent categories; this Sage helper remains only compatibility evidence for backend type probes. |

### Inherited Module and Hom Surfaces

| Sage surface | Source evidence | Spec owner or classification | Reconciliation |
| --- | --- | --- | --- |
| `FGP_Module(V, W, check=True)`, `FGP_Module_class.__init__(V, W, check=True)`, `V`, `W`, `cover`, `relations`, `construction`, `quotient_map` | `fgp_module.py:233`, `293`, `339`, `837`, `875`, `1832`, `1957`; Sage docs `fgp_module.html` | `Modules(R).FinitelyPresented().OverPID()` quotient construction and presentation data | Preserve the Sage factory as the source-backed quotient `V/W` constructor from a free PID-module cover `V` and free submodule of relations `W`; `FGP_Module_class` is the implementation class it caches by cover and relation basis matrices. Public project ownership belongs to finitely presented PID-module quotient construction, not to lattice constructors. `check` is Sage validation/cache construction data. `V`, `W`, `cover`, `relations`, and `quotient_map` expose presentation and structure-map data for the quotient, while `construction()` is Sage construction-functor metadata returning `(QuotientModuleFunctor(W), V)` and remains presentation/runtime reconstruction data rather than a new mathematical constructor. |
| `FGP_Module_class.__truediv__`, `FGP_Module_class.__gt__`, `FGP_Module_class.__ge__`, `FGP_Module_class._relative_matrix` | `fgp_module.py:487`, `590`, `608`, `915` | finitely presented module quotient/subobject protocol and coordinate backend | Preserve through module quotient and subobject categories. Division by a submodule constructs a further quotient; comparisons express submodule containment; `_relative_matrix` is presentation-coordinate plumbing. None become lattice-owned option paths. |
| `linear_combination_of_smith_form_gens`, `_smith_form`, `invariants`, `gens`, `gen(i)`, `smith_form_gens`, `smith_form_gen(i)`, `gens_to_smith`, `smith_to_gens`, `coordinate_vector`, `gens_vector` | `fgp_module.py:661`, `940`, `977`, `1016`, `1347`, `1040`, `1379`, `1071`, `1120`, `1247`, `1185` | finitely presented PID modules; presentation-coordinate interop | Preserve as module presentation and backend-coordinate witnesses. `_smith_form()` returns Smith normal form matrices for the relative presentation matrix; `invariants()` records its diagonal classification data. `gens()` and `gen(i)` expose the current presentation generators, while `smith_form_gens()` and `smith_form_gen(i)` expose Smith-form generators. `linear_combination_of_smith_form_gens(c)` recovers the quotient element represented by the Smith-generator coordinate vector `c`; `gens_to_smith` and `smith_to_gens` are matrices between the user-generator and Smith-form presentations. Public lattice API should speak in elements, generators, morphisms, and quotient objects; indexed generator access and coordinate vectors are localized presentation interop. |
| `optimized()` | `fgp_module.py:1406` | finitely presented PID module presentation normalization | Representation split: this returns an isomorphic optimized quotient presentation plus the matrix witness relating coordinates to the optimized presentation. It must not become lattice-object equality or a hidden basis change; public normalization must return a new object with an explicit isomorphism witness. |
| `submodule`, `is_submodule`, `has_canonical_map_to`, `__contains__` | `fgp_module.py:678`, `707`, `767`, `798` | module containment, subobject, and structure-map surfaces | Preserve as subobject and parent-containment evidence. `has_canonical_map_to(A)` means the presentation data induce a natural map by inclusion of covers and relations; public subobjects must carry inclusion/structure morphisms, not optional ambient state on a lattice object. |
| `hom(im_gens, codomain=None, check=True)`, `FGP_Module_class._hom_general`, `FGP_Module_class._hom_from_smith`, and `_Hom_` | `fgp_module.py:1480`, `1608`, `1652`, `1690`; Sage docs `fgp_module.html` | Hom category constructors and backend realization | Do not admit `hom(images)` as the main public constructor. Preserve through `M.Hom(N)` and Hom-parent constructors `from_dict`, `from_images`, `from_callable`, and `from_matrix`. Source recovery formula: image data for the current generators define a candidate map; empty image data produce the zero map to `M` or the supplied codomain, free-module codomains are promoted to the quotient `V/0`, Smith-generator data are realized on the optimized quotient cover, and non-Smith current-generator data are first pulled through the free module on the current generators with a relation check `r(B) <= N.W()` before converting to Smith-generator images. `check` is backend validation of relation preservation, and `_hom_general` / `_hom_from_smith` are realization strategies, not additional public constructor families. |
| `annihilator()` | `fgp_module.py:1886` | finitely presented module/torsion-module invariant | Preserve as the ideal `Ann_R(M)` that kills the quotient module. For finite discriminant groups this is torsion-module data; it is not a lattice invariant unless applied to the actual discriminant-group object. |
| `ngens()` | `fgp_module.py:1920` | presented/equipped module generator-count surface | Preserve as the number of current presentation generators, not abstract finite-generation evidence. Minimal Smith generators are represented by `smith_form_gens()`; equipped generating-set categories own any public chosen-generator count. |
| `is_FGP_Module(x)` | `fgp_module.py:269`; Sage docs `fgp_module.html` | deprecated interop predicate | Do not admit as project vocabulary. Use category containment/refinement and public module categories; this remains backend compatibility evidence only. |
| `random_fgp_module(n, R=ZZ, finite=False)`, `random_fgp_morphism_0(*args, **kwds)`, and `_test_morphism_0(*args, **kwds)` / `test_morphism_0` | `fgp_module.py:1977`, `2026`, `2065`; Sage docs `fgp_module.html` | testing/sample-data helpers | Preserve as Sage test-helper evidence only. `random_fgp_module` chooses a random-rank submodule `A` of `(Frac(R))^n`, then a random submodule `B <= A`, returns the finitely presented quotient `A/B`, and only loops for finite output when `finite=True`; source signature default is `finite=False` even though the docstring text says default `True`. `random_fgp_morphism_0` forwards its variadic inputs to that helper, then returns an endomorphism sending each Smith-form generator to a random `ZZ` multiple of itself. `_test_morphism_0` samples that morphism and asserts kernel invariant divisibility, image submodule containment, and lift correctness for the first Smith-form image generator; `test_morphism_0` is a deprecated alias. These do not define constructors, random lattice APIs, statistical distributions, or proof data; any project fixture/sample surface must name the mathematical object, presentation category, base ring, finiteness condition, randomness source, and hypotheses explicitly. |
| `FGP_Homset`, `FGP_Homset_class.__init__`, `FGP_Homset_class.__call__`, `FGP_Homset_class._coerce_map_from_`, and `FGP_Morphism.__init__(parent, phi, check=True)` | Sage docs `fgp_morphism.html`; `fg_pid/fgp_morphism.py:73`, `462`, `499`, `517`, `537` | module Hom parent and Hom-element construction | In the category of modules over `R`, `Hom_R(M,N)` is the set of `R`-linear maps from `M` to `N`. For finitely presented quotients, a Hom element can be represented by a cover map that sends relations in the domain to relations in the codomain. Sage accepts that cover-map data, an existing FGP morphism in the same Hom parent, or for endomorphism parents a scalar coerced through the base ring into the cover map. The public project vocabulary should expose `Hom_R(M,N)` and named construction routes from generator images, a matrix/cover-map representative, or a callable on generators; `check`, optimized-cover maps, relation tests, Hom-parent equality coercion, and scalar endomorphism coercion are implementation and interoperability data. |
| `FGP_Morphism.__add__`, `__sub__`, and `__neg__` | `fg_pid/fgp_morphism.py:188`, `202`, `215`; Sage docs/source `fgp_morphism.html` | additive structure on a module Hom object | In an additive category, each `Hom(M,N)` is an abelian group. For modules, the sum, difference, and negative of two homomorphisms are computed pointwise. Sage implements this for FGP morphisms by combining the underlying cover morphisms after coercion into the same Hom parent. These are not lattice multiplication, not a new constructor family, and not a formed-lattice-specific operation. |
| `FGP_Morphism.im_gens()` | Sage docs; `fg_pid/fgp_morphism.py:137-152` | generator-image presentation of a module homomorphism | A homomorphism from a presented module is determined by the images of chosen generators, provided the relations are respected. For a morphism `f: M -> N`, Sage returns the tuple `(f(g_i))` for the current domain generators. This is generator-image presentation data for Hom construction/display, not a lattice-owned invariant. |
| `FGP_Morphism.__call__(x)` on elements and submodules | Sage docs; `fg_pid/fgp_morphism.py:226-297` | module Hom evaluation and direct image of a subobject | In a concrete category of modules, a homomorphism `f: M -> N` sends each element `x in M` to `f(x) in N`. In the category of modules, a subobject `A <= M` has a direct image `f(A) <= N`, equivalently `im(f|_A)`. Sage implements the submodule branch by requiring `A` be an FGP submodule of the domain and returning the codomain submodule generated by the images of `A.smith_form_gens()`. Public project exposure should name this as subobject direct image or restriction image, not as an optional argument on `image()` and not as a lattice-local method. |
| `FGP_Morphism.kernel()` and `inverse_image(A)` | Sage docs; `fg_pid/fgp_morphism.py:299-366` | module/abelian-category kernel and inverse-image subobjects; formed kernels inherit restricted form data | In an abelian category, the kernel of `f` is the subobject `f^{-1}(0)`. For a subobject `A <= codomain(f)`, the inverse image `f^{-1}(A)` is a subobject of the domain. Sage implements these constructions for FGP modules; formed-module kernels additionally inherit restricted form data. |
| `FGP_Morphism.image()` | Sage docs; `fg_pid/fgp_morphism.py:368-384` | module/abelian-category image subobject; formed images inherit restricted form data | In an abelian category, the image of `f` is a subobject of `codomain(f)`. Sage returns `im(f)` for FGP modules. For formed morphisms this supports the formed image object, but it is not a cokernel or quotient construction. |
| `FGP_Morphism.lift(x)` | Sage docs; `fg_pid/fgp_morphism.py:386-456` | partial preimage witness for a module homomorphism | For a morphism `f: M -> N`, a lift of `x in N` is an element `y in M` with `f(y)=x`. Such a lift exists exactly when `x` lies in the image of `f`; nonexistence is mathematical failure of the lift, not optional best-effort data. |
| Missing formed `cokernel()` | Sage docs `fgp_morphism.html`; installed source `fg_pid/fgp_morphism.py:1-548`; project `category_specs/forms/subcategories/with_forms.py:100-119`; `[[SPEC-MAPPING-FORMS]]`; `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]` | `Modules(R).WithForms().HomCategory().ElementMethods.cokernel` with underlying module quotient and descended form codomain | The required surface is `Q = coker(f) = codomain(f) / image(f)` plus projection. If the form codomain map is `h: S_M -> S_N`, first take `N_0 = coker(h)`, then quotient `N_0` by cross-term images `b_N(image(f), codomain(f))`; the descended form has codomain this quotient. Lattice discriminant descent specializes this to `L -> L^#`: the coefficient cokernel is `K/R`, cross terms `b(L,L^#)` vanish modulo `R` by the metric-dual definition, and the resulting bilinear/quadratic codomains are `K/R` and `K/2R`. The task-level semantic closure is complete; runtime construction remains outside this spec-workflow row. |
| `FGP_Morphism._repr_`, `FGP_Morphism._richcmp_` | `fg_pid/fgp_morphism.py:122`, `154` | Hom-element display and comparison protocol | Morphisms can be displayed and compared as elements of a Hom object once the Hom parent, domain, codomain, and defining data are fixed. Sage's display and comparison protocols remain ordinary module/Hom interop. They are not lattice-specific methods and must not be copied into `Lattices`. |
| `FGP_Element.__init__`, `lift`, `__neg__`, `_add_`, `_sub_`, `_rmul_`, `_lmul_`, `additive_order` | `fg_pid/fgp_element.py:58`, `85`, `125`, `139`, `173`, `192`, `237`, `414`; Sage docs `fgp_element.html` | finitely presented PID quotient-element construction, additive group law, scalar action, cover lift, and element order | In a quotient module `V/W`, an element is a coset represented by an element of the cover `V`. Addition, negation, subtraction, and scalar multiplication are inherited from the module structure on the quotient. Sage stores a cover representative `_x in V`; `check=True` asserts `_x in parent.V()`. `lift()` returns that stored representative, so a discriminant-group lift is public only when the parent quotient is explicitly identified as `L^#/L` and the cover `V` is the chosen dual/rational lattice presentation. For a finitely presented module over a PID, additive order is computed from Smith invariant coordinates; nonzero coordinates in zero-invariant slots have infinite order, otherwise the order is the lcm of the modular coordinate orders. |
| `FGP_Element._repr_`, `__getitem__`, `vector`, `__hash__`, `_vector_(base_ring=None)`, `_richcmp_` | `fg_pid/fgp_element.py:282`, `294`, `312`, `337`, `354`, `392`; Sage docs `fgp_element.html` | reduced-coordinate display, indexing, hashing, conversion, and comparison protocol | In a finitely presented PID quotient, a chosen Smith presentation gives reduced coordinates for quotient elements. Sage's `vector()` caches `parent().coordinate_vector(self, reduce=True)` as an immutable reduced coordinate vector. Display, coordinate indexing, hashing, vector conversion, and rich comparison are derived from that reduced vector. Preserve coordinate-vector recovery only as quotient-presentation interop. It is not a lattice vector, basis-free invariant, or formed-lattice morphism surface unless an explicit quotient presentation supplies the mathematical identification. |
| `cardinality`, `is_finite`, `list`, `__iter__`, `random_element` | `fg_pid/fgp_module.py:1733`, `1755`, `1788`, `1800`, `1867`; Sage docs `fgp_module.html` | finite-carrier module/set enumeration and runtime | A finitely presented module over a PID is finite exactly when all Smith invariants are nonzero, and then its cardinality is the product of those invariants. Sage returns `+Infinity` when a Smith invariant is `0`; otherwise `cardinality()` is the product of invariants. `__iter__()` is implemented only for finite quotients over `ZZ`, enumerating product ranges for Smith invariants through the optimized cover basis; `list()` materializes that iterator. `random_element(*args, **kwds)` delegates randomness to the cover module `V` and coerces/reduces the sampled cover element into `V/W`. These are not lattice-owned methods; discriminant groups and torsion formed modules inherit the finite-carrier/enumerated-set behavior from their underlying finitely presented PID-module. |
| `Hom(X, Y, category=None, check=True)`, `End(X, category=None)`, `end(X, f)`, `Homset`, `HomsetWithBase`, `is_Homset(x)`, and `is_Endset(x)` | `homset.py:87`, `498`, `562`, `580`, `1267`, `1291`, `1316`; Sage docs `homset.html` | generic Hom and End objects in a category; runtime predicates remain interop | In any category, `Hom(X,Y)` is the collection of morphisms from `X` to `Y`, and `End(X)=Hom(X,X)` is the collection of endomorphisms of `X`. Sage's `Hom` constructs the cached Hom parent in the selected category; `End` is the `Hom(X,X)` specialization; `end(X,f)` is only the convenience element route `End(X)(f)` for endomorphism data `f`. `Homset` and `HomsetWithBase` store domain, codomain, category, base-ring data, and dynamic element-class wiring. Deprecated predicates remain interop probes only. Lattice Hom/End/Aut specs inherit this generic story rather than copying constructors, convenience calls, or predicates into lattice methods. |
| `HomsetsCategory`, `HomsetsCategory.default_super_categories`, `HomsetsCategory._test_homsets_category`, `HomsetsCategory._make_named_class_key`, `HomsetsOf`, `HomsetsOf._repr_object_names`, `Homsets`, `Homsets.SubcategoryMethods.Endset`, and `Homsets.ParentMethods.is_endomorphism_set` | `homsets.py:19`, `24`, `123`, `158`, `175`, `201`, `239`, `285`, `345`; Sage docs/source `homsets.py` | generic category of Hom objects, End axiom routing, and display/runtime support | In any category, Hom objects vary with their domain and codomain; the endomorphism case is the diagonal case `X=Y`. Sage's `HomsetsCategory` computes category-level Homset refinements; `HomsetsOf` is the stub category for base categories without their own Homsets class; `HomsetsOf._repr_object_names()` is display naming for that stub category; `Homsets().Endset()` records the endomorphism-set axiom and monoid refinement; `is_endomorphism_set()` checks domain identity at the Hom parent. Lattice Hom/End/Aut specs add only lattice-specific Aut refinements. |
| `FreeModuleHomspace`, `is_FreeModuleHomspace(x)`, `FreeModuleHomspace.__call__(A, **kwds)`, `FreeModuleHomspace.zero(side='left')`, `FreeModuleHomspace._matrix_space(side='left')`, `FreeModuleHomspace.basis(side='left')`, and `FreeModuleHomspace.identity(side='left')` | `free_module_homspace.py:85`, `132`, `133`, `230`, `276`, `303`, `342`; Sage docs `free_module_homspace.html` | Hom object for finite free modules, with additive and presentation data | In any category, `Hom(X,Y)` is the set of morphisms from `X` to `Y`; in an additive category, `Hom(X,Y)` is an abelian group; and `End(X)=Hom(X,X)` contains the identity. Sage realizes this for finite free modules by accepting exactly three Hom-element input shapes: matrix data, images of the domain generators, or a callable evaluated on the domain generators. In the generator-image and callable branches, `side='right'` transposes the coordinate matrix; nonzero maps require a base-ring coercion from the domain to the codomain, while zero maps are accepted even without such coercion. `zero(side)` is the zero morphism in the additive Hom group, `identity(side)` exists only for endomorphisms, and `_matrix_space(side)`, `basis(side)`, and `side` are finite-free presentation data. `is_FreeModuleHomspace` is deprecated. |
| `is_MatrixMorphism`, `MatrixMorphism_abstract.__init__`, `MatrixMorphism.__init__`, `MatrixMorphism_abstract._richcmp_`, `MatrixMorphism._repr_`, and `MatrixMorphism_abstract._call_with_args` | `matrix_morphism.py:59`, `96`, `121`, `229`, `1560`, `1726`; Sage docs/source `matrix_morphism.py` | deprecated predicate, finite-free matrix-presentation constructor, display/comparison, and call-protocol interop | A morphism between finite free modules may be represented by a matrix after bases or finite free presentations are chosen. Sage's matrix-morphism constructors validate Hom parent, side convention, matrix shape, and immutability of the representative; these are presentation and backend requirements, not category owners. The deprecated predicate remains compatibility only. Display, matrix comparison, and `_call_with_args` are protocol surfaces; forwarded optional arguments are codomain element-constructor data after evaluation and must not become public Hom option bags. |
| `FreeModuleMorphism.__init__`, inherited `MatrixMorphism._call_`, composition through multiplication, domain/codomain behavior, and function equality | `free_module_morphism.py:76`; inherited `matrix_morphism.py:149`, `261`, `292`, `312`, `483`, `1231`; Sage docs `free_module_morphism.html` and `matrix_morphism.py` | category morphism axioms, with concrete-function comparison only for concrete categories | In any category, morphisms have domains and codomains and compose. In a concrete category, a morphism can be evaluated on elements of its domain. Sage's `FreeModuleMorphism` and inherited `MatrixMorphism` methods are matrix-backed implementations of those facts for finite free modules. Public project exposure keeps evaluation and composition on the generic Hom/morphism surface; it does not route them through module Hom merely because this Sage implementation block contains them. |
| `FreeModuleMorphism.__add__`, `__neg__`, `__sub__`, scalar multiplication, `is_zero`, and Homspace `zero(side)` | `free_module_homspace.py:230`; inherited `matrix_morphism.py:466`, `483`, `653`, `735`, `751`, `1189`; Sage docs `free_module_homspace.html` and `matrix_morphism.py` | additive category for zero/additive morphism structure; linear category for scalar multiplication | In an additive category, each `Hom(X,Y)` is an abelian group and composition is bilinear; the zero morphism is the zero element of that group. In an `R`-linear category, each `Hom(X,Y)` is an `R`-module and composition is `R`-bilinear. Sage implements these facts for finite free modules by matrix addition, negation, subtraction, scalar multiplication, and the zero matrix. Lattice specs inherit these methods from the additive or linear Hom structure rather than adding lattice-local arithmetic methods. |
| `FreeModuleMorphism.inverse`, `is_bijective`, `is_identity`, and isomorphism/invertibility predicates | inherited `matrix_morphism.py:483`, `1071`, `1115`; project `category_specs/homsets/endsets.py`; `category_specs/homsets/autsets.py`; Sage docs `matrix_morphism.py` | End/Aut and isomorphism refinements of the ambient category | `End(X)=Hom(X,X)` contains the identity endomorphism. `Aut(X)` is the group of invertible endomorphisms of `X`, equivalently the isomorphisms `X -> X`. Sage tests these facts for finite free modules with matrix criteria, but the owner is the End/Aut or isomorphism structure, not the free-module implementation class. |
| `FreeModuleMorphism.kernel`, `image`, `inverse_image`, `pushforward`, `lift`/`preimage_representative`, `is_injective`, and `is_surjective` | inherited `matrix_morphism.py:863`, `913`, `977`, `1025`, `1040`, `1322`, `1371`, `1454`, `1649`, `1674`; project `category_specs/modules/homsets.py`; `category_specs/forms/subcategories/with_forms.py`; Sage docs `matrix_morphism.py` | abelian-category kernels, images, cokernels/coimages, mono/epi predicates, realized by module/free-module subobjects in this source | In an abelian category, kernels and cokernels exist, images and coimages are defined, monomorphisms are detected by zero kernels, and epimorphisms are detected by zero cokernels. Sage implements the finite-free-module case with matrix kernels, row or column spaces, submodule images, and linear or Hermite preimage witnesses. Module Hom and formed-module Hom refine the returned subobjects and form data. Lattice admission requires the formed/lattice subobject theorem, not a copy of free-module matrix methods. |
| `matrix(side=None)`, `side`, `side_switch`, rank/nullity of the representative matrix, restriction algorithms, and `change_ring(R)` | inherited `matrix_morphism.py:198`, `247`, `396`, `1025`, `1040`, `1322`, `1371`, `1454`, `1596`; Sage docs `matrix_morphism.py` | finite free presentation and matrix backend, not categorical ownership | A morphism has a matrix only after choosing finite free presentations or bases for its domain and codomain. Sage's `side`, `matrix(side)`, and `side_switch` record the orientation of that matrix. Rank and nullity are invariants of the chosen matrix representative. Restrictions, submodule algorithms, and `change_ring(R)` use the chosen presentation. None of these creates a lattice-local morphism constructor or a generic category method; formed/lattice use requires a theorem that the presentation operation preserves the extra structure. |
| `FreeModuleMorphism.eigenvalues`, `eigenvectors`, `eigenspaces`, inherited `MatrixMorphism.decomposition`, `characteristic_polynomial`/`charpoly`, `determinant`/`det`, `fcp`, and `trace` | `free_module_morphism.py:489`, `524`, `582`; inherited `matrix_morphism.py:91-94`, `818`; Sage docs `free_module_morphism.html` and `matrix_morphism.py` | endomorphisms in the category of finite-dimensional vector spaces over a field; matrix presentations of finite free modules when separately hypothesized | In the category of finite-dimensional vector spaces over a field, an endomorphism has characteristic polynomial, determinant, trace, eigenvalues after scalar extension when necessary, eigenspaces, and invariant subspace decompositions under the usual hypotheses. Sage delegates to matrix spectral/decomposition routines, with `side='right'` selecting right eigenvectors and `side='left'` selecting left eigenvectors before converting results back to domain subspaces. If a finite free module over a non-field is used as matrix presentation data, the row must state the module category, chosen finite free presentation, and any scalar extension to a field. These are not lattice automorphisms, isometries, or generic formed-module operations without a theorem connecting the endomorphism to the formed object and chosen presentation. |
| `BaseIsomorphism1D`, `BaseIsomorphism1D_to_FM`, and `BaseIsomorphism1D_from_FM` | `free_module_morphism.py:647`, `709`, `766`; Sage docs `free_module_morphism.html` | rank-one free-module/ring isomorphism witnesses | A chosen basis of a free rank-one `R`-module identifies that module with `R`; changing the basis changes the identification by a unit of `R`. Sage's ring `free_module` route creates the free rank-one module over a ring and two inverse isomorphisms between the ring and that module. Optional `basis` data multiply or divide by a unit of the ring; the source examples reject nonunit basis choices. These are module/ring equivalence witnesses and coercion-style maps, not lattice constructors, not Hom/End/Aut lattice surfaces, and not evidence that an arbitrary rank-one formed module is canonically identified with its scalar ring without a selected unit basis. |

### Toric Character-Lattice Boundary Correction

Sage's `ToricLattice` source is a warning against both over-narrowing the word
"lattice" to later Coble/Nikulin algorithms and erasing the formed structure carried by
presented torus character lattices. It subclasses Sage PID free-module classes and adds
named parent identity, dual-name bookkeeping, conversion barriers, and toric notation.
For a presented coordinate torus, the coordinate characters give a selected basis and
the identity Gram matrix gives a unimodular lattice. This does not create a separate
toric owner; it routes Sage toric lattice surfaces through ordinary free-module,
basis, and formed-lattice owners.

| Sage toric surface | Source evidence | Correct owner or classification | Reconciliation |
| --- | --- | --- | --- |
| `ToricLatticeFactory` as a `UniqueFactory` subclass, `ToricLatticeFactory.create_key(rank, name=None, dual_name=None, latex_name=None, latex_dual_name=None)`, `ToricLatticeFactory.create_object(version, key)`, and global factory instance `ToricLattice = ToricLatticeFactory("ToricLattice")` | `toric_lattice.py:238`, `322`, `362`, `381`; Sage toric lattice docs; Sage `UniqueFactory` docs | finite-rank free abelian module with selected basis; unimodular formed-lattice surface only when the coordinate-character presentation supplies the identity Gram form | Constructor evidence that named free abelian lattices must preserve parent identity and notation. The exact Sage input data are a nonnegative rank plus four naming/display fields; `create_key` normalizes them to `(rank, name, dual_name, latex_name, latex_dual_name)`, using default `N/M` toric names or a generated dual name, and `create_object` returns `ToricLattice_ambient(*key)`. `UniqueFactory` supplies cache/pickle infrastructure only; it is not a mathematical construction category and is not inherited by the returned lattice objects. Toric provenance alone is not a mathematical axiom, but the coordinate-character presentation supplies real basis/form data. Migration consequence: expose any admitted toric character-lattice constructor through the owning category surface with named input data; keep `UniqueFactory`, `create_key`, `create_object`, and the factory-instance assignment as Sage backend machinery, not public project constructors or lattice methods. |
| Deprecated toric predicates `is_ToricLattice`, `is_ToricLatticeQuotient`, `is_ToricLatticeElement` | `toric_lattice.py:166`, `199`; `toric_lattice_element.pyx:106`; Sage toric lattice docs | deprecated interop predicates | Do not admit as project vocabulary. Use category containment and exact parent/element classes at backend boundaries; these functions remain compatibility evidence only. |
| `ToricLattice_generic.__call__(*args, **kwds)` and `__contains__(point)` | `toric_lattice.py:401`, `486` | parent element constructor/coercion and runtime membership | Preserve only as Sage parent interop. Public project constructors should accept an explicit element-coordinate or generator presentation under the owning category constructor route; variadic coordinate shortcuts and membership-by-coercion are not mathematical method owners. |
| `ToricLattice_generic.construction()` | `toric_lattice.py:527` | Sage construction-functor interop guard | Preserve as negative backend evidence only: Sage returns `None` to prevent construction-functor arithmetic between different named toric lattices. This does not become a project construction category. |
| `ToricLattice_ambient.dual()` and `ToricLatticeElement._act_on_` | `toric_lattice.py:1013`; `toric_lattice_element.pyx:225`; Sage toric lattice docs | module dual object, metric `dual_lattice()` for the identity form, and their canonical unimodular identification | The module dual `Hom_ZZ(L, ZZ)` and the metric dual `L^#` coincide for the identity Gram form. Sage's dual parent and dual element action are implementation evidence for the compatibility, even though Sage exposes them through toric notation. For arbitrary formed lattices, the module dual and metric dual must be kept distinct unless an isomorphism is explicitly recorded. |
| `ToricLattice_sublattice_with_basis.dual()` and `ToricLattice_quotient.dual()` | `toric_lattice.py:1152`, `1666` | module-dual quotient/subobject construction, not a new toric lattice axiom | A saturated sublattice dual is represented as a quotient of the ambient dual by the integer kernel of the basis matrix; a quotient dual is represented as a sublattice of the ambient dual. Recover through ordinary Hom-dual, quotient, and subobject constructions with explicit saturation/kernel hypotheses. |
| `span`, `span_of_basis`, `intersection`, `saturation`, `quotient`, `direct_sum`, `ToricLattice_sublattice_with_basis`, `ToricLattice_sublattice`, `ToricLattice_quotient`, `ToricLattice_quotient._module_constructor` | `toric_lattice.py:544`, `601`, `643`, `743`, `764`, `808`, `1070`, `1211`, `1338`, `1589` | inherited module subobject, selected-basis subobject, quotient, saturation, and direct-sum surfaces | Sage preserves toric-flavored parent classes for usability, but the method owners are ordinary module/subobject/quotient/basis owners. `ToricLattice_sublattice_with_basis` is the selected-basis subobject realization, `ToricLattice_sublattice` is the generated-subobject realization, and `ToricLattice_quotient` is the finitely presented quotient realization; these implementation strata do not create new toric lattice axioms or constructors. The `positive_point` and `positive_dual_point` quotient arguments are mutually exclusive orientation witnesses only for one-generator torsion-free quotients: the source checks the chosen point or dual point, rejects zero orientation data, and stores only whether the selected generator must be sign-flipped. Public API should expose this, if at all, as an oriented quotient-generator witness attached to the quotient construction, not as broad lattice option bags. |
| `ToricLattice_quotient.gens`, `ToricLattice_quotient._element_constructor_`, `ToricLattice_quotient.base_extend`, `ToricLattice_quotient.is_torsion_free`, `ToricLattice_quotient.rank`/`ToricLattice_quotient.dimension`, `ToricLattice_quotient.coordinate_vector` | `toric_lattice.py:1467`, `1493`, `1613`, `1646`, `1686`, `1710`, `1712` | finitely presented PID-module quotient presentation and runtime surfaces | Preserve through the FGP/module quotient layer. `gens()` applies the stored quotient-generator sign choice; `is_torsion_free()` is the Smith-invariant test `sum(invariants()) == 0`; `rank()` is `rank(V)-rank(W)`; `base_extend(ZZ)` is identity and `base_extend(QQ)` is vector-space quotient interop. Other base rings hit a Sage implementation gap, so no public project base-extension overload is admitted without a separate module/base-change source obligation. Generator sign adjustment and coordinate vectors are presentation data, not abstract lattice identity. The quotient element constructor accepts compatible lattice, sublattice, quotient, coordinate, vector, or Smith-generator-coordinate data as interop routes to the same quotient element object; the public mathematical surface is the quotient object with elements and projection map, not a variadic project constructor. |
| `ToricLattice_ambient._sage_input_`, `ToricLattice_ambient.__richcmp__`, `ToricLattice_ambient._latex_`, `ToricLattice_ambient._repr_`, `ToricLattice_ambient.ambient_module`, `ToricLattice_ambient.plot`, `ToricLattice_sublattice_with_basis._repr_`, `ToricLattice_sublattice_with_basis._latex_`, `ToricLattice_sublattice_with_basis.plot`, `ToricLattice_quotient_element`, `ToricLattice_quotient_element._latex_`, `ToricLattice_quotient_element._repr_`, `ToricLattice_quotient_element.set_immutable`, `ToricLattice_quotient._latex_`, `ToricLattice_quotient._repr_`, `ToricLatticeElement.__richcmp__`, `ToricLatticeElement.__hash__`, `ToricLatticeElement._dot_product_`, `ToricLatticeElement._latex_`, `ToricLatticeElement._repr_`, `ToricLatticeElement.__reduce__`, `ToricLatticeElement.plot`, `unpickle_v1`, and Cython protocol names `__richcmp__`, `_dot_product_`, `__reduce__` | `toric_lattice.py:900`, `923`, `964`, `978`, `992`, `1044`, `1111`, `1131`, `1174`, `1253`, `1286`, `1302`, `1318`, `1543`, `1563`; `toric_lattice_element.pyx:167`, `205`, `300`, `328`, `344`, `360`, `373`, `397` | runtime, display, serialization, hashing, comparison, and plotting interop | Do not admit as mathematical lattice vocabulary. Parent names, dual-name labels, and LaTeX names are presentation/display data; `_sage_input_`, rich comparison, hashing, pickle helpers, immutable no-ops, and plotting are backend/display or serialization surfaces. `ToricLatticeElement._dot_product_` is a deliberate same-lattice multiplication rejection in Sage's toric dual-pair convention, not a negative mathematical claim for identity-formed project lattices. |
| same-lattice element dot product rejection | `toric_lattice_element.pyx:300`; Sage toric lattice docs | Sage implementation/interop limitation relative to the identity-formed project surface | Sage's refusal to multiply two elements of the same toric lattice reflects its toric dual-pair convention, not a negative mathematical claim for the project spec. In the presented coordinate-character lattice, the identity Gram form supplies the same-lattice bilinear form. |

### Order-Theoretic Lattice-Poset Name Collision

Sage's category-level `LatticePosets()` and `FiniteLatticePosets()` use "lattice" in
the order-theoretic sense: a partially ordered set with binary meet and join. These
surfaces touch the algebraic lattice subtree only as a terminology collision.

| Sage lattice-poset surface | Source evidence | Spec owner or classification | Reconciliation |
| --- | --- | --- | --- |
| `LatticePosets()`, `super_categories()`, `Finite` | `lattice_posets.py:19`, `43`, `55`; Sage lattice-posets docs | `Posets()` / order-theoretic lattice-poset categories, not algebraic `Lattices(R)` | Do not admit these as algebraic lattice surfaces. Objects are posets whose binary supremum and infimum exist; they carry no module, scalar ring, bilinear form, metric dual, or discriminant data. Any project treatment belongs in the posets subtree. |
| `meet(x,y)` and `join(x,y)` | `lattice_posets.py:60`, `76`; Sage lattice-posets docs | order-theoretic poset operations | These are greatest-lower-bound and least-upper-bound operations in a partial order. They are unrelated to lattice-theory bilinear pairings, joins of sublattices, or overlattice constructions unless a separate order object on a poset of subobjects is explicitly constructed. |
| `FiniteLatticePosets()` irreducible-element and morphism helpers, including `FiniteLatticePosets.ParentMethods.join_irreducibles_poset`, `FiniteLatticePosets.ParentMethods.meet_irreducibles_poset`, and `FiniteLatticePosets.ParentMethods.irreducibles_poset` | `finite_lattice_posets.py:15`, `43`, `65`, `86`, `108`, `129`, `172`; Sage finite-lattice-posets docs | finite poset/lattice-poset combinatorics | These are order-theoretic surfaces on finite lattice posets. Source computes join-irreducibles as elements with exactly one lower cover, meet-irreducibles as elements with exactly one upper cover, returns subposets on those selected elements, and builds `irreducibles_poset()` from their union with a one-element special case. `is_lattice_morphism(f,codomain)` is a finite order-lattice morphism predicate: it iterates two-element subsets and requires both `f(join(x,y)) = codomain.join(f(x),f(y))` and `f(meet(x,y)) = codomain.meet(f(x),f(y))`. They migrate to posets if needed; they do not define algebraic lattice element primitivity, discriminant data, Hom/End/Aut, or lattice isometry. |

### Lattice and Form Method Reconciliation

| Sage surface | Source evidence | Spec owner or classification | Reconciliation |
| --- | --- | --- | --- |
| `ambient_module`, `basis_matrix`, `inner_product_matrix`, `degree`, `FreeQuadraticModule_ambient._repr_`, `FreeQuadraticModule_ambient._latex_`, `FreeQuadraticModule_ambient_domain._repr_`, `FreeQuadraticModule_ambient_pid._repr_`, `FreeQuadraticModule_ambient_field._repr_`, `FreeQuadraticModule_submodule_with_basis_pid._repr_`, `FreeQuadraticModule_submodule_with_basis_pid._latex_`, `FreeQuadraticModule_submodule_pid._repr_`, `FreeQuadraticModule_submodule_with_basis_field._repr_`, `FreeQuadraticModule_submodule_field._repr_` | `free_quadratic_module.py:369`, `472`, `846`, `880`, `964`, `1074`, `1169`, `1293`, `1337`, `1441`, `1550`, `1660`; `free_quadratic_module_integer_symmetric.py:704` | presentation/ambient-cover surface plus display/runtime interop | Representation split: `ambient_module()` returns the ambient free bilinear module used by the selected submodule or presentation; for the ambient case it is rebuilt from `base_ring`, `degree`, and `inner_product_matrix`. `inner_product_matrix()` is the ambient form matrix and may have degree greater than the caller's rank; `gram_matrix()` is the restricted selected-basis matrix `B*A*B^T`. Public lattice semantics should expose a typed ambient/cover or inclusion relation only when that presentation object is part of the data; otherwise public objects expose generators, form evaluations, and morphisms. `basis_matrix`, `degree`, display hooks, storage behavior, and direct-class `_repr_`/`_latex_` protocols remain backend/display data. Sage's ambient LaTeX hook at `free_quadratic_module.py:880` currently raises `NotImplementedError`, which is a display-backend gap, not a missing mathematical lattice operation. |
| `FreeQuadraticModule_ambient_domain.ambient_vector_space()` and `FreeQuadraticModule_submodule_with_basis_pid.change_ring(R)` | `free_quadratic_module.py:1013`, `1351`; Sage docs `free_quadratic_module.html` | scalar-extension/base-change construction for free bilinear modules over domains/PIDs | Recover as a formed-module base-change construction with explicit source ring, target PID or fraction field, coerced basis data, and preserved inner-product matrix. `FreeQuadraticModule_ambient_domain.ambient_vector_space()` tensors the ambient domain object with its fraction field and returns the corresponding field-valued free bilinear module. `FreeQuadraticModule_submodule_with_basis_pid.change_ring(R)` coerces selected-basis vectors into the fraction field of `R`, builds the target formed free module over `R`, and preserves selected-basis structure when present. Sage's ambient-vector-space convention is not public lattice identity; the return object/codomain is the scalar-extended free bilinear module, with lattice promotion only after integral, symmetric, nondegenerate, finite-rank free hypotheses are witnessed. |
| `gram_matrix`, `determinant`, `discriminant` | `free_quadratic_module.py:390`, `408`, `439` | `Free + Bilinear` | Admit at the first free bilinear tier. Gram matrices are presentation data in selected generators, not identity of an abstract isometry class. |
| `FreeQuadraticModule_integer_symmetric._mul_(other, switch_sides=False)` | `free_quadratic_module_integer_symmetric.py:671`; Sage docs/source `free_quadratic_module_integer_symmetric.html` | representation split: selected-generator presentation change, not lattice multiplication | Do not admit as a public `*` operation on lattice objects. Source multiplies the selected basis matrix by a scalar or matrix on the left or right, then returns `sublattice(B.rows())` when `other` is integral and `span(B.rows())` otherwise. Migration consequence: public project code must spell the intended construction as a subobject/span or presentation-change operation with an explicit basis-change or inclusion witness; it must not overload lattice multiplication to mean generator scaling or matrix action. |
| `is_even`, `dual_lattice`, `discriminant_group(s=0)` | `free_quadratic_module_integer_symmetric.py:736`, `753`, `779` | finite-rank free integral bilinear modules; `dual_lattice` and `discriminant_group` require nondegeneracy for the metric-dual identification and finite quotient | Admit, but the project implementation must route `discriminant_group()` through `L -> L^# -> coker`, preserving quotient-valued form codomains. The Sage selector `s` is source-backed input data for the `s`-primary component of `L^#/L`: `s=0` returns the whole discriminant group, while `s != 0` routes through the discriminant group's primary-component/subobject construction rather than defining a separate lattice method. General Hom-duality remains a module dual-object surface, not `dual_lattice()`, unless a specific isomorphism with the metric dual is recorded. |
| `signature_pair`, Sage `signature()` as `n_+ - n_-` | `free_quadratic_module_integer_symmetric.py:839`, `855` | exact free symmetric form signature data over a base with an ordered real realization; display/index interop for `p-q` | Preserve exact signature data. The scalar `p-q` is Sage interop/display data, not the owner of signature semantics. `[[DECISION-ORDERED-REAL-SIGNATURE-OWNER]]` admits the selected ordered-real-realization owner; a bare integral-domain hypothesis is not enough. |
| `orthogonal_complement`, `orthogonal_submodule_to`, element `perp` | `free_quadratic_module_integer_symmetric.py:931`; `torsion_quadratic_module.py:890` | symmetric bilinear modules and subobjects | Admit at the symmetric bilinear owner. Inputs must be typed subobjects or elements with parent data, not arbitrary ambient vectors. For torsion quadratic modules, `orthogonal_submodule_to(S)` first coerces generator lists/tuples to a submodule and otherwise requires `S.is_submodule(self)`; the return object is the formed submodule `S^⊥` with the restricted quotient-valued form. Public API should expose the subobject-input route and a separately named generator-subobject construction route rather than accepting untyped lists as ordinary subobjects. |
| `is_primitive(M)` | `free_quadratic_module_integer_symmetric.py:901` | module subobject/inclusion predicate | Admit for subobjects via quotient torsion-freeness. Do not conflate with element divisibility unless a source-backed equivalence proof records the hypotheses. |
| element `divisibility` | local lattice doctrine and user constraint | symmetric bilinear module elements | Required project surface. Definition is `<b(v,M)> <= S`; for scalar-valued forms it is an ideal of `R`. Sage does not supply this definition as a lattice method. |
| `orthogonal_group(gens=None, is_finite=None)`, `automorphisms`, `GroupOfIsometries(...)`, `GroupOfIsometries.__init__`, `GroupOfIsometries._repr_`, `GroupOfIsometries.__reduce__`, `GroupOfIsometries.invariant_bilinear_form`, `GroupOfIsometries._get_action_`, `GroupOfIsometries._check_matrix`, `GroupActionOnSubmodule`, `GroupActionOnSubmodule.__init__`, `GroupActionOnSubmodule._act_`, `GroupActionOnQuotientModule`, `GroupActionOnQuotientModule.__init__`, and `GroupActionOnQuotientModule._act_` | `free_quadratic_module_integer_symmetric.py:1155`, `1313`; `groups/matrix_gps/isometries.py:45`, `91`, `134`, `161`, `183`, `201`, `239`, `264`, `291`, `313`, `353`, `378`, `397`; Sage isometries docs | formed-module Aut category; invariant-subobject and invariant-subquotient group-action surfaces | `O(M,b)` is `Aut_C(M)`. Sage's `GroupOfIsometries` constructor data are backend realization data: matrix degree, base ring, generator matrices, immutable invariant bilinear-form matrix, optional group category, generator-preservation check, and optional invariant submodule or quotient module. With `check=True`, Sage tests each generator by `x F x^T = F`, then verifies submodule preservation by equality `I*f = I` and quotient preservation by preserving both the cover `V` and relation submodule `W`. `GroupOfIsometries.invariant_bilinear_form()` returns the preserved matrix as backend witness data. Public lattice API should construct an Aut parent or subgroup from typed automorphism elements and invariant object data; it should not expose raw matrices, `check`, display/pickle hooks, or right-action registration as lattice methods. `_get_action_` is a coercion-system registration path for right multiplication on an invariant submodule, on the originally supplied quotient, or on an invariant subquotient `S <= T` whose cover is preserved by all group generators. `GroupActionOnSubmodule._act_` and `GroupActionOnQuotientModule._act_` implement left/right matrix action, with the quotient case acting on a lift and coercing back to the quotient parent. These are group-action surfaces on already-typed invariant subobjects and subquotients, while display and pickle hooks remain runtime/serialization interop. |
| `minimum`, `maximum`, and aliases `min = minimum`, `max = maximum` | `free_quadratic_module_integer_symmetric.py:1409`, `1435`, `1460`; Sage docs `free_quadratic_module_integer_symmetric.html` | project mathematical methods on finite-rank free symmetric integral formed objects with ordered real realization; aliases are compatibility spellings | Admit `minimum()` and `maximum()` as extremal self-product values on `L \setminus {0}`. Source behavior sends the minimum to `-Infinity` when a negative direction exists, the maximum to `+Infinity` when a positive direction exists, and uses PARI `qfminim` only in the definite case after the signature check. The empty-rank case is a source precondition failure, not a separate mathematical surface. The aliases `min` and `max` do not create new project methods or owners; they migrate to the same admitted extremal-value surfaces. |
| `LLL()` / `lll` | `free_quadratic_module_integer_symmetric.py:1463`; Sage docs `free_quadratic_module_integer_symmetric.html` | presentation-normalization algorithm obligation for `OverZZ + Free + Symmetric` | Admit only as an algorithm surface returning a new presented lattice plus the basis-change/isometry witness required by the presentation-sensitive lattice model. Sage returns `self.sublattice(U * self.basis_matrix())`, using PARI Simon indefinite reduction when both signs occur and Gram-LLL in the definite case; the project mapping must not treat the reduced presentation as equality without the witness. |
| `short_vectors(n, up_to_sign_flag=...)` | `free_quadratic_module_integer_symmetric.py:1500`; Sage docs `free_quadratic_module_integer_symmetric.html` | definite `OverZZ + Free + Symmetric` vector-enumeration algorithm surface | Admit the finite source shape as `short_vectors(bound)` and `short_vectors_up_to_sign(bound)`. Source rejects indefinite lattices and delegates to `QuadraticForm.short_vector_list_up_to_length`; the project surface must return typed lattice elements organized by self-product value, not raw coordinate tuples or a keyword bag. |
| `enumerate_short_vectors()`, `enumerate_close_vectors(target)`, `FreeQuadraticModule_integer_symmetric._fplll_enumerate` | `free_quadratic_module_integer_symmetric.py:1541`, `1596`, `1631`; Sage docs `free_quadratic_module_integer_symmetric.html` | explicit algorithm-spec obligation plus backend-only helper | The public obligation is a typed vector-enumeration algorithm with stated definiteness, target, ordering, and witness semantics. Sage docs warn that the iterators are not strictly ordered by length or distance, and the source routes both through the private fplll helper after LLL reduction. `_fplll_enumerate` remains backend-only; any public project exposure must be the named iterator surfaces with hypotheses, not the helper. |
| `quadratic_form()` | `free_quadratic_module_integer_symmetric.py:1393` | forms conversion | Admit as `associated_quadratic_module()` or a named constructor/conversion in forms. The result category is codomain data, not the owner of the lattice operation. |
| exact `QuadraticForm` equivalence and isometry helpers | classified above from `quadratic_form__equivalence_testing.py:25`, `119`, `178`, `304`, `553`, `654`; import wiring in `quadratic_form.py:501` | backend evidence for lattice isometry methods | Use as implementation evidence for global, local, and rational isometry surfaces only after converting through the formed-module/lattice presentation and returning typed morphism or transformation witnesses when requested. The precise Sage options are already classified as backend strategy/witness selectors above. |

### Torsion and Discriminant-Form Reconciliation

| Sage surface | Source evidence | Spec owner or classification | Reconciliation |
| --- | --- | --- | --- |
| element `b`, `inner_product`, `q`, `quadratic_product` | `torsion_quadratic_module.py:121`, `154` | torsion bilinear/quadratic element methods | Admit as `b` and `q`; `inner_product` is interop terminology only. Values live in quotient codomains, not in lifted rationals. |
| `gram_matrix_bilinear`, `gram_matrix_quadratic`, `value_module`, `value_module_qf` | `torsion_quadratic_module.py:457`, `487`, `1251`, `1271` | torsion formed modules and discriminant groups | Admit as quotient-valued form data, distinct from free integral Gram matrices. Caller data are a torsion quadratic module presented as `V/W`; `value_module()` returns the bilinear-value codomain `QQ / mZZ` where `m = (V,W)`, and `value_module_qf()` returns the quadratic-value codomain `QQ / nZZ` where `nZZ = (V,W) + ZZ{(w,w) : w in W}`. `gram_matrix_bilinear()` and `gram_matrix_quadratic()` are presentation matrices whose entries live in those codomains, not lifted rational matrices that identify the object. Public spec surfaces must expose the codomain objects and form evaluations explicitly so downstream discriminant-form arguments can state where values live. |
| `all_submodules`, `submodule_with_gens`, `primary_part` | `torsion_quadratic_module.py:363`, `1113`, `1149` | finite torsion modules with restricted formed/discriminant-group structure | Admit with finite subobject semantics. `all_submodules()` enumerates every finite abelian subgroup through Sage's GAP abelian-group backend and returns each as a torsion quadratic submodule with restricted form data; this is finite-carrier subobject enumeration, not an infinite lattice submodule search. `submodule_with_gens(gens)` takes an iterable of elements convertible into the caller and returns the formed submodule generated by their lifts plus relations, preserving the selected generator presentation even when the generators are dependent; public exposure should use a named generator-subobject constructor rather than raw positional lists. `primary_part(m)` takes an integer selector and returns the submodule generated by the annihilator's prime-to-`m` multiple of the cover, so the admitted surface is an `m`-primary component/decomposition route with exact integer input data and inherited quotient-valued forms; do not restrict the mapping to prime powers unless a later spec intentionally offers a prime-only convenience alias. |
| `normal_form(partial=False)` | `torsion_quadratic_module.py:939`; Sage docstring cites [MirMor2009] IV Definition 4.6 | torsion quadratic/discriminant-form presentation classification | Admit as a canonical representative construction for torsion quadratic modules: two torsion quadratic modules are isomorphic exactly when they have the same value modules and the same full normal form. The caller is a torsion quadratic module `(T,q)` with values in `QQ/nZZ`; the return object is an isomorphic torsion quadratic module whose Gram presentation is block-normal by prime. `partial=True` is a nonunique invariant-extraction/backend route and must be exposed separately if admitted, not as an ordinary option bag. |
| `brown_invariant()` | `torsion_quadratic_module.py:408` | torsion quadratic/discriminant-form invariant | Admit as the Brown invariant `Br(A,q) in Z/8Z` of a finite torsion quadratic/discriminant form with values in `QQ/2ZZ`, defined by the Gauss-sum equation `exp(2*pi*i*Br(q)/8) = |A|^{-1/2} sum_{x in A} exp(pi*i*q(x))` and additive under direct sums. Source precondition: the quadratic value codomain must be exactly `QQ/2ZZ`; other codomains are a caller error, not a fallback branch. Sage computes the invariant by primary decomposition through `annihilator().gen().prime_divisors()`, `primary_part(p)`, `normal_form()`, block collection, and private block contribution `_brown_indecomposable(q,p)`. Public project exposure is the invariant on the torsion formed/discriminant object; the prime-block decomposition is proof/backend evidence unless a later spec admits it as a named witness surface. |
| `_brown_indecomposable(q, p)` | `torsion_quadratic_module.py:1292`; Sage docs `torsion_quadratic_module.html`; source cites Shimada 2016 Table 2.1 | private Brown-invariant backend helper | Do not admit as public mathematical API. This helper evaluates the Brown invariant contribution of an indecomposable `1 x 1` or `2 x 2` rational quadratic block at a prime `p`; public access is through `brown_invariant()` on the torsion quadratic module after normal-form block decomposition and the `QQ/2ZZ` codomain precondition. |
| `genus(signature_pair)` | `torsion_quadratic_module.py:539`; source cites Nikulin Corollaries 1.9.4 and 1.16.3 | discriminant-form plus signature global-genus construction theorem | Preserve as a theorem-level construction from a finite discriminant form and signature pair, not as a constructor on the returned genus object. Caller data are a torsion quadratic/discriminant form `D`, nonnegative signature pair `(s_+, s_-)`, rank `r=s_+ + s_-`, determinant sign relation `det=(-1)^{s_-}|D|`, and the rank/length condition `length(D) <= r`. The return object is a `GenusSymbol_global_ring` with local symbols whose discriminant form and signature match the input; no such genus is a source-level failure state, not a fallback result. |
| `is_genus(signature_pair, even=True)` | `torsion_quadratic_module.py:743`; source TODO says odd genera are not implemented | discriminant-form plus signature realizability predicate, even-lattice theorem branch only | Admit only as an even-lattice realizability predicate unless an odd-genus source is added. Inputs are a torsion quadratic/discriminant form, nonnegative signature pair, and parity flag. For `even=True`, Sage requires quadratic values modulo `2` and checks rank/length, determinant, 2-adic parity, and Brown-invariant congruence `brown_invariant(D)=s_+-s_-`; return is a boolean. For `even=False`, Sage requires modulo-`1` data but raises `NotImplementedError`; project mapping must record this as a missing odd-genus theorem/implementation obligation rather than exposing an optional public branch. |
| `orthogonal_group(gens=None, check=False)` on torsion quadratic modules | `torsion_quadratic_module.py:816`; `groups/fqf_orthogonal.py:1-122`, `220-318` | `Lattices(R).DiscriminantGroups().AutCategory()` via finite torsion formed-module Hom/Aut machinery | Admit as finite torsion formed-module Aut for a torsion quadratic/discriminant form `(A,q)`. `gens=None` computes the full finite orthogonal group by Sage's finite-quadratic-form backend; `gens=<iterable>` constructs the subgroup generated by supplied action data, covering matrices, or abelian-group automorphisms after conversion through the automorphism parent. The return object is a finite `FqfOrthogonalGroup` of isometries preserving `q`, with elements acting on discriminant-form elements from the right against Smith-form generators. Raw matrices, action objects, abelian-group automorphisms, and `check` are backend constructor/validation inputs, not public lattice automorphism elements before Aut containment succeeds; project exposure must separate full-group computation from subgroup-from-generators construction and omit `check` as a public option bag. |
| finite `isotropic_elements()` on torsion quadratic/discriminant forms | `torsion_quadratic_module.py:154`; theory/foundations/coble-task-background.md Task 2.1 | finite torsion formed-module enumeration surface; discriminant groups inherit generic formed-module isotropic predicates | Admit as enumeration of finite carrier elements satisfying the already-generic isotropic predicate, e.g. `q(x)=0` in the quadratic codomain. Use "elements", not "vectors", unless a separate finite-vector-space structure has been constructed. This is finite-set enumeration, not a lattice-vector enumeration. |
| finite action `orbit(x)`, `orbits(S)`, `orbit_representatives(S)` for discriminant-form automorphism groups | `torsion_quadratic_module.py:816`; `groups/fqf_orthogonal.py:299`, `385`, `430`; theory/foundations/coble-task-background.md Task 2.1 | `DiscriminantGroupAut` and finite group-action categories | Admit as finite group-action vocabulary on the discriminant-form Aut/action object, not as a method owned by `nikulin_invariants()` or by the underlying abelian group alone. Sage source supplies the right action through `FqfOrthogonalGroup._get_action_` and `ActionOnFqf`: elements act from the right on the invariant torsion quadratic module, invariant submodules, and invariant subquotients; left actions are rejected. The acted-on set must be a typed finite subset of the discriminant group, such as isotropic elements. Project orbit and representative methods should be finite-action methods over that typed action, backed by the Sage right-action machinery, rather than object-level methods on the discriminant group or ad hoc GAP orbit calls on untyped tuples. |
| `discriminant_action()`, `image_in_discriminant_orthogonal_group()`, `kernel_of_discriminant_action()` | Sage example `torsion_quadratic_module.py:852-864` builds the induced finite action by `D.orthogonal_group(O.gens())` and `O.hom(Obar.gens()).kernel()`; theory/foundations/coble-task-background.md Tasks 2.1-2.2 | `Lattices(R).AutCategory()` for nondegenerate integral lattices with `discriminant_group()` | Admit as the canonical project bridge `O(L) -> O(A_L,q_L)`. Sage supplies backend evidence for the finite discriminant-form action, not direct methods with these names. The kernel is the stable orthogonal group acting trivially on the discriminant form; subgroup names and orbit-lifting methods must route through this homomorphism. |
| `twist(s)` on torsion quadratic modules | `torsion_quadratic_module.py:1207` | forms-owned form scaling | Admit as formed-module twist/rescale. Caller data are a torsion quadratic module `V/W` and a rational scalar `s` coerced into the fraction field of the base ring. Sage scales the ambient inner-product matrix by `s`, rebuilds `V` and `W` inside the scaled free quadratic module, and returns a torsion quadratic module on the same quotient carrier with scaled form data. If the old quadratic form was defined modulo `n`, the source states the new quadratic form is defined modulo `n*s`; public mapping must therefore update the value codomain explicitly and must not treat twisting as scalar multiplication of the underlying finite abelian group. |

### Lattices Homset Mirroring Audit

The lattice homset surface is intentionally thin: it mirrors Sage homset behavior by
routing generic containers through the shared Hom/End base, module morphism mechanics
through the module and formed-module layers, and only lattice-specific orthogonal-group
refinements through `Lattices(R).AutCategory()`.

| Sage or project surface | Source evidence | Spec owner or classification | Reconciliation |
| --- | --- | --- | --- |
| Generic `Homset` container methods: `Homset.__reduce__`, `Homset.homset_category`, `Homset._abstract_element_class`, `Homset.element_class_set_morphism`, `__contains__`, `Homset.natural_map`, `Homset.identity`, `Homset.one`, `Homset.domain`, `codomain`, `Homset.reversed` | `sage/categories/homset.py:683`, `775`, `975`, `1057`, `1130-1263` | generic Hom/End semantic base | Do not duplicate these in `Lattices`. `domain`, `codomain`, and `reversed` are hom-parent navigation; `identity` and `one` are generic End identity vocabulary; `natural_map` is coercion interop, not a lattice-theoretic natural transformation. Dynamic element-class synthesis and set-morphism wrapping are Homset backend protocol. |
| `FreeModuleHomspace`, `FreeModuleHomspace.__call__(A, **kwds)`, `zero(side='left')`, `basis(side='left')`, `FreeModuleHomspace._matrix_space(side='left')`, `identity(side='left')` | `free_module_homspace.py:132-368` | module Hom parent constructors and backend interop | Preserve the three finite construction paths through module Hom parent constructors such as `from_matrix`, `from_images`, and callable conversion on generators. Preserve `zero` and `identity` through the additive and identity structure of Hom/End parents, with `identity` only for endomorphism parents. The `side` option and `_matrix_space` are backend matrix-orientation data, not public lattice option bags; public project constructors should name the mathematical input shape rather than forwarding `**kwds`. |
| `FGP_Module_class.hom(im_gens, codomain=None, check=True)` | `fg_pid/fgp_module.py:1480-1606`; Sage docs `fgp_module.html` | finitely presented PID-module convenience constructor | Keep as module interop. It constructs a Hom element from generator images: `codomain=None` is inferred from the image universe except for the empty-image zero map, free-module codomains are promoted to finitely presented quotients with zero relations, and source validity is the relation-preservation condition before an optimized-cover map is wrapped as an `FGP_Morphism`. Lattice and formed-module code should expose `M.Hom(N)` plus named construction routes from generator images, matrices or cover maps, and callables; `hom(images)` remains a Sage convenience route and not the main lattice public constructor. |
| `FGP_Module_class._Hom_`, `FGP_Homset`, `FGP_Homset_class` | `fg_pid/fgp_module.py:1690-1731`; `fg_pid/fgp_morphism.py:462-515` | module Hom backend | These are Sage plumbing for finitely presented module Hom parents. Lattice homsets refine the generic/module Hom category instead of wrapping or copying this class. |
| `FGP_Morphism.im_gens`, element/submodule `__call__`, `kernel`, `inverse_image`, `image`, and `lift` | `fg_pid/fgp_morphism.py:137-456`; detailed rows in the core inherited-FGP table above | module Hom element methods; formed-module refinements inherit restricted subobjects where form hypotheses hold | Preserve these through the shared module/form Hom element layer, not through a lattice-local morphism class. The core table records caller, input, return, and owner splits; this audit row only records that lattice homsets inherit those mapped methods. |
| Missing formed `cokernel()` | Sage docs `fgp_morphism.html`; installed `fg_pid/fgp_morphism.py:1-548`; `category_specs/forms/subcategories/with_forms.py:100-119`; `[[SPEC-MAPPING-FORMS]]`; `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]` | formed-module Hom element quotient surface, specialized by lattice discriminant descent | Not solved by Sage FGP morphism helpers. Lattice homsets inherit the formed-module route: take the module quotient, take the form-codomain cokernel, quotient by cross-term images, and specialize to `A_L = coker(L -> L^#)` where the cross-term quotient is trivial and the codomains are `K/R` and `K/2R`. This is a settled spec surface, not a runtime implementation claim. |
| `FGP_Module_class.cardinality`, `is_finite`, `list`, `__iter__`, `random_element`, and `construction` | `fg_pid/fgp_module.py:1733`, `1755`, `1788`, `1800`, `1832`, `1867`; detailed rows in the inherited-FGP table above | inherited set/module enumeration, presentation-functor metadata, and runtime surfaces | Preserve as inherited finite-set and finitely presented module behavior. These methods are not lattice-owned; discriminant groups and torsion formed modules inherit the finite-carrier/set-enumeration surface from their underlying finitely presented PID-module and finite-set categories. `construction()` remains Sage reconstruction metadata for the quotient presentation, not a lattice constructor or Hom/End/Aut surface. |
| `FormedModulesCategory.ParentMethods.orthogonal_group()` and `FormedModulesCategory.HomCategory.ElementMethods.is_isometry()` | `category_specs/forms/subcategories/with_forms.py:52-55`, `102-106` | formed-module Aut and Hom element owner | `O(M,b) = Aut_C(M)` is formed-module vocabulary. Lattices specialize this through `Lattices(R).AutCategory()`; they do not own the generic definition of isometry. |
| `FreeBilinearModulesCategory.HomCategory.ElementMethods.to_matrix()` | `category_specs/forms/subcategories/free_bilinear.py:230-236` | free bilinear Hom element owner | Matrix realization is available for free bilinear morphisms and inherited by lattice morphisms. It is a representation of a typed Hom element, not a replacement for the Hom element. |
| `LatticeHomCategory.extra_super_categories()` and `ElementMethods` | `category_specs/lattices/homsets.py:41-55` | lattice HomCategory shell over formed/module Hom | Keep the shell thin: it refines the base category and `Modules(R).HomCategory()` but adds no duplicate generic Hom logic. |
| `LatticeEndCategory.ParentMethods.base_lattice()` | `category_specs/lattices/homsets.py:61-74` | lattice End parent surface | Lattice End objects may expose their structure lattice, but generic `domain()` remains generic Hom/End navigation. |
| `LatticeAutCategory.ParentMethods.special_subgroup`, `stable_subgroup`, `stable_special_subgroup`, and aliases | `category_specs/lattices/homsets.py:78-127` | lattice Aut parent refinement | These are lattice orthogonal-group subgroup constructors. Former object-level calls route through the aut object, e.g. `L.orthogonal_group().stable_orthogonal_group()`. |
| `LatticeAutCategory.ElementMethods.is_isometry()` | `category_specs/lattices/homsets.py:32-38` | lattice Aut element fact | Lattice automorphisms are isometries by category membership. This mirrors the formed-module `is_isometry()` definition without creating a separate morphism container. |
| `FreeQuadraticModule_integer_symmetric.orthogonal_group(gens=None, is_finite=None)` and `automorphisms` | `free_quadratic_module_integer_symmetric.py:1155-1313`; `groups/matrix_gps/isometries.py:45-123`, `201-411` | formed/lattice Aut backend evidence | Sage returns a `GroupOfIsometries` matrix group, with definite-generator computation and right-action behavior as implementation details. Project `gens` admission is subgroup construction after each candidate is promoted to an automorphism object and checked by Aut containment. Invariant submodule, quotient, and subquotient actions inherit from the typed Aut/action surface rather than from a lattice-local raw-matrix method. |
| `TorsionQuadraticModule.orthogonal_group(gens=None, check=False)` | `torsion_quadratic_module.py:816-867`; `groups/fqf_orthogonal.py:1-122`, `220-318` | discriminant-form Aut category | Route to `Lattices(R).DiscriminantGroups().AutCategory()` through finite torsion formed-module Hom/Aut machinery. Caller data are a torsion quadratic/discriminant form `(A,q)`; `gens=None` means full finite orthogonal group computation, while supplied `gens` are subgroup-generator data converted from right actions, covering matrices, FGP morphisms, or abelian-group automorphisms and checked for preservation of `q`. The returned group is the finite automorphism/isometry group of `(A,q)` with right action on elements and invariant subquotients. Raw matrices, abelian-group automorphisms, ambiguous representative matrices, and `check` are constructor/backend inputs, not public lattice automorphisms before containment succeeds; public spec vocabulary must expose the Aut parent and any subgroup-from-generators route as finite named surfaces rather than a broad optional `gens`/`check` method. |
| `discriminant_action()`, `image_in_discriminant_orthogonal_group()`, `kernel_of_discriminant_action()` | Sage example `torsion_quadratic_module.py:852-864`; downstream cards `[[TASK-LAT-PHASE5-DISCRIMINANT-KERNEL]]` and `[[TASK-LAT-PHASE5-ORTHOGONAL-SUBGROUPS]]` | project lattice Aut bridge | No direct Sage methods with these names were found in the checked corpus. Keep them as project methods on lattice orthogonal/aut objects, backed by the quotient action `O(L) -> O(A_L,q_L)` and downstream phase cards. |

- Searched: `category_specs/lattices/homsets.py`, `category_specs/forms/subcategories/with_forms.py`,
  `category_specs/forms/subcategories/free_bilinear.py`,
  `category_specs/modules/subcategories/finitely_presented_over_pid.py`,
  Sage reference page `sage.modules.fg_pid.fgp_morphism`, Sage `sage/categories/homset.py`,
  `sage/categories/homsets.py`,
  `sage/modules/free_module_homspace.py`, `sage/modules/fg_pid/fgp_module.py`,
  `sage/modules/fg_pid/fgp_morphism.py`,
  `sage/modules/free_quadratic_module_integer_symmetric.py`,
  `sage/modules/torsion_quadratic_module.py`, `sage/groups/matrix_gps/isometries.py`,
  and targeted `rg` searches for `discriminant_action`,
  `image_in_discriminant_orthogonal_group`, `kernel_of_discriminant_action`, and
  `def cokernel(`, plus `Autset` across the checked generic/module Hom sources.
- Found: Sage supplies generic Homset containers, free/FGP module Hom backends,
  matrix-group isometry backends, and torsion-form orthogonal-group examples. No
  dedicated Sage lattice Homset class, generic Sage `Autset`, direct Sage
  discriminant-action method names, or `FGP_Morphism.cokernel` implementation was found
  in the checked docs/source corpus.
- Conclusion: inference -- lattice Hom/End/Aut should remain a project shell over
  generic Hom/End, module Hom, and formed-module Aut semantics. Lattice-specific work is
  the Aut subgroup/discriminant bridge and downstream formed-cokernel spec obligation,
  not a new lattice-local homset hierarchy.
- Confidence: High.
- Gaps: Deleted historical implementation files and lazy-imported Sage modules outside
  the checked installed-source corpus were not exhaustively searched because the active
  spec source and downstream cards already own the required project surfaces.

### Formal Negative and Corrective Findings

- Searched: `sed` on `theory/spec_backups/lattices_written_spec_backup.py`; `test -e
  /home/dzack/research/theory/spec_backups/lattices_written_spec_backup.py`; `git
  ls-files theory/spec_backups`; `find theory -path '*spec_backups*' -type f`; `rg
  "lattices_written_spec_backup" theory .agents plans category_specs`; and
  `.agents/theory/spec-backups/lattices_written_spec_backup.py`.
- Found: the old path `theory/spec_backups/lattices_written_spec_backup.py` is absent
  from the current working tree and is not tracked by `git ls-files`, but the source
  material exists at `.agents/theory/spec-backups/lattices_written_spec_backup.py`.
  Its opening docstring supplies the presented-module-with-form model, and its element
  implementation records the old pairing-defined divisibility behavior. The
  lattice-redesign skill has also centralized the mined doctrine into
  `category-abc-spec.md`, `lattice-interface-style-guide.md`, and
  `lattice-redesign-corrections-spec.md`.
- Conclusion: inference -- current work should cite the centralized lattice-redesign
  doctrine as the active API authority and may use
  `.agents/theory/spec-backups/lattices_written_spec_backup.py` as mineable source
  provenance. The stale `theory/spec_backups/...` path should not be treated as
  unavailable mathematical content.
- Confidence: High.
- Gaps: I did not search deleted git history for the exact move/rename commit because
  the active source material is present in `.agents/theory/spec-backups/`.

- Searched: `sed` on `src/lattices/categories/bilinear_modules.py`,
  `src/lattices/categories/free_bilinear_modules.py`,
  `src/lattices/categories/lattices.py`; `git ls-files src/lattices`; `npx -y
  @probelabs/probe search` for the named local category surfaces under
  `/home/dzack/research` and `/home/dzack/research/category_specs`.
- Found: no active `src/lattices/categories/...` files are present or tracked in this
  checkout. The corresponding current spec surfaces live under `category_specs/forms`,
  `category_specs/modules`, and `category_specs/lattices`, with roadmap cards retaining
  older `src/lattices` references.
- Conclusion: inference -- the `src/lattices/categories/...` entries in
  `SAGE_INVENTORY.md` are stale local-source pointers for the current checkout; use the
  active `category_specs` files and lattice-redesign docs as the local source surface.
- Confidence: High.
- Gaps: I did not inspect deleted git history or quarantined backup trees for old
  implementation files because the task forbids edits outside this spec and the active
  spec files provide the current owner surface.

- Searched: `category_specs/lattices/docs/SAGE_INVENTORY.md`; installed Sage 10.7
  sources named above; `npx -y @probelabs/probe search "inclusion_morphism
  rational_span nikulin_invariants is_rationally_isometric_to is_locally_isometric_to"
  /home/dzack/miniforge3/envs/sage/lib/python3.12/site-packages/sage -l python`.
- Found: no Sage lattice methods named `inclusion_morphism`, `rational_span`,
  `nikulin_invariants`, `is_isometric_to`, `is_rationally_isometric_to`, or
  `is_locally_isometric_to` were found on `FreeQuadraticModule_integer_symmetric`.
  Sage does provide related backend evidence: `dual_lattice()`,
  `discriminant_group()`, quadratic-form rational/local equivalence helpers, and
  finite torsion-form orthogonal groups.
- Conclusion: inference -- these project lattice surfaces are genuine spec obligations
  or wrapper/admission work, not already-covered Sage lattice methods. Existing Sage
  code can support parts of their implementation but does not determine the public
  owner or signatures.
- Confidence: Medium.
- Gaps: I did not exhaust every lazy-imported Sage quadratic-form submodule beyond the
  local/rational equivalence and local-invariant sources surfaced by `QuadraticForm`.

## Converted Mapping Content

Records the mathematical justification for how every Sage surface maps to our hierarchy.
For each Sage type: what Sage provides, the correct mathematical concept, placement
decision, and consequence for refinement and regression tests.

---

## Hierarchy Overview

```
FormedModules(R) = Modules(R, dispatch=False).WithForms()  [owned in forms/]
└── .Bilinear()                                            [owned in forms/]
    └── .Symmetric()                                       [owned in forms/]
        └── .Nondegenerate()                               [owned in forms/]
            └── .Integral()                                [owned in forms/]
                └── .Lattice()                             [named endpoint in lattices/]

forms/subcategories/
├── with_forms.py          FormedModulesCategory
├── bilinear.py            BilinearModulesCategory
├── quadratic.py           QuadraticModulesCategory
├── symmetric.py           SymmetricBilinearModulesCategory
├── nondegenerate.py       NondegenerateBilinearModulesCategory
├── indefinite.py          IndefiniteBilinearModulesCategory
├── definite.py            DefiniteBilinearModulesCategory
├── integral.py            IntegralBilinearModulesCategory
├── rational.py            RationalBilinearModulesCategory
└── free_bilinear.py       FreeBilinearModulesCategory     (Free + Bilinear)

lattices/subcategories/
├── over_dedekind.py       _LatticesOverDedekindDomain
├── over_pid.py            _LatticesOverPID
├── over_integers.py       _LatticesOverIntegers    (= Lattices(ZZ))
├── even.py                _EvenLattices
├── unimodular.py          _UnimodularLattices
└── constructions/
    ├── dual_objects.py    Hom-dual objects, when admitted with explicit formed-object data
    ├── dual_lattices.py   metric-dual lattice construction / compatibility spelling
    ├── overlattices.py
    ├── orthogonal_direct_sums.py
    └── discriminant_groups.py
```

---

## Method Placement Table

The table answers: at what tier is each method first universally well-defined?

| Method | Minimal tier | Justification |
|--------|-------------|---------------|
| `form()` | `WithForms` | definitional; every object in this category carries a form |
| form evaluation | inherited from `Modules().WithForms()` | not lattice-owned; lattices inherit module-with-form evaluation rather than defining a lattice-specific `evaluate` method |
| `form().tensor_degree() -> Integer` | `WithForms` through the carried form object | Arity of the form datum: bilinear forms have tensor degree `2`; quadratic forms have tensor degree `1`; tensor variance tuples belong to `TensorAlgebraComponents(R).tensor_type()`, not this formed-module row. |
| `b(v, w)` | `Bilinear` | bilinear evaluation; only defined once the form is bilinear |
| `self_product(v)` | `Bilinear` | `b(v,v)`; defined for any bilinear form |
| `is_isotropic(v)` | `Bilinear` | `b(v,v) = 0`; defined for any bilinear form |
| `perp(v)` (element) | `Bilinear` | `{w ∈ M : b(v,w)=0}` is a submodule for any bilinear form |
| `orthogonal_submodule_to(S)` | `Bilinear` | `S^⊥ = {w : b(s,w)=0 ∀s∈S}`; always a submodule |
| `q(v)` | `Quadratic` | quadratic form evaluation |
| `is_symmetric()` | `Symmetric` | witness predicate |
| `is_alternating()` | `Alternating` | witness predicate |
| `is_nondegenerate()` | `Nondegenerate` | witness predicate |
| `is_indefinite()` | `Indefinite` over ordered real realizations | witness predicate; requires a signed scalar context |
| `is_definite()` | `Definite` over ordered real realizations | witness predicate; requires a signed scalar context |
| `gram_matrix()` | `Free + Bilinear` | requires a basis; entries `b(e_i,e_j)` lie in R; see note (1) |
| `inner_product_matrix()` | presentation/ambient-cover data | Sage ambient-space form matrix; not the restricted Gram matrix and not public lattice identity; see reconciliation |
| `rank()` | `Free` | rank of free module; undefined for general modules |
| `zero_submodule()` | module subobjects | zero subobject with its inclusion; for formed modules the form restricts to zero on the zero carrier |
| `cardinality()` | `Sets()` / `Modules(R)` underlying-set runtime | cardinality of the carrier set; FGP quotients compute `+Infinity` when any Smith invariant is `0`, otherwise the product of invariants (`fg_pid/fgp_module.py:1755`); not lattice-owned |
| `is_finite()` | `Sets()` / `Modules(R)` underlying-set runtime | finite-carrier predicate; FGP quotients are finite exactly when no Smith invariant is zero (`fg_pid/fgp_module.py:1867`); not lattice-owned |
| `list()` / `__iter__()` | finite enumerated sets/modules | materializes or iterates finite `ZZ` FGP carriers through product ranges for Smith invariants and the optimized cover basis (`fg_pid/fgp_module.py:1788`, `1800`); infinite lattice enumeration belongs to the separate countability/enumeration program |
| `random_element()` | parent/module runtime | samples an element of the carrier; Sage FGP samples the cover `V` and reduces modulo relations (`fg_pid/fgp_module.py:1733`); not lattice-owned |
| `determinant()` | `Free + Bilinear` | `det(gram_matrix)`; requires basis |
| `discriminant()` | `Free + Bilinear` | Sage convention `(-1)^floor(rank/2) * det(gram_matrix)`; requires basis and follows `FreeQuadraticModule_generic.discriminant()` rather than the generic quadratic-form discriminant sign convention |
| `is_positive_definite()` | `Free + Symmetric + ordered real realization` | eigenvalue/sign criterion needs finite free symmetric data over a scalar context with ordered real interpretation; see note (2) |
| `is_negative_definite()` | `Free + Symmetric + ordered real realization` | same |
| `signature_pair()` | `Free + Symmetric + ordered real realization` | inertia after scalar extension to the ordered real target; see note (2) |
| `signature()` | `Free + Symmetric + ordered real realization` | derived Sage scalar `p - q`; signature semantics are owned by `signature_pair()` |
| `dual_lattice()` | `Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` | metric dual `L^#={v in L_K: beta(v,L) subset R}` uses the nondegenerate pairing inside scalar extension; see note (3) |
| `discriminant_group()` | `Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` | `L^#/L` with the descended quotient-valued form; follows from `dual_lattice()`; see note (3) |
| `discriminant_form()` | `Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` | named access to the quotient-valued bilinear/quadratic form on `L.discriminant_group()`; never just the invariant tuple |
| `inclusion_morphism()` | `Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` | `i: L -> L^#`; same metric-dual tier |
| `is_even()` | `Bilinear.Integral` | `b(m,m) in 2R for all elements m`; requires integrality but not freeness |
| `is_unimodular()` | `Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` | `L = L^#`, i.e. `|det|=1` |
| `orthogonal_complement(S)` (parent) | `Bilinear.Symmetric` | `S^⊥` is a submodule for ANY symmetric bilinear module; see note (4) |
| `is_primitive(M)` | `Free + OverIntegralDomain` | quotient L/M is torsion-free |
| `direct_sum(other)` | `Free + Bilinear` | orthogonal direct sum with block-diagonal gram matrix |
| `tensor_product(other)` | `Free + Bilinear` | Kronecker product of gram matrices |
| `sublattice(basis)` | `Free + Bilinear + OverPID` | sublattice on a basis requires PID structure |
| `overlattice(gens)` | `Free + Symmetric + Nondegenerate + OverIntegralDomain` | L + span(gens) ∩ dual; requires dual |
| `maximal_overlattice(p)` | `OverZZ` | algorithm uses ZZ-specific arithmetic |
| `twist(s)` | `WithForms` | scale form by scalar; defined for any module with form |
| `genus()` | `OverZZ` | local-global genus theory; requires ZZ (or at least Dedekind) |
| `genus(signature_pair)` | `Torsion + Quadratic` discriminant-form data plus integral signature theorem | Sage torsion quadratic modules construct the global genus defined by a finite discriminant form and signature pair; this is distinct from lattice `genus()` on a concrete integral lattice |
| `is_genus(signature_pair, even=True)` | `Torsion + Quadratic` discriminant-form realizability theorem | Even-lattice realizability predicate for a discriminant form and signature pair; Sage's odd branch is a missing theorem/implementation obligation, not a public option fallback |
| `orthogonal_group()` | `Modules(R).WithForms().AutCategory()` | `O(M,b) = Aut(M,b)` in the category of modules with forms; see note (5) |
| `special_orthogonal_group()` | `Lattices(R).AutCategory()` parent-method refinement | determinant-one subgroup of the lattice orthogonal group, defined once the aut surface has a determinant realization |
| `stable_orthogonal_group()` | `Lattices(R).AutCategory()` parent-method refinement | orientation or positive-cone refinement of the lattice orthogonal group, not a method on lattice objects |
| `discriminant_action()` | `Lattices(R).AutCategory()` plus lattice `discriminant_group()` data | homomorphism from lattice isometries to automorphisms of the discriminant form |
| `image_in_discriminant_orthogonal_group()` | same as `discriminant_action()` | image subgroup of `O(A_L,q_L)` |
| `kernel_of_discriminant_action()` | same as `discriminant_action()` | stable orthogonal subgroup acting trivially on `A_L` |
| `nikulin_invariants()` | `OverZZ + Free + Symmetric + Nondegenerate` | convenience invariant tuple for sourced 2-elementary classification; not a substitute for `discriminant_group()` or discriminant-form orbit data |
| `is_isometric_to(other)` | `OverZZ + Free + Symmetric + Nondegenerate` | lattice isometry test |
| `minimum()` (`min` alias) | finite-rank `OverZZ + Free + Symmetric` with ordered real realization | extremal self-product value `min{b(x,x) : x in L \ {0}}`; source returns `-Infinity` once a negative direction exists and uses PARI only in the positive-definite case; rank-zero is a source precondition failure; alias is a Sage compatibility spelling, not a separate project method |
| `maximum()` (`max` alias) | finite-rank `OverZZ + Free + Symmetric` with ordered real realization | extremal self-product value `max{b(x,x) : x in L \ {0}}`; source returns `+Infinity` once a positive direction exists and uses PARI on `-gram_matrix` only in the negative-definite case; rank-zero is a source precondition failure; alias is a Sage compatibility spelling, not a separate project method |
| `LLL()` | `OverZZ + Free + Symmetric` | LLL reduction; ZZ-specific |
| `short_vectors(n)` | `OverZZ + Free + Symmetric` | ZZ-specific enumeration |
| `short_vectors(n, up_to_sign_flag=True)` | `short_vectors_up_to_sign(n)` at the same tier | Sage forwards `**kwargs` to `QuadraticForm.short_vector_list_up_to_length`; the installed source exposes the single meaningful keyword `up_to_sign_flag`. The project splits that finite case into a named method instead of exposing a keyword bag. |
| `quadratic_form()` | `Free + Symmetric` | convert to `QuadraticForm` object |
| `rational_span()` | `Free + OverIntegralDomain` | `L ⊗_R Frac(R)` |
| `base_change_to(ring)` | `Free + Bilinear` | change coefficient ring |
| `gram_matrix_bilinear()` | `Torsion + Bilinear` | Gram matrix in Q/mZ; see note (6) |
| `gram_matrix_quadratic()` | `Torsion + Quadratic` | quadratic Gram matrix |
| `isotropic_elements()` | `Torsion + Quadratic + finite carrier` | finite enumeration of elements with `q(x)=0`; excludes infinite lattice-vector enumeration semantics |
| `orbit(x)`, `orbits(S)`, `orbit_representatives(S)` | `FiniteGroupAction` on a typed finite set | action methods belong to the group/action object, including the right action of `DiscriminantGroupAut` on discriminant-group elements, invariant subobjects, and invariant subquotients |
| `brown_invariant()` | `Torsion + Quadratic` with quadratic values in `QQ/2ZZ` | Brown invariant of a finite torsion quadratic/discriminant form; Sage source defines it for `(D,q)` with values in `QQ/2ZZ`, not for bare symmetric bilinear torsion data |
| `normal_form(partial=False)` | `Torsion + Quadratic` | canonical MirMor normal-form representative; full form is an isomorphism classifier together with value modules, while partial normal form is nonunique backend/invariant-extraction data |
| `primary_part(m)` | `Torsion` | `m`-primary component selected by integer `m`; for Sage torsion quadratic modules this is returned as a formed submodule with the inherited quotient-valued form |
| `value_module()` | `Torsion + Bilinear` | `QQ / mZZ` containing bilinear form values, where `m = (V,W)` in Sage torsion quadratic modules |
| `value_module_qf()` | `Torsion + Quadratic` | `QQ / nZZ` containing quadratic form values, where `nZZ = (V,W) + ZZ{(w,w) : w in W}` in Sage torsion quadratic modules |
| `additive_order(v)` | `Torsion` (element) | order in torsion group |
| `lift(v)` | quotient-presentation element / discriminant-group specialization | Sage `FGP_Element.lift()` lifts an element of `V/W` to the cover `V`; a discriminant-form lift is a dual/rational-lattice representative only when the presented quotient is explicitly `L^#/L` |
| `divisibility(v)` | `Bilinear.Symmetric` (element) | pairing-image submodule `<b(v, L)> <= S`; for scalar-valued forms `S = R`, this is an ideal; see note (9) |
| `is_primitive(v)` | `Modules` (element) | cyclic submodule primitive predicate via `v.span().inclusion().is_primitive()`; not a unit-divisibility rule without a source-grounded equivalence proof |
| `discriminant_class(x)` | metric-dual lattice element, i.e. an element of `L.dual_lattice()` | quotient map `L^# -> L^#/L`; ordinary `v in L` maps to the zero class via `L -> L^#`; see note (8) |
| `reflection(v)` | `Free + Symmetric + Nondegenerate` with `b(v,v) != 0` (element) | `s_v(w) = w - 2b(v,w)/b(v,v) · v` in the scalar extension; it is an automorphism of the original lattice exactly under the lattice-preservation condition in note (11) |
| `is_root(v)` | `Free + Symmetric + Integral` (element) with lattice-preservation condition | root predicate for vectors whose reflection preserves the integral lattice: `2 b(v,x) / b(v,v) in R` for every `x in L`; the even two-elementary specialization is `v^2=-2`, or `v^2=-4` with `div(v)=2` |
| `norm(v)` | `Bilinear` (element) — see note (7) | b(v,v); defined for any bilinear form |

---

## Notes

**(1) `gram_matrix()` placement**: Sage places this at `FreeQuadraticModule_generic`
(free over commutative ring). We confirm: entries `b(e_i, e_j)` live in R only when the
form is R-valued and M is free (so the `e_i` are a basis). For a torsion module over a
PID, the analogous concept is `gram_matrix_bilinear()` in Q/mZ. These are distinct
methods at distinct tiers; do NOT merge them.

**(2) `signature_pair()` placement**: Sage places this at
`FreeQuadraticModule_integer_symmetric` (= ZZ). Mathematically, signature and positive
or negative definiteness require finite free symmetric bilinear data plus a scalar
context where signs make sense, such as `ZZ -> QQ -> RR` or another ordered real
realization. `[[DECISION-ORDERED-REAL-SIGNATURE-OWNER]]` decides that the abstract
owner is finite free symmetric bilinear data with a selected ordered real realization.
A bare `OverIntegralDomain` hypothesis does not choose such a realization and is not
enough. The `OverIntegers` tier provides concrete Sage evidence through the canonical
`ZZ -> QQ -> RR` ordered realization; fields or domains with multiple orderings need a
chosen ordering/real embedding or a separate total-signature surface.

**(3) `dual_lattice()` placement**: `L^# = {v in L_K : beta(v,L) <= R}` where
`L_K = L tensor_R K` and `K = Frac(R)`. This is the metric dual inside scalar
extension, not the general module dual `L^* = Hom_R(L,R)`, and its elements are not
functionals by definition. The bilinear form sends `x in L^#` to the functional
`beta(x, -)` when that expression is defined in the scalar extension, and under
finite projective/free nondegenerate hypotheses this transport can identify `L^#`
with `Hom_R(L,R)`. That identification does not by itself put an
`R`-valued form on `Hom_R(L,R)`: the metric dual inherits the scalar-extended
`K`-valued form, and any form transported to the module dual must name the
identification and codomain. In the unimodular integral case, including the identity
Gram toric-coordinate presentation, `L = L^#` and the transported form is the original
`R`-valued form. The discriminant group `L^#/L` is the finite quotient with descended
form data in the nondegenerate integral lattice setting. Sage's `ZZ` implementation is
one concrete algorithm for this owner.

The method docstring for `dual_lattice()` and any lattice-side `dual()` compatibility
surface must include the diagnostic-warning conditions. With the global category
diagnostic flag enabled, implementations should warn when the returned object may be
misread as the Hom dual: for example, in degenerate formed modules such as a rank-one
summand `<e>` in the hyperbolic plane `U`, `L^#` is the metric-dual construction being
returned, not an evaluation-bearing object of `Hom_R(L,R)`. In nondegenerate finite
free cases the warning text should name the recorded form-induced identification if
the implementation transports between `L^#` and `Hom_R(L,R)`. The warning must be
gated by the disabled-by-default category diagnostic flag specified in
`[[SPEC-MAPPING-CAT]]`; it is explanatory logging only and must not hide invalid
degenerate, non-free, or non-integral inputs.

**(4) `orthogonal_complement(S)` placement**: `S^⊥ = {v ∈ M : b(v,s) = 0 ∀s ∈ S}`.
This is always a submodule. No assumptions needed beyond having a bilinear form.
For symmetric forms, left and right orthogonal complements coincide, so the symmetric
axiom is needed only to guarantee `(S^⊥)^⊥ = S`. Abstract stub belongs at
`Bilinear.Symmetric`. Computability (as a free module of explicit rank) requires
nondegeneracy + free; that is an algorithm concern, not a placement concern.

**(5) `orthogonal_group()` placement**: `O(M,b) = Aut(M,b)` in the category of
modules with forms. Equivalently, its elements are module automorphisms `f` such that
`b(fv, fw) = b(v, w)`, or the corresponding form-preservation diagram commutes. This
definition does not require freeness, nondegeneracy, or integrality, so it covers
degenerate formed modules such as `e^perp` in `U`, rational formed modules, integral
lattices, and finite discriminant forms. Freeness and nondegeneracy are only needed for
particular realizations such as matrix groups inside `GL_n(R)`. The `OverZZ` definite
lattice computation uses Plesken-Souvignier; that is an algorithm detail, not the
mathematical owner.

**(6) Torsion `gram_matrix_bilinear()`**: For `TorsionQuadraticModule = V/W`, the form
takes values in `Q/mZ = (V^*/W^*) / (V/W)^*`. This is a *different type* from the
integral Gram matrix. Both are "gram matrices" but they live at different tiers with
different codomains. The torsion version is named `gram_matrix_bilinear()` (following
Sage) to avoid ambiguity with `gram_matrix()` at the free level.

**(7) `norm(v)` vs `self_product(v)`**: Both are `b(v,v)`. The name `norm` is Sage
lattice convention (appears in `Lattices.ElementMethods`). The name `self_product`
appears in our `BilinearModules.ElementMethods`. Both belong at `Bilinear` (element).
In the spec we use `self_product` at the generic bilinear level and provide `norm` as an
alias at the `Lattices(ZZ)` level (where "norm" is standard terminology).

**(8) `discriminant_class(x)` ownership**: The nontrivial map is the quotient
`L^# -> L^#/L`, so the method belongs to elements of the metric-dual lattice returned
by `L.dual_lattice()`. It does not belong to category-theoretic `DualObjects()`: those
are Hom-dual objects with evaluation behavior, while elements of `L^#` become
functionals only after explicit transport through the bilinear form. The former
ordinary lattice-element reading is recovered by first applying the inclusion
`L -> L^#`; its discriminant class is necessarily the zero element of
`L.discriminant_group()`, so it is not a separate element obligation on `L`.

**(9) `divisibility(v)` ownership**: For a symmetric bilinear module `(M, b)` with
`b: M x M -> S`, the invariant definition is the `R`-submodule
`<b(v, w) : w in M>` of `S`. In the scalar-valued case this is an ideal of `R`.
Principal generators and gcd presentations are representation choices under extra
hypotheses; they are not the owner definition and must not be replaced by coordinate
content in `Modules(R).Free()`.

**(10) Discriminant form and orbit ownership**: `nikulin_invariants()` is only a
classification summary under explicit 2-elementary hypotheses. The object needed for
orbit work is the discriminant group `A_L = L^#/L` together with its descended
quotient-valued form. The predicate `is_isotropic(x)` is generic formed-module/form
vocabulary; discriminant forms only add finite carrier enumeration such as
`isotropic_elements()`. Orbit decomposition belongs to a finite group action, and for
lattices the relevant action is supplied by the homomorphism `O(L) -> O(A_L,q_L)`.
Downstream lifting theorems may use the kernel/image of this homomorphism only after
their hypotheses have been checked against the actual computed lattice and
discriminant form.

**(11) Reflection lattice-preservation condition**: For a non-isotropic element `v` of
a finite free symmetric bilinear object, the formula
`s_v(w) = w - 2 b(v,w) / b(v,v) * v` defines an orthogonal reflection after scalar
extension to a ring where `b(v,v)` is invertible. This formula is not automatically an
automorphism of the original integral lattice. For an integral lattice `L`, the
reflection lies in `O(L)` exactly when `2 b(v,x) / b(v,v) in R` for every `x in L`,
equivalently `2 div(v) / b(v,v) in R` in the scalar-valued PID setting. Roots are
therefore defined by integrality of the reflection, not by `b(v,v)` being a unit. In
the even two-elementary lattice theory note, the source-backed specialization is
`v^2 = -2`, or `v^2 = -4` with `div(v) = 2`.

---

## Construction-Category Vocabulary

The category-theoretic dual construction name `DualObjects()` is reserved for
Hom-dual objects: parents that are objects of the category and also carry the
hom-object/evaluation behavior coming from `Hom_R(N, R)`. It is not the owner for the
metric-dual lattice `L^#`.

The metric-dual lattice construction is `dual_lattice()` at the method level. If a
construction-category spelling is retained for compatibility, use the lattice-specific
`DualLattices()` spelling for that metric construction and keep it separate from
`DualObjects()`.

Other lattice construction names audited in this pass are not duplicate spellings of
standard construction categories:

| Lattice surface | Relationship to standard construction vocabulary | Decision |
| --- | --- | --- |
| `DualObjects()` | Hom-dual object construction: objects represented as `Hom_R(N, R)` and therefore carrying hom-object/evaluation behavior. A formed-object structure requires an explicit transported form or separate data. | Category dual surface, not the metric-dual owner. |
| `DualLattices()` | Lattice-specific metric-dual construction for `L^# = {v in L_K : beta(v,L) subset R}` when retained as a construction category. | Compatibility spelling for metric duals; keep separate from `DualObjects()`. |
| `Overlattices()` | Objects under a fixed lattice with finite-index, same-rational-span, inherited-form conditions. | Keep as lattice-specific refinement, not a replacement for `ObjectsUnder(base)`. |
| `OrthogonalDirectSums()` | Cartesian-product construction plus the orthogonal block-sum form and summand access. | Keep as refinement below `CartesianProducts()`. |
| `IntegralLatticeDirectSum(...)` | Sage top-level factory for the same orthogonal direct-sum construction, optionally returning summand embeddings. | Recover through `OrthogonalDirectSums()` and structure morphisms; do not expose `return_embeddings` as a public option. |
| `IntegralLatticeGluing(...)` | Sage top-level gluing factory producing an overlattice of a direct sum from discriminant-group elements. | Recover through `Overlattices()`/gluing vocabulary with typed discriminant-group input data and explicit summand embeddings. |
| `DiscriminantGroups()` | Finite torsion formed modules `L^#/L` with discriminant-form data. | Keep as lattice-specific quotient/form construction, not generic `Quotients()`. |
| `local_modification(M, G, p, check=True)` | p-adic local genus modification algorithm: `M` is a `\ZZ_p`-maximal integral lattice, `G` is the Gram matrix of a lattice isomorphic to `M` over `\QQ_p`, and `p` is a prime; Sage returns an integral lattice in the ambient space of `M` locally equal to `M` away from `p` and locally equivalent to `G` at `p` (`free_quadratic_module_integer_symmetric.py:1708`). Source computes `d = p^valuation_p(denominator(G^-1))`, constructs the `p`-maximal overlattice of `IntegralLattice(G)`, transports through p-adic normal-form matrices for that lattice and `M`, returns the sublattice generated by `((M.span(B) & M) + d*M)`, and optionally asserts p-adic genus-symbol equality against `G`. | Keep as backend/theorem-level local-genus modification witness, not a core constructor or ordinary subobject/overlattice spelling. A public project surface would require typed source lattice, target local Gram/lattice data, prime/place data, local-equivalence and maximality hypotheses, ambient-space equality away from `p`, and proof/witness semantics; do not expose `check` or p-adic normal-form Gram-matrix surgery as lattice API. |

`Lattices(R).ObjectsOver(L)` and `Lattices(R).ObjectsUnder(L)` keep the
lattice-specific `structure_lattice()` and lattice morphism `structure_map()`.
Their former local `structure_domain()` and `structure_codomain()` implementations now
map to the Cat-owned universal structure-morphism methods through
`structure_morphism().domain()` and `structure_morphism().codomain()`.

---

## Sage Type → Spec Category Mapping

| Sage Type | Spec Category | Justification |
|-----------|--------------|---------------|
| `FreeQuadraticModule_generic` | `FormedModules(R).Bilinear()` plus free finite-rank module refinements | free quadratic module over commutative ring |
| `FreeQuadraticModule_generic_pid` | `FormedModules(R).Bilinear()` plus free finite-rank `OverPID()` refinements | adds span/span_of_basis with PID structure |
| `FreeQuadraticModule_generic_field` | `FormedModules(K).Bilinear()` plus free finite-rank `OverField()` refinements | over a field (= vector space with form) |
| `FreeQuadraticModule_submodule_*_pid` | forms-owned bilinear subobjects over PID | submodule of free quadratic over PID |
| `FGP_Module_class` | `Modules(R).FinitelyPresented().OverPID()` | V/W presentation; no form |
| `TorsionQuadraticModule` | `forms.subcategories.torsion_quadratic_modules.TorsionQuadraticModulesCategory` | V/W with Q/mZ-valued bilinear form |
| `FreeQuadraticModule_integer_symmetric` | forms-owned finite-rank free symmetric nondegenerate integral bilinear chain, then `Lattices(ZZ)` | the canonical integral lattice |
| `QuadraticForm` | forms-owned finite-rank free symmetric nondegenerate integral bilinear chain, then `Lattices(ZZ)` | same category; different presentation (upper-triangular coefficients) |

**Note on `QuadraticForm` vs `FreeQuadraticModule_integer_symmetric`**: Both represent
the same mathematical object (an integral lattice). Sage keeps them as separate classes
for historical reasons. Our spec treats them as objects in the same category. Recover
the object through the admitted module/form constructor surface, then refine to the
lattice category when the nondegenerate symmetric integral hypotheses are witnessed.

---

## What Lives in `forms/` vs What Lives in `lattices/`

### Lives in `forms/`

The forms subtree owns the formed-module hierarchy:

- `FormedModulesCategory`
- `BilinearModulesCategory`
- `QuadraticModulesCategory`
- `SymmetricBilinearModulesCategory`
- `AlternatingBilinearModulesCategory`
- `NondegenerateBilinearModulesCategory`
- `DefiniteBilinearModulesCategory`
- `IndefiniteBilinearModulesCategory`
- `IntegralBilinearModulesCategory`
- `RationalBilinearModulesCategory`
- `FreeBilinearModulesCategory`
- `TorsionQuadraticModulesCategory`

### Lives in `lattices/subcategories/`

Only lattice-specific axiom classes live here:

- `over_dedekind.py` — `_LatticesOverDedekindDomain`
- `over_pid.py` — `_LatticesOverPID`
- `over_integers.py` — `_LatticesOverIntegers` (canonical `Lattices(ZZ)`)
- `even.py` — `_EvenLattices`
- `unimodular.py` — `_UnimodularLattices`

The former lattice files for generic formed-module axioms are compatibility shims that
import the forms-owned classes.

### Discriminant groups live in `lattices/subcategories/constructions/discriminant_groups.py`

`DiscriminantGroups(ZZ)` = finite torsion formed modules with discriminant form data.
The full discriminant group method surface lives here, while generic torsion quadratic
module ownership lives in `forms`.

The standard type package is owned by
`lattices/subcategories/constructions/discriminant_groups.py` and re-exported through
`types.py` as `DiscriminantGroup`, `DiscriminantGroupElement`,
`DiscriminantGroupMorphism`, `DiscriminantGroupHom`, `DiscriminantGroupEnd`,
`DiscriminantGroupAut`, and the corresponding Hom/End/Aut category and element names.
These names use the module Hom/End/Aut machinery as the categorical carrier; when the
base category is `Lattices(R).DiscriminantGroups()`, containment is interpreted in the
finite torsion formed-module category, not as raw matrices or Sage torsion backends.

The required finite discriminant-form surface includes named access to the bilinear and
quadratic quotient-valued forms, inherited generic isotropic predicates, finite
`isotropic_elements()` enumeration, and `orthogonal_group()`. Orbit decomposition is
exposed by the resulting `DiscriminantGroupAut` or a typed finite group-action object
through `orbit(x)`, `orbits(S)`, and `orbit_representatives(S)`. Lattice orthogonal
groups expose `discriminant_action()`, `image_in_discriminant_orthogonal_group()`, and
`kernel_of_discriminant_action()` to connect lattice isometries to discriminant-form
automorphisms. These methods are not owned by `nikulin_invariants()`.

---

## Dual Convention: `dual()` vs `lattice_dual()`

The metric dual `L^# = {v ∈ L_K : β(v, L) ⊆ R}` and the Hom dual
`L^* = Hom_R(L, R)` are distinct mathematical objects kept as separate types with
compatible surfaces.

### Justification for the distinction

The degenerate case `<e> ⊂ U` (a generator of the hyperbolic plane) proves the
distinction is necessary:

- The Hom dual `<e>^* = Hom_Z(<e>, Z) ≅ Z`, carrying the evaluation pairing
  `(f, e) ↦ f(e)`. Its elements are functionals.
- The metric dual `<e>^# = {v ∈ <e>_Q : β(v, <e>) ⊆ Z}`. Since
  `β(e, e) = 0`, the condition `β(v, e) ∈ Z` imposes no restriction on the
  rational multiple of `e`. So `<e>^# ≅ Q·e`, not `Z`. These are fundamentally
  different objects — one is rank-1 free, the other is rank-1 rational.

In the nondegenerate integral case, both duals are canonically isomorphic via
`v ↦ β(v, –)`, but the isomorphism is structure-dependent (it uses the form)
and must be recorded explicitly.

### End-user convention: `lattice_dual()`

The **lattice** subcategory overrides the dual surface so that users working with
concrete lattices (integral formed modules) get the expected behavior:

- `L.lattice_dual()` → the metric dual `L^#`, returned as a **lattice** (formed
  module). This is the standard meaning of "dual lattice" in the lattice theory
  literature and the object needed for discriminant group computations.
- `L.dual()` → in the `Lattices(R)` category, `dual()` is overridden to call
  `lattice_dual()`, returning the metric dual. A diagnostic-level log message
  (gated by the global category diagnostic flag) informs the user that the metric
  dual is being returned, not the module-theoretic Hom dual.
- `L.hom_dual()` → the Hom dual `Hom_R(L, R)`, returned as a module. Available
  when the module dual is needed as a bare `R`-module without the transported
  form data.

### Subcategory override rules

- `Modules(R).ParentMethods.dual()` returns `Hom_R(M, R)` (the generic module
  dual). No log warning.
- `Lattices(R).ParentMethods.dual()` overrides to call `lattice_dual()`, with
  the optional log warning described above.
- `BilinearModules(R).ParentMethods.dual()` keeps the generic module dual
  (no form transported). The metric dual `L^#` is accessed via
  `L.dual_lattice()` at `Free + Bilinear.Symmetric.Nondegenerate.Integral`.
- Subcategories of lattices (even, unimodular, over_integers) inherit the
  override from `Lattices(R)`.

### Diagnostic logging convention

All dual-like methods that may confuse metric duals with Hom duals participate
in the global category diagnostic system specified in `[[SPEC-MAPPING-CAT]]`:

- Warnings are gated by the disabled-by-default diagnostic flag.
- When enabled, `L.dual()` on a lattice logs: "Returning metric dual L^#;
  use L.hom_dual() for the Hom-dual module."
- The log level is `INFO` and the message is explanatory, not a correctness
  check.
- The same convention applies to `discriminant_class()` and `inclusion_morphism()`
  — any method that returns or uses metric-dual data.

### Resolved design decisions

- `dual()` is NOT removed from the lattice subcategory. It is overridden to
  return `lattice_dual()`, aligning normative lattice-theory usage.
- `lattice_dual()` is the unambiguous name for the metric dual. No other
  category owns this name.
- `hom_dual()` is the unambiguous name for the module-theoretic Hom dual on
  formed-module categories.

## 6-Gate Protocol Review Log

**Review date:** 2026-05-07
**Reviewer:** Hermes Agent (fresh-context subagent)
**Scope:** Core mathematical correctness — tier table, forms/lattice boundary, discriminant group. Less exhaustive than full Sage file scan; prioritizes mathematical reasoning.

---

### G1 — Source Grounding: PASS

Every mathematical claim in the spec traces to a canonical source. Verified source references:

| Claim | Source checked | Result |
|-------|---------------|--------|
| Source Coverage Ledger (lines 44-63) | 13 installed Sage files probed via `search_files` on `/home/dzack/miniforge3/envs/sage/lib/python3.12/site-packages/sage/` | All 11 named files confirmed present. The former visibility-gap entries for `src/lattices/categories/...` are now classified as retired project-local provenance whose active owners are `category_specs/forms`, `category_specs/modules/subcategories/finitely_presented_over_pid.py`, and `category_specs/lattices`; `TASK-MAPPING-DOC-COMPLETENESS-RESEARCH` is complete. |
| Constructor reconciliation table Sage line references (lines 109-117) | `free_quadratic_module.py:310`, `free_quadratic_module_integer_symmetric.py:625`, `torsion_quadratic_module.py:20/190`, `quadratic_form.py:1150+` | Line ranges verified against Sage 10.7 source. The `__init__` signatures match the spec's descriptions. |
| LatticesCategory chain (lines 247-281) | `/home/dzack/research/category_specs/lattices/__init__.py:68-71` — `_base_category_class_and_axiom = (IntegralNondegenerateSymmetricFiniteRankFreeBilinearModulesCategory, "Lattice")` | Confirmed. The lattice endpoint sits at the end of the forms chain. |
| Forms chain (lines 247-281) | `/home/dzack/research/category_specs/forms/chain.py:30-296` — `FiniteRankFreeFormedModulesCategory` → `Bilinear` → `Symmetric` → `Nondegenerate` → `Integral` | Confirmed. Each link's `_base_category_class_and_axiom` matches. |
| TorsionQuadraticModulesCategory | `/home/dzack/research/category_specs/forms/subcategories/torsion_quadratic_modules.py:17-60` — `super_categories()` returns `FinitelyPresented().OverPID().Torsion().WithForms().Quadratic()` | Confirmed. Abstract methods `gram_matrix_quadratic`, `gram_matrix_bilinear`, `brown_invariant` present; invariant-factor surfaces inherit from the finite-presentation-over-PID owner. |
| DECISION-ORDERED-REAL-SIGNATURE-OWNER | `/home/dzack/research/plans/features/FEATURE-CATEGORY-SPECS-AND-SAGE-SURFACES/decisions/DECISION-ORDERED-REAL-SIGNATURE-OWNER.md` — status: `decided`, chosen: "Add ordered-real-realization refinement" | Confirmed. The spec's `signature_pair()` tier assignment reflects this decision. |
| SPEC-MAPPING-CAT cross-reference | `/home/dzack/research/plans/features/FEATURE-CATEGORY-SPECS-AND-SAGE-SURFACES/specs/SPEC-MAPPING-CAT.md` — exists at 528+ lines | Confirmed. Diagnostic flag gating documented there. |
| Formal Negative findings (lines 180-238) | `.agents/theory/spec-backups/lattices_written_spec_backup.py` — confirmed present; no active `src/lattices/categories/` — confirmed absent | Both findings verified. The docstring model and stale-path conclusions are correct. |

**G1 finding (advisory):** The hierarchy overview diagram (lines 249-255) omits `.Free().FiniteRank()` from the canonical chain. The actual chain in `chain.py` starts at `Modules(R).Free().FiniteRank().WithForms()`. The method placement table correctly prefixes tiers with `Free +` where needed (e.g., `Free + Bilinear` for `gram_matrix()`), so the tier assignments are internally consistent. The diagram is simplified for readability; this is acceptable for a mapping spec but may confuse readers comparing against the Python source.

**G1 verdict: PASS.** All sources resolve on disk. One advisory on diagram simplification.

---

### G2 — Sage Surface Completeness: PASS

The spec accounts for every inventoried Sage lattice surface through four reconciliation tables plus the method placement table:

| Inventory section | Coverage | Check |
|------------------|----------|-------|
| `FreeQuadraticModule` / `IntegralLattice` constructors (lines 109-117) | 8 Sage surfaces mapped | All 8 have owner classification + reconciliation rows |
| Inherited Module and Hom Surfaces (lines 119-130) | 11 surface groups (FGP_Module, FGP_Morphism, FGP_Element, enumeration) | All admitted or routed to correct owners |
| Toric Character-Lattice Boundary (lines 131-147) | 4 surface groups | Correctly routed to free-module/formed-lattice owners; toric-dual convention flagged as interop limitation, not negative claim |
| Lattice-poset name-collision boundary | 3 surface groups | Correctly routed to order-theoretic Posets surfaces, not algebraic `Lattices(R)` |
| Lattice and Form Method Reconciliation (lines 149-163) | 18 surface groups | All have correct owner classification |
| Torsion and Discriminant-Form Reconciliation (lines 165-178) | 11 surface groups | All admitted with codomain/hypothesis precision |
| Method Placement Table (lines 285-362) | 60 method rows | Covers free, bilinear, quadratic, torsion, discriminant-group, and aut-group methods |

**Cross-reference check:** The method placement table rows are consistent with the reconciliation tables. Specifically verified:
- `gram_matrix()` at `Free + Bilinear` ↔ reconciliation says "Admit at the first free bilinear tier" (line 154) ✓
- `dual_lattice()` at `Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` ↔ reconciliation lines 155-156 ✓
- `discriminant_group()` at same tier ↔ reconciliation lines 155-156 ✓
- `is_even()` at `Bilinear.Integral` ↔ reconciliation lines 155 ✓
- `orthogonal_group()` at `Modules(R).WithForms().AutCategory()` ↔ reconciliation lines 160 ✓

**G2 finding 1 (closed):** Inherited module surfaces from the FGP reconciliation table (`cardinality`, `is_finite`, `list`, `__iter__`, `random_element`) are now represented in the Homset mirroring audit as inherited set/module enumeration and runtime surfaces, not lattice-owned methods.

**G2 finding 2 (closed):** The formed `cokernel()` obligation is represented in the inherited-FGP table and Homset mirroring audit. The Sage absence remains source evidence, while the project-owned formed-module quotient formula and lattice discriminant specialization are settled by `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]`.

**G2 verdict: PASS.** All inventoried Sage surfaces accounted. Two advisory findings on documentation completeness.

---

### G3 — Mathematical Correctness: PASS with ADVISORY NOTES

This gate focuses on the tier table, forms/lattice boundary, and discriminant group.

#### 3a. Tier Table Correctness

Verified the mathematical justification for 12 key tier assignments:

| Method | Claimed tier | Mathematical check | Result |
|--------|-------------|-------------------|--------|
| `b(v,w)` | `Bilinear` | Bilinear evaluation defined once form is bilinear | CORRECT |
| `gram_matrix()` | `Free + Bilinear` | Requires basis; entries `b(e_i,e_j)` in R | CORRECT |
| `determinant()` | `Free + Bilinear` | `det(gram_matrix)`; basis-dependent | CORRECT |
| `dual_lattice()` | `Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` | Metric dual `L^# = {v in L_K : beta(v,L) subset R}` requires free (for scalar extension), symmetric, nondegenerate (for finite index), integral (for L subset L^#), over integral domain (for Frac(R)) | CORRECT. Each hypothesis is necessary. |
| `discriminant_group()` | same as `dual_lattice()` | `L^#/L` follows from `dual_lattice()` | CORRECT |
| `signature_pair()` | `Free + Symmetric + ordered real realization` | Inertia after extension to ordered real target; bare integral domain insufficient | CORRECT. Reflects DECISION-ORDERED-REAL-SIGNATURE-OWNER. |
| `is_even()` | `Bilinear.Integral` | `b(m,m) in 2R` requires integrality, not freeness | CORRECT |
| `is_unimodular()` | `Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain` | `L = L^#` depends on `dual_lattice()` | CORRECT |
| `orthogonal_group()` | `Modules(R).WithForms().AutCategory()` | `O(M,b) = Aut(M,b)`; no freeness/nondegeneracy needed for definition | CORRECT. Verified against `chain.py:54-60` where `orthogonal_group()` is defined at `FiniteRankFreeFormedModulesCategory.ParentMethods`. Note: the Python implementation is at the finite-rank-free tier, but the spec correctly places the mathematical definition higher (at generic WithForms). The implementation's lower placement is a runtime convenience; the spec's tier is the correct mathematical owner. |
| `divisibility(v)` | `Bilinear.Symmetric` (element) | `<b(v,w) : w in M>` as submodule; symmetry needed so `b(v,w)` and `b(w,v)` generate same ideal | CORRECT. Note 9 (lines 449-454) provides rigorous justification. |
| `reflection(v)` | `Free + Symmetric + Nondegenerate` with `b(v,v) != 0` (element) | `s_v(w) = w - 2b(v,w)/b(v,v) · v` in scalar extension | CORRECT as a rational/scalar-extension formula; see advisory G3-a below for the lattice-preservation condition. |
| `brown_invariant()` | `Torsion + Quadratic` with quadratic values in `QQ/2ZZ` | Brown invariant of a finite torsion quadratic/discriminant form | CORRECTED in current audit: the Sage source defines `Br(D,q)` for a torsion quadratic module with values in `QQ/2ZZ`, so bare symmetric bilinear torsion data are insufficient. |

**Advisory G3-a (reflection tier):** The reflection formula is a scalar-extension
operation: `b(v,v) != 0` lets `s_v(w) = w - 2b(v,w)/b(v,v) · v` live in the rational or
fraction-field formed object. Nondegeneracy does not imply every nonzero vector is
anisotropic, and it does not imply the resulting reflection preserves the original
integral lattice. For integral lattices the correct automorphism condition is
`2b(v,x)/b(v,v) in R` for every `x in L`, equivalently `2 div(v)/b(v,v) in R` in the
scalar-valued PID setting. Thus the mapping surface is split: the formula belongs to
the finite-free symmetric nondegenerate scalar-extension tier, while membership in
`O(L)` belongs to the lattice Aut object after the lattice-preservation/root condition
is checked. This replaces the misleading "unit-check" phrasing; `b(v,v)=±2` is not a
unit in `ZZ`, but `(-2)`-roots still give integral lattice reflections.

**Closed G3-b (form arity versus tensor type):** The former `form_degree() -> (p,q)`
row conflated two different notions and has been replaced. The formed-module surface
records the arity/tensor-degree data carried by the form object: bilinear forms have
tensor degree `2`, quadratic forms have tensor degree `1`, and the scalar-action
endomorphism records the semilinearity. Tensor variance tuples are owned by the tensor
component surface: installed Sage `free_module_tensor.py:441` defines
`tensor_type()` as `(k,l)` with `k` contravariant and `l` covariant, `tensor_rank()`
as `k+l`, `free_module_alt_form.py:15` identifies an alternating degree-`p` form as
type `(0,p)`, and `finite_rank_free_module.py:2573` identifies a symmetric bilinear
form as a type-`(0,2)` tensor. Thus scalar-valued bilinear forms are `(0,2)` tensors
when represented as tensor components, but the formed-module owner records only the
form datum and its arity.

**Closed G3-c (discriminant sign convention):** Installed Sage source defines
`FreeQuadraticModule_generic.discriminant()` as
`(-1)^floor(rank/2) * det(gram_matrix)`; the source docstring says `r = n/2` for even
rank `n` and `r = (n-1)/2` for odd rank. The method-placement row now records this as
Sage's convention, rather than the generic quadratic-form discriminant sign convention
some readers may expect.

#### 3b. Forms/Lattice Boundary Correctness

The boundary is clean and mathematically justified:

- **forms/** owns: FormedModules, Bilinear, Quadratic, Symmetric, Alternating, Nondegenerate, Definite, Indefinite, Integral, Rational, FreeBilinear, TorsionQuadraticModules (lines 522-537)
- **lattices/subcategories/** owns: OverDedekindDomain, OverPID, OverIntegers, Even, Unimodular (lines 539-547)
- **lattices/subcategories/constructions/** owns: DualObjects, DualLattices, Overlattices, OrthogonalDirectSums, DiscriminantGroups (lines 249-280)

**Verification:**
- The lattice category at `lattices/__init__.py:68` extends `IntegralNondegenerateSymmetricFiniteRankFreeBilinearModulesCategory` with axiom `"Lattice"`. This means the lattice endpoint adds only the `Lattice` axiom name — all formed-module axioms (Bilinear, Symmetric, Nondegenerate, Integral) are owned by forms. Mathematically correct: a lattice is an integral nondegenerate symmetric bilinear module that happens to be free of finite rank, and the "Lattice" axiom is the named endpoint.
- The `Even` and `Unimodular` subcategories are lattice-specific (nonstandard outside lattice theory). Correct ownership.
- The `DualLattices()` construction is correctly distinguished from `DualObjects()` (Hom-dual) — the degenerate case `<e> ⊂ U` (lines 586-594) provides a rigorous counterexample proving the two duals differ. This is strong mathematical evidence.

**Closed G3-d (dual_lattice tier versus graph vocabulary):** Sage written docs define
`dual_lattice()` for integral lattices as `L^∨ = {x in L tensor QQ : (x,l) in ZZ for
all l in L}`, and installed Sage source computes it by spanning
`gram_matrix().inverse() * basis_matrix()`. The mapping owner therefore remains
`Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain`: freeness gives
the scalar extension `L_K`, `OverIntegralDomain` gives `K = Frac(R)`, nondegeneracy
gives the metric-dual identification with a full-rank object, and integrality gives the
inclusion `L -> L^#` needed for `L^#/L`. The local graph currently has
`Modules(R).OverIntegralDomain()` but formed and lattice chains only propagate `OverPID`
and stronger base-ring refinements. That is a visible spec-graph obligation: propagate
the existing module `OverIntegralDomain()` refinement through formed/lattice categories,
or record an explicit proof that a stronger owner is intentionally required. It is not
an implementation-phase concern and does not change the mathematical method owner.

#### 3c. Discriminant Group Correctness

The discriminant group surface is mathematically well-defined:

- `A_L = L^#/L` is a finite abelian group with descended bilinear form `b_L: A_L × A_L → Q/Z` and (when L is even) quadratic form `q_L: A_L → Q/2Z`
- The spec correctly identifies the codomains as quotient-valued (`K/R`, `K/2R`), distinct from integral Gram matrices (lines 170-171)
- `discriminant_action()` is correctly identified as the canonical bridge `O(L) → O(A_L, q_L)` (line 177)
- `nikulin_invariants()` is correctly identified as a convenience invariant under 2-elementary hypotheses, not a substitute for the discriminant group (lines 335, 456-465)
- Orbit methods (`orbit`, `orbits`, `orbit_representatives`) are correctly owned by finite group actions, not by `nikulin_invariants()` (lines 176, 348)
- The type package (`DiscriminantGroup`, `DiscriminantGroupElement`, etc.) correctly uses the module Hom/End/Aut machinery (lines 558-565)

**Closed G3-e (formed cokernel):** Sage provides `kernel`, `image`, and `lift` on `FGP_Morphism` but not a formed `cokernel` with descended bilinear/quadratic form. The project-owned spec obligation is now settled at the formed-module Hom-category owner by `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]`: `coker(f) = codomain(f) / image(f)` with projection, form-codomain cokernel, and cross-term quotient; lattice discriminant descent specializes this to `A_L = coker(L -> L^#)` with codomains `K/R` and `K/2R`.

#### 3d. Picard Lattice vs Picard Group

The task requested checking this distinction. The spec `SPEC-MAPPING-LATTICES.md` does not address Picard lattices or Picard groups. This is correct scope discipline: Picard groups (divisor class groups) and Picard lattices (Néron-Severi lattices with intersection form) are geometry concepts owned by geometry categories, not by the algebraic lattice mapping surface. The distinction is properly treated in the geometry spec `SPEC-HISTORICAL-GEOMETRY-NOUN-SURFACE.md` (line 72: "Picard group is not the Picard lattice"). No mathematical conflation exists in this mapping spec.

**G3 verdict: PASS.** The former reflection, form-degree, discriminant-sign,
dual-lattice tier, and formed-cokernel advisories are closed at the spec level. No tier
assignment is wrong. No boundary is misplaced. The discriminant group surface is
mathematically correct. The remaining dual-lattice graph work is a named
`OverIntegralDomain()` propagation obligation, not a method-owner ambiguity.

---

### G4 — Nonmathematical Rejection: PASS

The spec correctly rejects or marks as interop-only:

| Surface | Classification | Check |
|---------|---------------|-------|
| `ambient_module`, `ambient_vector_space`, `basis_matrix`, `inner_product_matrix`, `degree`, `_repr_` (line 153) | presentation/ambient-cover plus runtime/display interop | Correctly separated from public lattice identity. `ambient_module` and `inner_product_matrix` are presentation/cover data when a selected ambient object is part of the surface; `basis_matrix`, `degree`, and `_repr_` remain implementation/display data. |
| `hom(images)` as main constructor (line 126) | rejected; Hom-parent constructors admitted instead | Correct. `hom(images)` is an ad hoc Sage convenience; the spec routes through `M.Hom(N)` and structured constructors. |
| `discard_basis` option (line 116) | rejected as public option | Correct. Basis changes must be explicit. |
| Sage's variadic `short_vectors(n, **kwargs)` (line 341) | rejected; `short_vectors_up_to_sign(n)` admitted | Correct. The single keyword `up_to_sign_flag` is split into a named method. |
| Raw Sage `Parent`/`Element` surface leaks | absent from public spec | Verified: the spec consistently uses project type names (Lattice, DiscriminantGroup, etc.). |
| `nikulin_invariants()` as discriminant-form owner (line 335) | rejected; convenience tuple only | Correct. The discriminant group + form is the canonical object. |
| `genus()` result owning the method (line 173) | rejected; "do not let the result object own the method" | Correct. The genus is evidence; lattice theorem methods own the verdict. |

**G4 verdict: PASS.** All nonmathematical Sage surfaces correctly rejected or marked interop-only. No "option bag" constructors or raw Sage types leak into the public API.

---

### G5 — Ambiguity Routing: PASS

The spec routes mathematical ambiguities through tracked decision cards:

| Ambiguity | Routed to | Status |
|-----------|----------|--------|
| Signature/definiteness owner (lines 156, 302-304, 377-382) | `[[DECISION-ORDERED-REAL-SIGNATURE-OWNER]]` | `decided` — "Add ordered-real-realization refinement" |
| Diagnostic flag for dual-vs-metric-dual (lines 399-409, 628-639) | `[[SPEC-MAPPING-CAT]]` | exists at `plans/features/.../SPEC-MAPPING-CAT.md` |
| Source-visibility gaps from inventory (line 63) | `[[TASK-MAPPING-DOC-COMPLETENESS-RESEARCH]]` | complete; former `src/lattices/categories/...` pointers are retired project-local provenance, not live installed Sage providers |
| Missing Sage `cokernel` with descended form (line 127) | `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]` | Spec obligation settled; Sage absence remains backend evidence |

**G5 finding (closed):** `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]` settled the module/formed-module Hom-category owner, quotient formula, descent hypotheses, and lattice discriminant-descent routing. No human naming or ownership decision remains for this spec surface.

**G5 verdict: PASS.** One routing recommendation.

---

### G6 — Obligation Preservation: PASS

The spec preserves all Sage obligations and adds project-owned ones:

| Obligation | Status |
|-----------|--------|
| Every Sage lattice/formed-module surface inventoried in SAGE_INVENTORY.md | Preserved — each has a reconciliation row, owner classification, and mapping consequence |
| `element divisibility` (line 159) — Sage does not supply this | Added as required project surface with definition at Note 9 |
| `cokernel` with descended form (line 127) | Settled by `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]`; runtime construction remains downstream and outside this spec workflow |
| `inclusion_morphism()` (line 317) — Sage does not supply this | Added at correct tier |
| `discriminant_form()` (line 316) | Added as named accessor |
| `rational_span()` (line 343) | Added at `Free + OverIntegralDomain` |
| `is_isometric_to()` (line 336) | Added as lattice isometry test |
| `discriminant_class(x)` (line 358) | Correctly owned by metric-dual element |
| `special_orthogonal_group()` / `stable_orthogonal_group()` (lines 330-331) | Correctly routed through Aut-category parent methods |
| Dual convention preservation (lines 578-648) | `dual()` is overridden (not removed); `lattice_dual()` and `hom_dual()` added as unambiguous names |

**G6 finding (closed):** The `cokernel` gap (line 127) is a genuine spec obligation that exceeds current Sage coverage. It is preserved and settled in `[[TASK-FORMED-COKERNEL-DESCENDED-FORM]]`; the remaining non-Sage fact is downstream runtime construction, not spec closure.

**G6 verdict: PASS.** No obligations deleted, weakened, or relocated without grounded replacement.

---

### Summary

| Gate | Result | Blocking? |
|------|--------|-----------|
| G1 Source Grounding | **PASS** | No |
| G2 Sage Surface Completeness | **PASS** | No |
| G3 Mathematical Correctness | **PASS** with reflection, form-degree, discriminant-sign, dual-lattice tier, and cokernel items closed | No |
| G4 Nonmathematical Rejection | **PASS** | No |
| G5 Ambiguity Routing | **PASS** | No |
| G6 Obligation Preservation | **PASS** | No |

**Overall verdict: PASS.** The spec is mathematically sound, the tier table is correct,
the forms/lattice boundary is clean and rigorously justified, and the discriminant
group surface is complete. The former `dual_lattice()` tier/category graph advisory is
closed as a source-backed `OverIntegralDomain()` propagation obligation. The former
reflection, form-degree, discriminant-sign, and formed-cokernel gaps are closed at the
spec level.

**Closed local advisories:**
- G3-d: `dual_lattice` tier — Sage docs/source settle the metric-dual owner at
  `Free + Bilinear.Symmetric.Nondegenerate.Integral + OverIntegralDomain`; the current
  graph gap is the explicit spec obligation to propagate existing
  `Modules(R).OverIntegralDomain()` vocabulary through formed/lattice chains or record
  a stronger-owner proof.
- G3-a: reflection is split between the scalar-extension formula and the
  lattice-preservation/root condition for membership in the original lattice Aut
  object.
- G3-b: formed-module arity is split from tensor-component `tensor_type()`; bilinear
  forms have arity `2`, and scalar-valued bilinear tensor representatives have type
  `(0,2)`.

---

## Compatibility Paths

`modules/subcategories/with_forms.py`, `modules/subcategories/bilinear.py`,
`modules/subcategories/quadratic.py`, `modules/subcategories/torsion_quadratic_modules.py`,
and the old generic formed-module files in `lattices/subcategories/` re-export the
forms-owned classes. They exist only to preserve old import paths.

Former ordinary lattice-element calls to `v.discriminant_class()` are represented as
`L.inclusion_morphism()(v).discriminant_class()` or, equivalently, the zero element of
`L.discriminant_group()`.

Former lattice-object calls to `L.special_orthogonal_group()` and
`L.stable_orthogonal_group()` are represented by first taking the lattice aut object:
`L.orthogonal_group().special_orthogonal_group()` and
`L.orthogonal_group().stable_orthogonal_group()`. The subgroup constructors live on
`Lattices(R).AutCategory().ParentMethods`; `special_subgroup()` and
`stable_subgroup()` are the primitive subgroup selectors there.
