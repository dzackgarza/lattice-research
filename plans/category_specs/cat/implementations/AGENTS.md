# cat implementations

This directory is intentionally empty of implementation modules for now.

`Cat()` currently declares category-object, functor, and construction-category
surfaces only. Add implementation files here only when a nontrivial algorithm is
needed; trivial Sage wiring belongs on the category surface.
