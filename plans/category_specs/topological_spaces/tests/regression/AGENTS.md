# AGENTS.md — topological_spaces/tests/regression

Regression tests here cover admitted topological-space behavior. Named sets with
topological structure should be built through `Sets().Constructors()` and then tested
through their `TopologicalSpaces()` refinements.
